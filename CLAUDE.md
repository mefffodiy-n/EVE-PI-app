# PI Director — контекст проекта

Планировщик планетарного производства для EVE Online. Считает, сколько колоний
и каких нужно, распределяет их по персонажам и показывает, где план упирается
в ограничения.

Этот файл — то, что нужно знать, прежде чем что-то менять. Правила ниже не
пожелания: часть из них проверяется тестами, и нарушение роняет сборку.

---

## Правила проекта

**1. Никаких выдуманных данных.** Все числовые константы происходят из
проверенных источников и сверены между собой. Если значения нет — так и
пишется «нет данных». Правдоподобное, но ложное число хуже честного пробела.
Прецедент: в первой версии фронтенд рисовал `Math.random()` как таймер
экстрактора, и это выглядело работающим.

**2. Приоритет источников при расхождении.** Побеждают JSON-шаблоны застройки:
их маршруты ссылаются на игровые `type_id`, а не на названия, в которых бывают
опечатки. Названия проверяются по всем источникам, при сомнениях — поиск.
Так был разрешён конфликт по `Industrial Explosives` (Polytextiles, не
Polyaramids) и удалён несуществующий `Positron Cord`.

**3. Пользователь не инициирует обращения к ESI.** Ни один HTTP-обработчик не
ходит в сеть. Всё внешнее собирают скрипты по расписанию, страницы читают
готовые снимки. Проверяется тестом: `urlopen` подменяется на исключение, и
эндпоинт всё равно обязан ответить.

**4. Flask, не FastAPI. Никакого Docker.** Обычное WSGI-приложение за nginx.

**5. Не нагружать сервер при большом числе пользователей.** Flask синхронный,
поэтому: справочники считаются раз на процесс и отдаются с ETag, расчёт плана
кэшируется по параметрам и выполняется вне блокировки, размер запроса ограничен.

**6. Справка и «О проекте» обновляются вместе с приложением.** Реестр
возможностей — `domain/features.py`, проверка — `tests/test_docs.py`. Порядок:
код → запись в реестр → текст на двух языках → тест зелёный. Если тест упал,
правьте справку, а не реестр.

**7. Версия живёт только в `version.py`.** Оттуда её берут API, окно «О проекте»
и `User-Agent`. Дубликат в другом файле роняет тест. Раньше номер был вписан
трижды и разошёлся: 0.5.0, 0.6.0 и 0.1.0 одновременно.

**8. Целостность вёрстки проверяется тестом, а не глазами.**
`tests/test_frontend_integrity.py`. Причина: объявление вне блока CSS парсер
принимает за селектор и выбрасывает вместе с ним **следующее** правило.
Ломается не то, что правили. Так дважды молча пропадали рабочие стили.

---

## Устройство

```
domain/           расчёты, без Flask и без сети
  recipes.py      дерево рецептов P0-P4
  planets.py      данные о планетах: сырьё, радиус, регионы
  capacity.py     CPU/PG: структуры + головы + линки против лимита CC
  templates.py    разбор игровых JSON-шаблонов застройки
  throughput.py   производительность фабрик, разворот потребности
  factory_site.py подбор планет под переработку
  planner.py      распределение персонажей по планетам
  profit.py       выгода в ISK на колонию-час
  advice.py       подсказки по вместимости пула персонажей
  direct_p2.py    P2 целиком на добывающей планете
  plan_storage.py сохранение и сравнение планов
  features.py     реестр возможностей (см. правило 6)

api/              Flask, только чтение готовых данных
  __init__.py     фабрика приложения, отдача web/, обработка ошибок
  cache.py        LRU-кэш, ETag, разбор тела запроса
  blueprints/     reference, plans, market, export, meta

scripts/          всё, что ходит в сеть или готовит данные
  esi_client.py            единая точка обращений к ESI
  refresh_server_status.py статус сервера по расписанию
  refresh_market_prices.py цены по расписанию
  scheduler.py             запуск сборщиков без внешних зависимостей
  extract_schematics.py    количества вход/выход из шаблонов
  seed_dev_characters.py   заглушки персонажей (нет ESI-токенов)
  diagnose.py              самодиагностика
  check_templates.py       разбор набора шаблонов
  cleanup.py               уборка от файлов прошлых этапов

data/             только чтение
  recipes.json              68 продуктов, сверено с вики: ноль расхождений
  pi_reference.json         структуры, ёмкость CC, модель линков, допущения
  eve_webtools_reference.json  type_id, раскладка сырья по типам планет
  type_ids.json             91 запись, включая структуры и командные центры
  templates/                68 игровых шаблонов (только вариант 00)
  planet_industry.csv       968 планет региона
  schematics.json           создаётся локально, в git не хранится

web/index.html    весь интерфейс одним файлом, без сборки
```

---

## Что известно точно, а что нет

**Проверено по четырём источникам** (шаблоны DalShooth, их README,
eve-webtools.com, вики EVE University): рецепты и количества, стоимости
структур, ёмкость командных центров, длительности циклов, модель линков
`L = 0.012·radius + 1`, `cpu = 15 + 0.2·L`, `pg = 10 + 0.15·L`.

**Не подтверждено, помечено в `data/pi_reference.json` → `assumptions`:**

- **Число планет на персонажа** — принято `Interplanetary Consolidation + 1`.
  Единственное непроверенное число в самом расчёте. Возвращается в ответе
  `/api/calculate` в поле `assumptions`.
- **Стоимость второго причала** — README даёт 3600 CPU за один и 5200 за два,
  хотя 2×3600 = 7200. Взято консервативное 7200. Решает, применим ли двойной
  P4-шаблон на планетах крупнее 9 600 км (иначе предел 61 700). Проверяется
  постановкой двух P4-шаблонов на планету ~20 000 км при CCU V.
- **Компоновка прямого P2** — готового шаблона в источнике нет, состав
  посчитан из стоимостей структур. Сами стоимости проверены.
- **Плотность сырья считается постоянной.** В игре месторождения истощаются,
  живых данных нет. Есть множитель `extraction_margin`, но это поправка,
  а не расчёт.

---

## Механики игры, важные для расчёта

- Один персонаж — одна колония на планету. **Разные** персонажи могут ставить
  колонии на одну планету без ограничений, поэтому нехватка планет не является
  ограничением: упирается всё в число персонажей.
- Два шаблона на планету требуют Command Center Upgrades V.
- Добывающий шаблон (00) требует CCU IV: при III он перегружает PG на планете
  любого размера. Это расчётный вывод, в источнике его нет.
- P4 ставится только на Barren и Temperate — правило игры. Для P2/P3 это наше
  предпочтение (мельче планета — дешевле линки), и при нехватке таких планет
  P2/P3 размещаются на других типах с предупреждением.
- Используются только шаблоны варианта 00; LS пропускаются при загрузке.

---

## Запуск

```
pip install -r requirements.txt
python run.py                      # http://127.0.0.1:8000/ — именно корень
python -m pytest tests/ -q         # все тесты
python -m scripts.diagnose         # проверка данных и эндпоинтов
python -m scripts.scheduler        # сборщики по расписанию
```

Первый запуск требует `python -m scripts.extract_schematics --write` —
без `data/schematics.json` план не считается.

Для правки вёрстки удобен Live Server; тогда сервер запускается как
`PI_DEV_CORS=1 python run.py`, фронтенд сам определит другой origin.

---

## Что дальше (см. roadmap.md)

**Фаза 3 — подключение к игре.** Заблокирована одним шагом: зарегистрировать
приложение на developers.eveonline.com и получить `client_id`. Дальше OAuth,
шифрование токенов, синхронизация скиллов и колоний. Это оживит кольца циклов
и заменит dev-заглушки настоящими персонажами.

**Фаза 6 — развёртывание.** Сейчас приложение живёт, пока открыт терминал.
Нужны служба (waitress за nginx), автозапуск сборщиков, резервные копии
`data/plans/`.

**Осталось от Фазы 1:** проверить в игре расхождение по второму причалу.

---

## Как здесь принято работать

- Проверять числа, а не доверять им. Каждое расхождение между источниками
  разобрано и записано в `_source` соответствующего файла данных.
- Тесты пишутся на то, что уже ломалось. Половина тестов существует потому,
  что соответствующая ошибка была допущена и найдена.
- Ошибки признаются и чинятся в том же заходе. В комментариях к коду часто
  сказано, какая именно ошибка привела к текущему решению — это не мусор,
  а причина, по которой не стоит «упрощать обратно».
