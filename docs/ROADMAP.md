# EVE PI Manager — Roadmap (v2)

> Документ основан на анализе предыдущей реализации (репозиторий `mefffodiy-n/EVE-PI-app`,
> файлы `main.py`, `recipes.json`, `planet industry.csv`, `requirements.txt`, `readme.md`).
> Все константы (рецепты P1–P4, плотности сырья по планетам) переносятся из существующих
> файлов без изменений — они верны и переиспользуются как есть.

## 0. Принципы, обязательные для новой архитектуры

1. **ESI дергают только фоновые сборщики по расписанию.** Ни один пользовательский
   запрос (открытие страницы, расчёт плана, экспорт) не должен инициировать вызов ESI
   напрямую или косвенно (ни синхронно, ни "лениво при первом обращении").
2. **Минимум ESI-запросов в принципе:**
   - Type ID / SDE-справочники (имена P0–P4, id) — не запрашивать вообще при каждом
     старте; собрать один раз и закоммитить как статический JSON (как уже сделано для
     `recipes.json`), обновлять раз в релиз/раз в квартал через отдельный ручной скрипт,
     а не при запуске сервера.
   - Данные, которые вообще не требуют ESI (рецепты, плотности сырья по планетам,
     расчёт CPU/PG, дерево цепочек) — считать локально, как сейчас и делается в
     `recipes.json` + CSV.
   - Данные, которые обязаны идти через ESI (скиллы персонажа, статус колоний,
     таймеры экстракторов) — только через фоновые сборщики по расписанию
     (`scripts/` + `scripts/scheduler.py`), независимо от HTTP-запросов
     пользователей. Единственное исключение — сам OAuth-flow: обмен кода на
     токен по природе синхронный, но разовый (см. `CLAUDE.md`, правило 3).
3. **Стек: Flask, без Docker.** FastAPI заменён на Flask; развёртывание —
   обычное WSGI-приложение (waitress на Windows, gunicorn на Linux) за nginx.
4. **Приложение не должно тяжело нагружать сервер при большом числе
   пользователей.** Flask синхронный, поэтому долгая работа в обработчике
   блокирует воркер целиком. Механизмы (реализованы в `api/cache.py`):
   справочники считаются один раз на процесс и отдаются с ETag (304 на
   повторный запрос — тело не формируется вовсе); расчёт плана кэшируется
   по параметрам и выполняется вне блокировки; жёсткие лимиты на размер
   запроса (20 продуктов на расчёт, 500 строк на экспорт); CSV читается
   и очищается однократно. Кэш — в памяти процесса; Redis вводить только
   после того, как нагрузка подтвердится измерением.
5. **Никакой имитации ESI-данных в проде.** Мок-персонажи (`Factory Chief`, `Miner N`)
   допустимы только в fixtures для тестов/дев-окружения, не в основном коде.
6. **Заглушки персонажей — легитимный инструмент отладки, но не часть auth-эндпоинта.**
   На момент разработки нет действующих ESI-токенов, и это нормально: логика
   планирования (`planner.py`, `capacity.py`) должна отлаживаться на тестовых
   персонажах независимо от того, готова ли ESI SSO. Требование — физически
   развести два пути:
   - боевой `/api/auth/callback` сразу пишется под реальный ESI OAuth (PKCE) и
     просто не будет работать, пока нет `client_id`/`client_secret` от
     developers.eveonline.com — это ожидаемо и не блокирует остальную разработку;
   - тестовые персонажи заводятся отдельным dev-only механизмом
     (`scripts/seed_dev_characters.py` или `POST /api/dev/seed-characters` под
     фиче-флагом `ENV=dev`), который пишет в ту же таблицу `characters`, что и
     реальный OAuth-flow, — благодаря этому `planner.py` не отличает, откуда взялся
     персонаж, и код не придётся переписывать, когда появятся настоящие токены.

7. **Справка и «О проекте» — часть поставки, а не приложение к ней.**
   Любое изменение функциональности, версии или данных сопровождается
   правкой этих разделов в том же заходе. Правило проверяется тестом
    по реестру : каждая заявленная
   возможность обязана быть описана на обоих языках.

## 1. Итоги аудита прошлой версии (кратко)

| Область | Проблема | Решение в v2 |
|---|---|---|
| ESI/Auth | `/api/auth/callback` не делает реальный OAuth-обмен, вставляет мок-персонажей | Полноценный ESI SSO flow (PKCE), токены только через фоновый refresh-worker |
| ESI/Auth | Тестовые персонажи вшиты прямо в боевой auth-роут — заглушку нельзя отключить, не переписывая эндпоинт | Заглушки выносятся в отдельный dev-only механизм (`scripts/seed_dev_characters.py`), не пересекающийся с боевым `/api/auth/callback` |
| ESI/частота | TypeID грузятся с ESI при каждом рестарте процесса | Статический `data/type_ids.json`, обновляется вручную/CI, не при старте |
| Токены | access/refresh хранятся в SQLite открытым текстом | Шифрование at rest (Fernet/libsodium), отдельная таблица `credentials` |
| Расчёты | CPU/PG — захардкоженные числа (95/85/75) | Реальная модель CPU/PG по типу Command Center и структурам |
| Расчёты | Штрафы на радиус планет заявлены, но не реализованы | Отдельный модуль `logistics/link_penalty.py` на основе `Radius [km]` из CSV |
| Данные | Рассинхрон имён (`Polyramids` vs `Polyaramids`) | Единый источник правды — нормализованный `recipes.json`, валидация при импорте CSV |
| Данные | Правая часть CSV (плотности для прямого R0→P2) не используется | Отдельный сценарий "прямое P2-производство" в планировщике |
| Инфраструктура | SQLite, без миграций, без multi-tenant | PostgreSQL + Alembic, per-user изоляция данных |
| Инфраструктура | Нет планировщика | APScheduler/Celery beat — единственная точка входа к ESI |
| Frontend | `web/index.html` — готовый SPA (751 строка), но FastAPI его не отдаёт: открывается как файл, отсюда хардкод `API_BASE='http://localhost:8000/api'` и вынужденный `CORS: *` | Смонтировать `web/` через `StaticFiles` — снимает и хардкод, и необходимость в `CORS: *` |
| Frontend | **Фиктивные таймеры экстракторов**: `index.html:475` дорисовывает `hours_left: Math.random()*22`, и это отрисовывается как настоящее время (красная пульсация при <2ч, сортировка «по времени таймера») | Поле `extractor_hours_left` приходит с бэкенда; `None` = «неизвестно» и так и показывается. Реальные данные — из `sync_colony_status` (Фаза 3) |
| Данные | `templates/*.json` (шаблоны застройки) не читает **ни бэкенд, ни фронтенд** — мёртвые данные, хотя readme обещал расчёт CPU/PG по ним | Каталог шаблонов в `data/pi_reference.json`, реально используется в `domain/capacity.py` |
| Данные | **Числа в `miner_p1.json` не соответствуют источнику**: заявлено `12400 CPU / 16800 PG` для «Miner + 10 Basic Factories». `12400` — это полный CPU шаблона P4 на 1 фабрику, `16800` — PG от 24 Advanced-фабрик. Два несвязанных числа из разных строк | Файл удалён. Реальный 00-шаблон майнера: 1 экстрактор (10 голов) + 8 basic-фабрик + storage + launchpad, 10 линков = **6100 CPU / 10400 PG** без голов и линков |
| Расчёты | Константы v1 (16 фабрик для P4, 24 для P2/P3) неявно предполагали Command Center Upgrades V у **всех** персонажей — это ровно варианты шаблонов «2 шаблона на планету». Значение 12 для P1 не соответствует ни одному шаблону (у 00 — 8 фабрик, у LS — 4) | Доступность вариантов проверяется явно через `available_templates_for(ccu, planet_type)` |
| Логика | «Рекомендация» продукта = `max(цена за единицу)` по Jita buy, с ручным исключением Water/Oxygen — цена за единицу P4 несопоставима с P1, объём производства и затраты не учитываются | Считать ISK/час на цепочку, а не максимум цены |
| ESI/частота | Кнопка «SYNCING FUZZWORK» во фронте инициирует поход сервера на market.fuzzwork.co.uk (если кэшу >1ч) — пользовательское действие вызывает внешний запрос | `/api/market/best-product` только читает снапшот; наполняет его `refresh_market_prices` по расписанию |

## 2. Извлечённые константы (источник правды — без изменений)

- **Рецепты производства (P1→P4):** переносятся из `recipes.json` как есть — структура
  `{ "type": "P1|P2|P3|P4", "inputs": {...}, "source": "..." }` уже корректна и
  используется как основной справочник в новом модуле `domain/recipes.py`.
- **Плотности сырья по планетам региона Fountain** (16.09.2026 —
  подтверждено пользователем, см. раздел 7): переносятся из `planet industry.csv`
  (столбцы `Constellation; System; Planet; Type; Radius [km]; POCO Tax Rate [%];
  POCO Owner; <34 R0-ресурса>; <28 P2-ресурсов для прямого R0→P2>`).
  При импорте — обязательная нормализация заголовков (устранение опечаток
  `Polyramids→Polyaramids`, `Supertensil→Supertensile`) и валидация на дубликаты/`#REF`.
- **Шаблоны застройки и характеристики структур:** источник —
  https://github.com/DalShooth/EVE_PI_Templates (README + разбор самих
  JSON-шаблонов; копии также на wiki.eveuniversity.org с разрешения автора).
  Выгружено в `data/pi_reference.json`:
  - ёмкость командного центра по уровням Command Center Upgrades (0–5):
    от `1675 CPU / 6000 PG` до `25415 CPU / 19000 PG`;
  - стоимость каждой структуры: ECU `400/2600`, basic-фабрика `200/800`,
    advanced `500/700`, high-tech `1100/400`, storage `500/700`,
    launchpad `3600/700`, голова экстрактора `110/550`;
  - **модель линков формулой**, а не таблицей: `L = 0.012·radius + 1`,
    затем `cpu = 15 + 0.2·L` и `pg = 10 + 0.15·L` на один линк.
    Формула воспроизводит все 60 точек 5 таблиц README с отклонением < 1
    и, в отличие от таблиц, работает при любом числе линков — это
    обязательно, потому что у 00-шаблона майнера 10 линков, а такой
    таблицы в README нет;
  - каталог шаблонов (**только варианты 00**, LS исключён решением проекта);
  - пропускная способность фабрик и рекомендованные уровни скиллов.

  Стоимости структур сходятся со строками README для вариантов на 1 фабрику
  точно (6 проверок). Набор шаблонов разобран полностью: 68 файлов
  (45 P2/P3, 8 P4, 15 майнеров 00), составы совпали с каталогом.

  **Два расхождения внутри источника**, оба зафиксированы в `_source`:
  1. Предупреждение про перегрузку майнера выше 15 000 км не сходится
     с таблицами README (расчёт даёт ~26 000 км для LS). На 00-вариант
     не влияет — там 10 голов, а не 16.
  2. README указывает `Starport 3600 CPU` для варианта на 1 фабрику,
     но `Starport x2 = 5200` для варианта на 2, хотя 2×3600 = 7200
     (по PG при этом сходится). Модель берёт консервативные 7200.
     **Цена вопроса велика**: для `p4_2factory` при CCU V предельный
     радиус планеты — 9 600 км против 61 700 км.
     **Закрыто без проверки (16.09.2026, см. раздел 6, Фаза 1):** в
     регионе Fountain нет Barren/Temperate планеты нужного радиуса
     (~20 000 км), проверить в игре не на чем — консервативное значение
     остаётся; управляется полем
     `assumptions.launchpad_count_for_two_templates`.

- **Сами JSON-шаблоны** разбираются модулем `domain/templates.py`:
  пины, линки, маршруты, схемы, число голов экстрактора. Это нужно не
  только для расчёта CPU/PG, но и чтобы подставить `CmdCtrLv` под уровень
  конкретного персонажа и отдать готовый файл для импорта в игру —
  README источника описывает эту правку как ручную операцию.

## 3. Предлагаемая структура приложения

> **Историческая заметка.** Диаграмма ниже — исходное предложение. По факту
> фоновые сборщики лежат плоско в `scripts/` (без `workers/jobs/`), `infra/`
> — это `config.py`/`db.py`/`models.py`/`crypto.py` (без вложенного `db/`),
> добавились `credentials.py`, `logging.py`, `esi_sso.py`, `serve.py`,
> `backup.py`, `sync_*`. Актуальное устройство — в `CLAUDE.md`, раздел
> «Устройство».

Маршруты намеренно плоские (`/api/initial-data`, `/api/calculate`, ...) —
именно так их вызывает существующий `web/index.html`. Менять контракт без
нужды нельзя: рабочий фронтенд пришлось бы переписывать.

```
pi-director/
├── run.py                    # точка входа (разработка и WSGI)
├── api/                      # Flask — только чтение из БД/кэша, без вызовов ESI
│   ├── __init__.py           # фабрика приложения, отдача web/, обработка ошибок
│   ├── cache.py              # LRU-кэш, ETag, валидация тела запроса
│   └── blueprints/
│       ├── reference.py      # GET /api/initial-data, POST /api/systems, /api/thresholds
│       ├── plans.py          # POST /api/calculate (кэшируется)
│       ├── market.py         # GET /api/market/best-product (ЧТЕНИЕ КЭША)
│       └── export.py         # POST /api/export
├── web/
│   └── index.html            # существующий SPA, переносится как есть
├── workers/                  # ЕДИНСТВЕННОЕ место, откуда идут вызовы ESI (Фаза 2-3)
│   ├── scheduler.py
│   ├── jobs/
│   │   ├── sync_character_skills.py
│   │   ├── sync_colony_status.py     # даст РЕАЛЬНЫЕ таймеры экстракторов
│   │   ├── refresh_tokens.py
│   │   └── refresh_market_prices.py
│   └── esi_client.py
├── domain/
│   ├── recipes.py            # загрузка data/recipes.json, дерево P0-P4
│   ├── planets.py            # загрузка data/planet_industry.csv, нормализация
│   ├── capacity.py           # структуры, лимиты CC, линки по радиусу, расчёт CPU/PG
│   ├── templates.py          # разбор игровых JSON-шаблонов застройки
│   ├── factory_site.py       # подбор планет под переработку + предупреждения
│   ├── throughput.py         # производительность фабрик, разворот потребности
│   └── planner.py            # построение плана (сменяет calculate_plan)
├── infra/                    # Фаза 2-3
│   ├── db/
│   ├── crypto.py
│   └── config.py             # флаг ENV=dev|prod
├── data/
│   ├── recipes.json
│   ├── schematics.json       # создаётся локально из шаблонов, не в git
│   ├── planet_industry.csv   # переименован (был с пробелом в имени)
│   ├── type_ids.json         # статический снапшот, не запрашивается рантаймом
│   ├── pi_reference.json     # структуры, ёмкость CC, модель линков (см. раздел 2)
│   └── templates/            # игровые JSON-шаблоны (варианты 00; LS пропускаются)
├── scripts/
│   └── seed_dev_characters.py
└── tests/
```

Что удаляется из старого репозитория: `__pycache__/` и `pi_director.db`
(бинарная БД в git), плюс `planet industry.csv` переименовывается без пробела.

## 4. Функциональные изменения относительно v1

- **Реальная ESI SSO авторизация** (PKCE) вместо мок-callback; после авторизации —
  персонаж и его refresh-token попадают в очередь фонового воркера, а не
  обрабатываются в HTTP-хендлере.
- **Синхронизация скиллов и колоний** — отдельные периодические джобы
  (`sync_character_skills`, `sync_colony_status`) с собственным интервалом (например,
  раз в 6–24 часа), independent от активности пользователей в UI.
- **Настоящий расчёт CPU/PG**: потребление = структуры шаблона + головы
  экстракторов + линки; лимит = ёмкость CC при данном уровне CCU.
- **Радиус планеты реально влияет на расчёт** — линки дорожают с радиусом,
  поэтому одна и та же застройка влезает на маленькую планету и не влезает
  на большую. В v1 колонка `Radius [km]` читалась из CSV и не использовалась.
- **Два шаблона на одну планету** (`p2p3_2factory` / `p4_2factory`) —
  доступны только при Command Center Upgrades V. Это вдвое сокращает число
  нужных планет, поэтому напрямую влияет на весь план и проверяется явно
  для каждого персонажа.
- **Ограничения по типу планеты**: P4-шаблоны — только Barren и Temperate.
- **Правило подбора площадок под переработку** (решение проекта, сужает
  источник — там P2/P3 разрешены на любом типе): вся переработка P2–P4
  идёт только на Barren и Temperate, планеты берутся **по возрастанию
  радиуса**, поскольку линки дорожают с размером.
- **Предупреждение вместо тихого отката.** Если в выбранной пользователем
  домашней системе планеты крупнее предела для двух шаблонов, планировщик
  не подставляет молча одиночный вариант, а сообщает: самая мелкая планета
  такая-то, предел такой-то, и предлагает выбрать — другая домашняя
  система или один шаблон на планету (вдвое больше планет и персонажей).
  Одиночный вариант применяется только по явному согласию
  (`allow_single_template_fallback`).
- **Генерация готовых шаблонов под персонажа**: подстановка `CmdCtrLv`
  в исходный JSON и выдача файла для импорта в игру.
- **Поддержка прямого сценария R0→P2** — вторая часть CSV-матрицы, которая в v1
  импортировалась, но никогда не использовалась.
- **Мультирегиональность** — CSV-импорт делается как отдельная фоновая задача с
  версионированием набора данных (регион/дата снапшота), а не единственный
  захардкоженный файл, тянущийся в память при старте.
- **Мультитенантность** — персонажи и планы привязаны к пользователю/аккаунту,
  а не к общей таблице `characters`.
- **Подключение существующего фронтенда** — `web/index.html` монтируется через
  `StaticFiles`, что убирает хардкод `API_BASE='http://localhost:8000/api'`
  и снимает необходимость в `CORS: *` (фронт станет same-origin).
- **Устранение фиктивных данных в UI** — убрать `Math.random()` из расчёта
  времени экстракторов; пока бэкенд не отдаёт реальное значение, показывать
  «неизвестно», а не правдоподобное число.
- **Задействование шаблонов застройки** — характеристики из
  `data/pi_reference.json` реально влияют на расчёт (в v1 шаблоны лежали
  в репозитории без использования, да ещё и с неверными числами).

## 5. Технологический стек (предложение)

- **Backend**: Flask (замена FastAPI по правилам проекта). Валидация тела
  запроса — вручную в `api/cache.py::parse_json_body`, без Pydantic:
  задача минимальна (не пустить мусор в domain), отдельная схема избыточна.
- **WSGI-сервер**: waitress (Windows, основная среда разработки) либо
  gunicorn (Linux). Docker не используется.
- **БД**: SQLAlchemy 2.0 + Alembic. На разработке — SQLite `data/pi_director.db`
  (реализовано, Фаза 3); в проде — PostgreSQL (нужен для multi-tenant и
  конкурентного доступа воркеров и API), переключение сменой `PI_DATABASE_URL`.
- **Планировщик**: APScheduler (если инфраструктура простая, один процесс) или
  Celery + Redis (если нужна отказоустойчивость/несколько воркеров) — в любом
  случае это единственная точка вызова ESI.
- **Кэш**: Redis для рыночных цен и справочников (замена `MARKET_CACHE` в памяти
  процесса — сейчас кэш теряется при каждом рестарте).
- **Frontend**: оставляем как есть — `web/index.html` это самодостаточный SPA
  (Tailwind через CDN, весь JS inline, без билд-шага). Переписывать не нужно;
  требуются только точечные правки: убрать `Math.random()`-таймеры и заменить
  хардкод `API_BASE` на относительный путь после монтирования статики.
  Сборщик/фреймворк не вводим — для одного файла это лишняя сложность.
- **Секреты**: `.env`/Vault, шифрование токенов в БД, `POCO Owner`/регион — конфиг,
  а не хардкод.
- **Кэш**: на Фазе 1 — в памяти процесса (`api/cache.py`), без внешних
  зависимостей. Redis вводится в Фазе 2 вместе с воркерами, когда
  появится необходимость делить снапшот цен между процессами.

## 6. Дорожная карта по фазам

### Фаза 1 — Фундамент (без ESI вообще)
- [x] `recipes.json` и `planet_industry.csv` — read-only seed-данные, читаются
      слоем `domain/` с кэшированием на процесс.
- [x] Нормализация заголовков CSV (опечатки `Polyramids`, `Supertensil`),
      нормализация радиуса (разряды через пробел) и отсев служебных строк.
- [x] `domain/capacity.py` — расчёт CPU/PG: структуры + головы экстракторов +
      линки по радиусу против лимита CC. Проверен против чисел источника.
- [x] `domain/templates.py` — разбор игровых JSON-шаблонов (пины, линки,
      маршруты, схемы) и подстановка `CmdCtrLv` под уровень персонажа.
- [x] `domain/factory_site.py` — подбор планет под переработку: только
      Barren/Temperate, по возрастанию радиуса, с предупреждением и
      выбором вместо тихого отката на одиночный шаблон.
- [x] `domain/planets.py` — нормализация радиуса (в CSV разряды разделены
      пробелами, без очистки колонка читается как строка и любое сравнение
      радиуса молча даёт неверный результат) и выборки по системе.
- [x] `domain/throughput.py` — количества вход/выход извлекаются из маршрутов
      реальных шаблонов (`scripts/extract_schematics.py`), а не задаются
      константами. Тот же скрипт строит карту «имя → type_id», закрывая
      `type_ids.json` без обращения к ESI.
- [x] `domain/planner.py` — распределение персонажей: переработка в домашней
      системе, добыча по плотности сырья, подбор персонажа по требуемому
      для конкретной планеты уровню CCU, учёт слотов, честный дефицит.
      Подключён к `/api/calculate`.
- [x] `scripts/seed_dev_characters.py` — 13 dev-персонажей с разной прокачкой,
      включая CCU IV и III, чтобы проверялся не только счастливый путь.
- [x] Пороговые радиусы показаны под выбором домашней системы: предел для
      двойного шаблона по P2/P3 и P4 и сколько планет системы ему подходят.
      Для этого добавлен `/api/system-planets`.
- [x] Выбор при упоре в размер планет: план не строится наполовину молча,
      появляется блок с двумя кнопками. Согласие уходит на сервер как
      `allow_single_template_fallback`, и приложение остаётся на вкладке
      настроек — пустой дашборд ничего не объяснил бы.
- [x] Flask-слой: маршруты под контракт существующего фронтенда, единый
      формат ошибок, ETag, лимиты размера запроса, кэш планов.
- [x] Отдача `web/index.html` с того же origin — CORS не нужен вовсе.
- [x] `API_BASE` во фронтенде заменён на относительный `/api`. Абсолютный
      адрес ломал все запросы при открытии по 127.0.0.1 вместо localhost:
      разные origin, а CORS отключён — списки молча оставались пустыми.
- [x] `scripts/diagnose.py` — самодиагностика: проверяет файлы данных,
      разбор CSV, схемы, персонажей, эндпоинты и фронтенд, называя причину.
- [x] Справочный эндпоинт больше не падает при отсутствии CSV: продукты
      отдаются всегда, а проблемы приходят в поле `data_problems` и
      показываются в интерфейсе.
- [x] `Math.random()` для таймеров убран вместе со старым интерфейсом.
      Теперь `hours_left = null` означает «неизвестно» и так и показывается.
- [x] `Positron Cord` удалён из `recipes.json` (68 продуктов).
- [x] `Industrial Explosives` исправлен на Fertilizer + Polytextiles.
- [x] Полная сверка всех 68 продуктов с вики EVE University: ноль расхождений.
- [x] Длительности циклов подтверждены (basic 30 мин, advanced и high-tech
      по 60). Это было последнее непроверенное число, влиявшее на расчёт
      количества добывающих планет.
- [x] `data/type_ids.json` — 105 записей: продукты, сырьё, структуры,
      командные центры.
- [x] **Закрыто без проверки (16.09.2026).** Расхождение по CPU второго
      причала (см. раздел 2) требовало поставить два P4-шаблона на
      планету ~20 000 км при CCU V. Отложено пользователем на последнюю
      очередь 11.09.2026 в ожидании подходящей планеты; 16.09.2026
      пользователь подтвердил, что в загруженном регионе (Fountain) нет
      ни одной Barren/Temperate планеты такого радиуса — проверить
      физически не на чем. Решение: оставить консервативное значение
      (`assumptions.launchpad_count_for_two_templates`, 7200 CPU) как
      есть, не как временное — а как окончательное для этого региона.
- [x] Полный набор шаблонов загружен и разобран (68 файлов).
- [x] `pin_type_ids` выверен по реальному набору: подтверждён
      `high_tech_industry_facility = 2475`, исправлена ошибка
      с `2557` (это launchpad газовых планет, а не storage).
- [x] Экспорт в Excel: три листа («План», «Сводка», «Проверка»). Сводка
      на формулах COUNTIF, а не на числах из Python, — пересчитывается при
      ручной правке. Планеты с загрузкой от 90% подсвечены.
      Формулы проверены на вычислимость (0 ошибок).

> На выходе из Фазы 1 весь функционал планирования уже тестируется на dev-заглушках —
> Фазы 2–3 (реальные ESI-вызовы) не блокируют разработку и отладку основной логики.

### Фаза 2 — Инфраструктура фоновых сборщиков
- [x] `scripts/esi_client.py` — единая точка обращений к ESI: User-Agent
      с контактом, заголовок `X-Compatibility-Date` (зафиксирован, не «сегодня»),
      слежение за лимитом ошибок с запасом до нуля, уважение `Retry-After`,
      кэш ETag.
- [x] `scripts/refresh_market_prices.py` — сбор цен по расписанию, а не по
      действию пользователя. Плюс `scripts/scheduler.py` для запуска обоих
      сборщиков одним процессом, без внешних зависимостей.
- [x] `scripts/refresh_tokens.py` в расписании `scheduler.py` (раз в 15 мин):
      продлевает access-токены по refresh до истечения, отозванные удаляет
      (Character остаётся, персонажу нужен повторный вход). Пока никто не
      вошёл через настоящий SSO — мгновенный no-op.
- [x] Защита от бана: клиент читает `X-ESI-Error-Limit-Remain` и сам
      останавливается за 10 ошибок до нуля — ноль означает 420 на всех
      маршрутах, включая исправные.
- [x] **Проверка `Expires` перед запросом, а не только после (12.09.2026).**
      До этого `esi_client.py` хранил только `ETag` и получал `304`
      постфактум — сам запрос всё равно уходил раньше времени, если
      расписание сборщика случайно совпадало со свежим кэшем ESI
      (замечено на живой синхронизации 11.09.2026: два ручных прогона
      `sync_colony_status` подряд дали бы одинаковый снимок, будь второй
      раньше `Expires`). Документация CCP называет это обходом кэша так же
      прямо, как игнорирование `ETag`.
      `EsiClient._etags` теперь хранит `{"etag", "expires"}` на url вместо
      голой строки (старый формат файла читается без миграции — `_load_etags()`
      оборачивает старую запись на лету). Перед запросом `_cache_fresh(url)`
      сравнивает текущее время с сохранённым `Expires`: кэш ещё свежий —
      `EsiResponse(304, from_cache=True)` без обращения в сеть вообще, тот
      же контракт, что и у настоящего 304 (вызывающий код уже был готов —
      `sync_colony_status.py::sync_one()` и остальные читают `from_cache`,
      не код статуса напрямую). Настоящий `304` тоже продлевает `Expires`
      для уже известного `ETag`, даже если сервер не прислал новый `ETag`
      повторно. Тесты — `tests/test_esi_client.py` (файла не было вовсе).

- [x] `domain/profit.py` — оценка выгоды в ISK на колонию-час вместо
      бессмысленного максимума цены за единицу.
- [x] `domain/plan_storage.py` — сохранение и сравнение планов на сервере.
- [x] `domain/advice.py` — подсказки по вместимости пула персонажей:
      что взять при нехватке, чем занять избыток.
- [x] Логистика: колонии одного персонажа кучкуются по системам
      (разброс упал с 6.0 до 2.5 систем на персонажа).

### Фаза 3 — Реальная ESI SSO и синхронизация персонажей
- [x] Слой БД: SQLAlchemy 2.0 + Alembic (`infra/config.py`, `infra/db.py`,
      `infra/models.py`, `migrations/`). Таблицы `characters` и `plans`. На
      разработке — SQLite `data/pi_director.db` (Docker не нужен), в проде —
      Postgres сменой `PI_DATABASE_URL`; модели те же. `seed_dev_characters.seed()`
      пишет dev-персонажей туда же, куда будет писать OAuth callback
      (`source="dev"` против `"esi"`); вне `PI_ENV=dev` seed запрещён, а
      dev-строки не читаются.
- [x] `domain/plan_storage.py` переведён с json-файлов в `data/plans/` на
      таблицу `plans`. Публичный интерфейс (save/list/load/delete/compare)
      не изменился — `api/` и тесты править не пришлось. Старые json-файлы
      автоматически не импортируются.
- [x] PKCE OAuth-flow написан: `api/blueprints/auth.py` (`/api/auth/login-url`
      и `/api/auth/callback`), `scripts/esi_sso.py` — единая точка обращений к
      `login.eveonline.com` (PKCE-пара, обмен кода, refresh, проверка JWT по
      JWKS: подпись, issuer, audience `client_id` + «EVE Online», срок).
      Callback пишет персонажа `source="esi"` и зашифрованные токены. Без
      `PI_ESI_CLIENT_ID` и `PI_TOKEN_KEY` эндпоинты отдают 503 (локальная
      разработка без них идёт на dev-заглушках). `client_id` получен и
      настроен в `.env` на проде — реальные пользователи входят через SSO.
- [x] Первая синхронизация скиллов и колоний сразу после входа
      (`_sync_first_login_async` в `api/blueprints/auth.py`, фоновый
      поток) — не ждать ближайшего расписания `scheduler.py` (скиллы —
      6 ч, колонии — 30 мин). Второе (наравне с обменом кода на токен)
      исключение из правила 3 — обоснование там же и в `CLAUDE.md`.
- [x] Шифрование токенов: `infra/crypto.py` (Fernet), таблица `credentials`
      (access/refresh лежат зашифрованными). Multi-tenant — колонки
      `account_id` в `characters` и `plans` заведены изначально, но до
      11.09.2026 не читались ни одним запросом: разные вошедшие через SSO
      пользователи видели персонажей, колонии, кэш расчёта и планы друг
      друга. Найдено пользователем в проде и исправлено: `api/session.py`
      (подписанная cookie сессии, `account_id` привязывается при входе в
      `auth.py::_store()`), `/api/characters`, `/api/colonies`,
      `/api/calculate` (account_id — первый в ключе кэша), `/api/plans/*`
      и `domain/plan_storage.py` теперь везде фильтруют по account_id
      текущей сессии; в проде анонимный визит (без входа через SSO)
      честно получает пустые списки, а не данные всех подряд. Существующие
      до фикса персонажи и планы с `account_id=NULL` не привязываются
      задним числом — нужен повторный вход через SSO.
- [x] `scripts/refresh_tokens.py` в `scheduler.py` (раз в 15 мин): продлевает
      access-токены по refresh до истечения; отозванные удаляет (нужен
      повторный вход). Общая запись токенов — `infra/credentials.py`.
- [x] `scripts/sync_character_skills.py` в `scheduler.py` (раз в 6 ч, после
      refresh_tokens): подтягивает Command Center Upgrades (type_id 2505) и
      Interplanetary Consolidation (2495) из `GET /characters/{id}/skills/`,
      пишет в `Character`. type_id — в `data/pi_reference.json`. Авторизованные
      запросы через `esi_client.get(..., token=)`, токен из `credentials`.
      Работает на настоящих данных реальных персонажей.
- [x] `scripts/sync_colony_status.py` в `scheduler.py` (раз в 30 мин): снимает
      `GET /characters/{id}/planets/` + деталь по каждой планете, находит
      ближайшее `expiry_time` среди пинов; имена планет — `GET /universe/planets/`
      (public). Пишет в таблицу `colonies` (снимок, не расчётный план).
      `GET /api/colonies` отдаёт список; фронтенд — блок «Мои колонии в игре»
      на дашборде, обратный отсчёт считает сам из `nearest_expiry`, подсвечивает
      планеты с программой на исходе (< 2 ч). Это то самое место, куда v1
      писал `Math.random()`.
- [x] Поимённый состав реальной колонии (`Colony.structures`, миграция
      `946bc27481dc`): из type_id каждого пина, тот же формат, что и
      `structures_detail` расчётного плана — «Мои колонии в игре» рисует
      те же карточки `.colony`/`.strip`/`structOrb()`, что и колонии
      плана на дашборде, а не отдельный список строк.
      **Ошибка, найденная на живых данных (11.09.2026):** считалось, что
      только командный центр специфичен для типа планеты — на деле КАЖДАЯ
      структура своя на каждый тип («Storm Basic Industry Facility» и
      «Temperate Basic Industry Facility» — разные предметы). Полная
      матрица 8 типов планет × 6 структур добыта через
      `scripts/resolve_pi_structure_type_ids.py` (`POST /universe/ids/`,
      разовая задача) и лежит в `data/type_ids.json`.
- [x] «Мои колонии в игре» используют ту же машину рендера, что и план
      дашборда, а не отдельную: `orb()`, `colonyCardHTML()`, режимы вида
      (детали/список/сетка) и сортировка — одни и те же кнопки управляют
      обоими блоками (`realRows()`, `sortedColonies()` в `web/index.html`).
      Роль колонии (добыча/переработка) определяется по составу структур
      (`role_key`), тем же `rolebadge`, что и у плана; правая плашка
      показывает цикл, соответствующий роли — обратный отсчёт экстрактора
      для добычи, состояние фабрик для переработки (см. следующий пункт —
      на момент этой строки ещё не было считано, стало честным «нет
      данных»; выдуманную загрузку CPU/PG, которой для факта из игры
      взять неоткуда, не показываем).
- [x] Панель колонии (кнопка «Детали») показывает настоящее состояние
      каждого пина, а не только состав (`Colony.pins`, миграция
      `bb723c4fd745`) — причал/склад: реальное содержимое (`contents`
      ESI); экстрактор: число голов и какой ресурс добывает
      (`extractor_details`); фабрика: что производит и идёт ли цикл
      («В производстве» с отсчётом) или простой («Простаивает» —
      честные дни/часы) — из `schematic_id`+`last_cycle_start`, которые
      раньше не читались вовсе (роадмап ошибочно утверждал, что «ESI не
      отдаёт состояние фабрик» — отдаёт, просто эти два поля не
      использовались).
      **Вторая ошибка, найденная на живых данных (11.09.2026):**
      `data/schematics.json` (собран из поля «S» игровых шаблонов,
      `scripts/extract_schematics.py`) считался источником настоящего
      ESI `schematic_id` — неверно: «S» на самом деле type_id продукта
      (для Plasmoids в файле ключ 2389 — это и есть type_id Plasmoids, а
      настоящий ESI `schematic_id` той же фабрики — 122, из другого
      пространства). Заменено на `GET /universe/schematics/{id}/` —
      публичный эндпоинт ESI, отдающий имя продукта и длительность цикла
      напрямую, с кэшем на диске (`data/cache/schematics.json`, схемы —
      статические данные игры, не меняются).
      **Заполненность причала/склада (11.09.2026, по скринам игрового
      grid view — третья находка того же рода).** «Вместимость» всегда
      показывала «нет данных»: реальное содержимое (`contents`) у ESI
      есть, а объём одной единицы предмета — нет, штуки не перевести в
      м³. Добавлен `type_volume()` — `GET /universe/types/{id}/`, поле
      `volume`, кэш на диске (`data/cache/type_volumes.json`), тот же
      приём, что и у `schematic_info()`. `used_m3` — `None`, если объём
      хоть одного предмета не узнать (честный пробел, не заниженная
      сумма); ёмкость (10 000 м³ причал, 12 000 м³ склад) — игровая
      константа. Сетка/список «Моих колоний» получили тот же вид, что и
      в игре: иконка добываемого сырья или конечного продукта переработки
      (не иконка постройки) поверх текстуры планеты, голубое кольцо —
      заполненность причала, плашка — одна единица времени (часы, минуты
      при <1ч), как в игре, а не «23 ч 18 мин».
- [x] Синхронизированные данные подключены к панели конкретной колонии:
      сборщик кладёт систему и номер планеты (`planet_index` из имени),
      фронтенд сопоставляет колонию со строкой плана по «персонаж + система +
      номер». При совпадении экстрактор показывает настоящий таймер вместо
      «данных нет», в шапке — уровень командного центра и скиллы персонажа
      (CCU/IC — из того же `sync_character_skills`).
- [x] Плавный переход заложен: `load_characters()` читает из таблицы `characters`
      независимо от источника, `planner.py`/`capacity.py` про источник не знают.
- [x] **Отзыв токенов персонажа прямо на нашем сайте (12.09.2026).**
      Сверено заново с документацией CCP перед реализацией (правило 11):
      revoke-эндпоинт у ESI SSO есть — `POST /v2/oauth/revoke`, адрес
      берётся из `.well-known/oauth-authorization-server`. Но все три
      поддерживаемых способа аутентификации запроса
      (`client_secret_basic`/`_post`/`_jwt`) требуют `client_secret` —
      наше приложение публичный PKCE-клиент БЕЗ секрета (у него физически
      нет безопасного места его хранить, см. правило 11 выше), значит
      вызвать этот эндпоинт мы не можем ни при каких условиях. Вывод тот
      же, что был до проверки, просто теперь обоснован документацией,
      а не её отсутствием.
      Честная замена реализована: кнопка «Отвязать» у персонажей с
      `source="esi"` в списке персонажей дашборда
      (`POST /api/auth/unlink/<id>`, `api/blueprints/auth.py::unlink`) —
      стирает строку `credentials` (`infra/credentials.py::delete_tokens`)
      и обнуляет `characters.account_id`, тем же способом, каким
      `/api/characters`/`/api/colonies`/`/api/calculate` и так решают,
      кому персонаж виден (`account_id`, найдено 11.09.2026) — планировщик
      перестаёт видеть персонажа сразу, не дожидаясь `refresh_tokens`.
      Только владелец текущего визита может отвязать своего персонажа
      (`account_id` сверяется, тот же честный «не найден», что и у чужих
      планов); dev-заглушки (`source="dev"`) отвязать нельзя — не
      проходили через SSO.

      **Дополнено в тот же день.** Владелец приложения зарегистрировал
      его на developers.eveonline.com с секретом (confidential client) и
      передал `PI_ESI_CLIENT_SECRET` — вывод «вызвать `/v2/oauth/revoke`
      нельзя ни при каких условиях» перестал быть верным для ЭТОГО
      конкретного приложения, хотя логика документации не изменилась
      (без секрета — по-прежнему нельзя). `scripts/esi_sso.py::revoke()` —
      настоящий отзыв с `client_secret_basic`, вызывается ИЗ `unlink()`
      ДОПОЛНИТЕЛЬНО к локальному удалению, только когда секрет задан;
      сбой обращения к CCP (сеть, токен уже отозван) не мешает локальному
      удалению — оно происходит в любом случае, это гарантия, а не
      предположение. Вход (`login-url`/`callback`) остаётся PKCE без
      изменений — секрет используется только для отзыва и (уже
      существовавшим кодом `_token_request`) опционально при обмене кода,
      если задан. Ответ `unlink()` несёт `revoked: bool`, UI показывает
      честное сообщение по нему по-разному — реальный отзыв или только
      наша сторона (иначе было бы «правдоподобное, но ложное», что
      запрещает правило 1). Токен на стороне CCP без секрета формально
      ещё жив до естественного истечения (~20 минут) или явного отзыва
      самим пользователем на eveonline.com/account/third_party_apps —
      это тоже прямо сказано в справке.

### Фаза 4 — Функциональное расширение
- [x] Сценарий прямого R0→P2 (вторая часть CSV-матрицы). `domain/direct_p2.py`
      подбирает планеты с обоими видами сырья, ставит P2 целиком на добывающей
      планете (флаг `direct_p2` в `/api/calculate`, галка в интерфейсе).
      `compare_with_split` честно считает, что по числу колоний прямой путь
      обычно не выигрывает — его смысл в логистике. Роль строки — «Прямое P2».
- [x] Мультирегиональность поддержана кодом: если в выгрузке появится
      колонка `Region`, созвездия сгруппируются по регионам сами, а кнопка
      «Весь регион» отметит их разом. В текущем файле колонки нет, поэтому
      все созвездия считаются одним регионом.
- [x] Наглядное предупреждение об истечении программ экстракторов: блок
      «Мои колонии в игре» на дашборде подсвечивает планеты с программой
      на исходе. Push/почта — при желании позже, поверх тех же данных.
      **Дополнено (11.09.2026, по просьбе пользователя):** тот же
      сигнал — у забитого под завязку причала (`isLaunchpadFull()`,
      порог 99% от `launchpadFillPct()`): кольцо заполненности мигает
      красным (`--alarm`) вместо ровного синего, колония попадает в
      «требуют внимания» сводной строки наравне с истекающим таймером —
      складывать добычу дальше некуда, даже если программа экстрактора
      ещё не кончилась.
      **Дополнено ещё раз (11.09.2026, по референсному скрину игрового
      «вид по персонажам»):** иконка ресурса у колонии, которой нужно
      внимание, уступает место красному знаку запрета (своя SVG-иконка —
      это состояние интерфейса, не тип предмета из игры, поэтому не с
      images.evetech.net); при наведении — подсказка с причиной,
      добываемым/производимым ресурсом и заполненностью причала (чистый
      CSS `:hover`, без JS). Заодно снято `content-visibility:auto` с
      `.pilot-row` — оно тянет за собой paint containment и обрезало
      подсказку по границе строки. В виде «Список по персонажам» ряд
      персонажа сжат до одного портрета (как в игре, без текста рядом) и
      получил пунктирные «+»-кружки свободных слотов планет —
      `planet_slots` персонажа (`/api/characters`, то же допущение
      Interplanetary Consolidation + 1) минус реально построенные
      колонии, только там, где `planet_slots` вообще известны.
- [x] **Настоящая загрузка CPU/Power у факта из игры (11.09.2026, по
      референсному скрину игрового «детального вида»).** Раньше считалось,
      что это в принципе не посчитать без радиуса планеты, а ESI его не
      отдаёт — верно только наполовину: радиус есть в `data/planet_industry.csv`
      (том же файле, что использует сам расчётный план), просто раньше его
      не смотрели для колоний вне плана. `domain/capacity.py::
      calculate_real_colony_load()` — то же разложение «структуры + головы +
      линки», что и у шаблонного расчёта, но по НАСТОЯЩИМ данным колонии
      (состав структур, число линков и голов экстрактора — из самого ответа
      ESI на `/characters/{id}/planets/{id}/`, `upgrade_level` — тоже
      ESI, это фактический уровень командного центра, а не потолок скилла
      персонажа). `domain/planets.py::PlanetBook.radius_km()` — радиус по
      системе и номеру планеты; `None`, если планеты нет в CSV (файл
      покрывает только загруженный регион, не весь New Eden) — тогда честное
      «нет данных», как и раньше, а не оценка без радиуса. Считается один
      раз при синхронизации (`scripts/sync_colony_status.py::
      real_colony_load()`), хранится в `Colony.cpu_percent`/`pg_percent`
      (миграция `c1a2f3e4b5d6`), отдаётся `/api/colonies` — значит, работает
      и в компактной полосе иконок (`.strip`), и в панели «Детали» любой
      реальной колонии, а не только совпавшей со строкой плана.
      Разбивка «из чего сложилась загрузка» (структуры/головы/линки в %,
      `pg_breakdown` у расчётного плана) для настоящей колонии не считается —
      сама загрузка от этого не менее настоящая, просто без этой детали.
      **Дополнено (11.09.2026, по скриншотам панели «Детали» из
      стороннего PI-инструмента):** два уточнения по тому же скриншоту.
      (1) Панель показывает не только
      проценты, но и абсолютные числа — «7514/21315 tf», «16124/17000 MW»;
      `real_colony_load()` теперь возвращает и их (`ColonyLoad.used`/
      `.capacity`, они уже считались, просто не сохранялись) — новые
      колонки `Colony.cpu_used/cpu_capacity/pg_used/pg_capacity` (миграция
      `d4e5f6a7b8c9`). (2) У командного центра оказалось собственное
      хранилище — «Storage 0/500 m³», раньше не читалось вовсе. 500 м³ —
      верифицировано тем же скриншотом реального игрового клиента (ESI
      ёмкость структур не отдаёт, как и у причала/склада — тот же приём,
      `STORAGE_CAPACITY_M3['command_center']`); `pin_detail()` теперь
      считает `contents`/`used_m3` для command_center так же, как для
      причала и склада (`storageBlockHTML()` на фронтенде — общая
      разметка для всех трёх).
- [x] **«Вход» у настоящей фабрики (11.09.2026, тот же заход).** ESI
      отдаёт, что фабрика производит, но не что она потребляет.
      `data/recipes.json` это уже знает (проверено по источникам,
      правило 2) — `/api/initial-data` теперь отдаёт `recipe_inputs`
      (имя продукта → имена входов; у P1 вход один, из поля `source`,
      не `inputs`), панель «Детали» показывает иконки входа рядом с
      «Производит», как в игровом клиенте.
- [x] **Настоящий прогресс цикла фабрики на компактном круге (там же).**
      Раньше полоса иконок колонии (`.strip`, `colonyCardHTML`) всегда
      рисовала фабрики с пунктирным кольцом «нет данных о цикле», хотя
      сама панель «Детали» уже показывала настоящий прогресс (`pin.
      last_cycle_start`+`cycle_minutes`, см. пункт про панель колонии
      выше) — данные были, просто не доходили до этой полосы. Теперь
      `pinCycleInfo()`/`structOrbsForRow()` считают то же самое для
      каждого пина: кольцо крутится дугой во время цикла и мигает белым
      целиком, когда цикл кончился, а новый не начался («простаивает»)
      — тот же сигнал «требует внимания», что и у остальных индикаторов.
      **Дополнено (11.09.2026):** сводное «N простаивает» над блоком (одно
      на все три вида — детали/список/сетка, не свой счёт в каждом) раньше
      считало только добычу без запущенной программы. Переработка, где
      простаивают ВСЕ фабрики (та же `pinCycleInfo()` по каждому пину),
      теперь тоже туда попадает — `isColonyIdle()`. Без пинов колонии
      переработка честно не считается ни простаивающей, ни работающей.
      **Дополнено ещё раз (11.09.2026):** пины настоящей колонии в
      детальном виде (полоса `.strip` и полная панель «Детали»,
      `sortPinsForDisplay()`) идут в порядке «командный центр →
      экстрактор → причал → склад → фабрики» — по просьбе пользователя,
      отдельно от `STRUCTURE_ORDER` расчётного плана в `domain/planner.py`
      (там порядок другой: причал и склад перед экстрактором — трогать
      не просили). Сортировка устойчивая: несколько фабрик между собой
      сохраняют исходный порядок ESI.
      **Дополнено ещё раз (11.09.2026):** портрет персонажа на самом круге
      (`.orb .pilot`) дублировал уже показанный рядом (карточка колонии,
      ряд «Список по персонажам») — `orb(row, size, showPilot=false)` в
      видах «Детали» и «Список» отключает его на круге; в «Сетке», где
      портрета рядом больше нигде нет, остаётся как был.
      **Найдена и исправлена системная ошибка (11.09.2026, обнаружил
      пользователь: «ни одной работающей фабрики, хотя это не так»).**
      `last_cycle_start` у пина фабрики ESI пересчитывает НЕ в реальном
      времени, а только когда игрок открывает эту колонию в игровом
      клиенте (подтверждено сторонними источниками — esi-issues #654,
      форум разработчиков ESI). Фабрика в игре крутит циклы сама, пока
      есть сырьё, а поле у ESI «зависает» на последнем визите. Прежний
      расчёт («цикл по расчёту кончился → простаивает») делал вывод из
      устаревших данных как из свежих — почти все колонии, которые давно
      не открывали в игре, показывались простаивающими поголовно, хотя
      наверняка продолжали работать. Ровно то «правдоподобное, но ложное»
      число, которое запрещает правило 1.
      Исправлено (`pinCycleInfo()`, `structureUnitFields`): три честных
      состояния вместо двух — идёт цикл (знаем точно, свежесть визита не
      важна), «ещё ни разу не запускалась» (`last_cycle_start` нет вовсе —
      единственное надёжное «простаивает»), и «нет свежих данных» (цикл
      по расчёту кончился, но дальше неизвестно — не подставляется
      «простаивает»). `isColonyIdle()` для переработки задействует только
      первое надёжное состояние, поэтому теперь редко засчитывает
      колонию простаивающей — это не баг, а честность: для большинства
      колоний мы действительно не знаем текущее состояние фабрик.
      **Пересмотрено в тот же день.** Пользователь показал скриншот
      стороннего PI-инструмента с работающими фабриками, снятый БЕЗ
      захода в игру — и данные подтверждённо актуальны. Оказалось: факт
      про ESI («пересчитывает только при визите в клиент») верен, но
      вывод «дальше неизвестно» был излишне консервативным — так у
      сообщества PI-инструментов не принято. Planets in Space (ещё один
      сторонний трекер) прямо описывает в своём чейнджлоге: «Between ESI
      updates the app simulates your colony forward so the storage bars
      stay current» — досчитывает состояние вперёд локально от
      последнего подтверждённого снимка, предполагая, что фабрика
      продолжает крутиться сама (так она и ведёт себя в игре, пока есть
      сырьё). Это не выдумка в смысле правила 1 — настоящие
      `last_cycle_start`/`cycle_minutes` от ESI, честная проекция, а не
      случайное число.
      `pinCycleInfo()` переписана: вместо «нет свежих данных» после
      первого известного цикла — прогресс по модулю `(now -
      last_cycle_start) % cycle_minutes`, с пометкой `projected` для
      панели («проекция: колонию давно не открывали в игре»). Кольцо на
      круге теперь крутится дугой и во время подтверждённого, и во время
      спроецированного цикла — мигает только у пина, где
      `last_cycle_start` нет вовсе (`idle` — реально ни разу не
      запускалась, единственное состояние, для которого проецировать
      нечего). Тот же вопрос остаётся открытым для заполненности
      причала/склада (`contents`) — по тем же источникам, скорее всего,
      подвержена той же особенности ESI; решение по ней пока не принято.
      **Пересмотрено ещё раз (13.09.2026).** Модульная проекция оказалась
      сама неверна: `(now - last_cycle_start) % cycle_minutes` считает
      фабрику крутящейся БЕСКОНЕЧНО, пока не доказано обратное — остаток
      от деления всегда попадает в 0-100%, сколько бы реального времени
      ни прошло. Пользователь показал новый скриншот стороннего
      PI-инструмента: часть фабрик
      «In production» с честным первым циклом, часть — «Waiting for
      resources, Idle for 1d 5h 47m» с РАЗНЫМ простоем у каждой. При
      бесконечной модульной проекции такого разброса быть не может — все
      выглядели бы одинаково «в цикле». Значит, `last_cycle_start` можно
      доверять напрямую: если с его начала прошло больше `cycle_minutes`,
      а новый `last_cycle_start` ESI не прислала, цикл не продолжился
      (кончилось сырьё) — это и есть простой, а не отсутствие данных.
      `pinCycleInfo()` переписана: без переноса по модулю, `idle`+`idleMs`
      наступают по истечении ПЕРВОГО известного цикла (`projected` и
      предупреждение «колонию давно не открывали в игре» — убраны за
      ненадобностью). `structureUnitFields()` (панель «Детали») переведена
      на ту же функцию вместо своей копии той же формулы. `isColonyIdle()`
      теперь реально ловит простаивающую переработку, а не только «ни
      разу не запускалась».
      **Пересмотрено в четвёртый раз (13.09.2026) — предыдущая правка сама
      оказалась регрессией.** Сравнение `last_cycle_start` напрямую с
      `Date.now()` (реальным «сейчас») дало новую системную ошибку в обе
      стороны: пользователь сообщил (1) «теперь все фабрики в простое,
      хотя это неверно» и (2) «на перерабатывающих персонажей зашёл в
      игру, данные по заводам не получены». Причина: `last_cycle_start`
      достоверен только НА МОМЕНТ, когда ESI сама пересчитала колонию
      (визит в клиент), а не на текущее время — сравнение с `Date.now()`
      почти для любой колонии, которую давно не открывали, считало цикл
      завершённым, даже если фабрика продолжала работать. Разобрано по
      предложению пользователя на исходном коде стороннего PI-
      инструмента с открытой лицензией (Kotlin/Compose):
      классификация состояния фабрики там сравнивает `last_cycle_start`
      с `lastUpdate` — временем снимка из самого ESI
      (`/characters/{id}/planets/`, поле `last_update`), а не с текущим
      временем; дальше тот инструмент досчитывает вперёд полным
      симулятором материальных потоков — это уже отдельная, гораздо
      более крупная задача (нужны маршруты между пинами и точные
      количества входов по схематике, которых мы сейчас не храним) и в
      этой правке сознательно не реализована.
      Добавлено поле `Colony.game_last_update` (миграция
      `f6a7b8c9d0e1`) — то самое `last_update` из списка колоний ESI,
      которое раньше не читалось вовсе; заполняется в
      `sync_colony_status.py::sync_one()`, отдаётся `/api/colonies`.
      `pinCycleInfo(pin, gameLastUpdate)` переписана на честную модель из
      ЧЕТЫРЁХ состояний вместо трёх: идёт цикл (сравнение со снимком
      подтверждает), простаивает с известной длительностью (снимок
      подтверждает завершение цикла), «неизвестно с момента снимка» (на
      момент снимка цикл ещё шёл, но с тех пор прошло больше
      `cycle_minutes`, а колонию не открывали заново — не «простаивает»
      и не «в производстве», честно отдельное состояние с новой строкой
      `cycProjectedHint` — «подтверждено на момент последнего захода в
      колонию, дальше неизвестно»), и «ещё ни разу не запускалась» (как
      раньше). Все точки вызова (`isColonyIdle`, `structOrbsForRow`,
      `realRows`, `structureUnitFields`, `pinUnitsHTML`,
      `openRealColonyDetail`) прокинуты через `game_last_update`
      колонии. Проверено вручную синтетическими данными на все четыре
      состояния (Producing/Idle/Unknown/Never) в панели «Детали».

### Фаза 5 — Frontend и UX
- [x] Судьба `templates/`/`web/` решена: `templates/` оказалась игровыми
      JSON-шаблонами застройки (их разбирает `domain/templates.py`), а
      `web/index.html` — самодостаточный SPA, отдаётся Flask с того же origin
      (Фаза 1). Отдельный сборочный фронтенд не вводится.
- [x] Интерфейс RU/EN доведён до конца: словарь `T` во фронтенде плюс каталог
      `domain/plan_messages.py` для строк, которые формирует сервер
      (предупреждения, допущения, роли колоний, число фабрик, панель
      вместимости, экспорт в Excel). `/api/calculate`, `/api/advice`,
      `/api/export` принимают `lang`; `applyLang` перерисовывает все панели.
- [x] UI просмотра плана (дашборд: детали/список/сетка, панель колонии по
      структурам) и экспорта в Excel.
- [x] **UI статуса синхронизации фоновых джобов (12.09.2026).** Пункт 3
      из трёх согласованных (Expires → отзыв токена → этот). Веб-процесс
      и планировщик — разные процессы (`deploy/README.md`), состояние
      `Job.last_run`/`failures` живёт только в памяти планировщика —
      тот же класс задачи, что и у онлайна сервера
      (`scripts/refresh_server_status.py` → `server_status.json`), решён
      тем же приёмом: `scripts/scheduler.py::_write_status_snapshot()`
      пишет `data/cache/scheduler_status.json` после КАЖДОГО запуска
      джоба (успешного или нет), `api/blueprints/meta.py::_job_status()`
      читает готовый файл (правило 3 — веб-обработчик никуда не ходит) и
      отдаёт через `/api/meta`. Джоб просрочен, если не запускался дольше
      двух своих интервалов (у джобов разный период, от 10 минут до
      суток — с запасом, чтобы не поднимать ложную тревогу); падающий
      джоб (`failures > 0`) — отдельный признак независимо от свежести
      (задержка перед повтором сама укладывается в интервал, см.
      `run_job()`). Чип «сборщики» в шапке (`web/index.html`, рядом с
      уже существовавшим, но нигде не документированным чипом «ESI») —
      точка цвета по худшему статусу среди джобов, подсказка при
      наведении перечисляет каждый джоб с возрастом и числом сбоев.
      Прячется на узких окнах вместе с чипом «данные» — диагностика для
      владельца, не для каждого посетителя.
- [x] **Версия 0.10.1, справка и «О проекте» приведены в порядок
      (13.09.2026, по прямому запросу пользователя).**
      Версия поднята с 0.10.0 (PATCH — исправления и симуляция колоний
      в пределах уже закрытых фаз 3/7, не новая фаза).
      Раздел справки «Мои колонии в игре» — самый большой (свыше
      80 строк на язык) и заодно самый устаревший (описывал давно
      отменённую проекцию «по модулю» из ранней версии `pinCycleInfo()`,
      см. Фазу 3) — сокращён примерно втрое на обоих языках: те же
      факты (снимок из игры, четыре категории сводки, содержимое панели
      детали, честная проекция состояния фабрики по маршрутам, порог
      дефицита, кнопка «Перезайти»), без построчного перечисления всех
      мелких визуальных деталей. Прочие разделы справки не трогались.
      `tests/test_docs.py` подтверждает: сокращение не потеряло ни
      одного упоминания из реестра `domain/features.py`.
      «О проекте»: строка «Этап» (`aboutPhase`, бралась из
      `version.py::PHASE`) убрана целиком — вместе с ней удалён и сам
      `PHASE` (в `version.py`, `/api/meta`, фронтенде), как более не
      нужный. Вместо неё — «Разработчик», «Лицензия: MIT» и «Исходный
      код» (ссылка на GitHub) в первом блоке; раздел «Права» дополнен
      предложением от автора (mefffodiy-n, проект сделан для себя и
      выложен открыто под MIT) и текстом о товарных знаках по прямой
      формулировке пользователя (сверено с footer eveonline.com):
      «EVE Online® and Fenris Creations™ … are trademarks of Fenris
      Creations. ©2026 Fenris Creations. All rights reserved» — заменяет
      прежнюю строку про CCP hf. `tests/test_docs.py` обновлён вместе:
      `test_about_mentions_ccp_trademark` → `test_about_mentions_
      trademark_owner` (ищет «Fenris Creations», не «CCP hf») и новый
      `test_about_mentions_license_and_source` (MIT, ссылка на GitHub,
      имя автора на обоих языках) — тест кодировал старое поведение,
      обновлён вместе с сознательным изменением, а не в обход правила.

### Фаза 6 — Развёртывание (не требует ESI)
- [x] `scripts/serve.py` — production-запуск через waitress (`PI_HOST`/
      `PI_PORT`/`PI_THREADS`), отдельно от dev-режима `run.py`. `deploy/` —
      README + образцы: службы Windows через NSSM (веб и сборщики), nginx
      (`nginx.conf.sample`), задание бэкапа (`backup-task.xml`). Установка
      на конкретный сервер — по `deploy/README.md`, это уже ops.
- [x] `scripts/backup.py` — согласованная копия SQLite (`.backup()`) и
      снимков `data/cache/` в `{PI_BACKUP_DIR}/pi-backup-<ts>/`, старые
      чистятся до `PI_BACKUP_KEEP` (14). Джоб в `scheduler.py` раз в сутки;
      для Postgres — `pg_dump` (в README).
- [x] Журналирование: `infra/logging.py` — файл `{PI_LOG_DIR}/<компонент>.log`
      с ротацией (2 МБ × 5) плюс консоль, уровень `PI_LOG_LEVEL`. Подключён
      в `scheduler.py`, `serve.py`, фабрике приложения (кроме тестов).
- [x] Фаза реально развёрнута на Ubuntu VPS (1 CPU/1 ГБ RAM/10 ГБ SSD):
      systemd-юниты веба и сборщиков, nginx + Let's Encrypt (certbot),
      бесплатный поддомен DuckDNS с автообновлением IP, SSH только по
      ключу на нестандартном порту, `ufw` + `fail2ban`, автообновления
      безопасности, 2 ГБ swap (обязателен при 1 ГБ RAM — сборка pandas/
      numpy). Пошагово — `deploy/README.md` раздел 7.

### Фаза 7 — Мониторинг добычи

Была заблокирована Фазой 3 (нужен реальный `client_id`) — сняла блокировку
живая синхронизация на настоящих персонажах. Первые два пункта сделаны
11.09.2026, по прямому запросу пользователя (референсный скрин детальной
панели стороннего PI-инструмента, где показаны Avg./Current/Total
Output и график).

- [x] Отслеживать добычу по колониям: новая таблица `ExtractionSample`
      (миграция `e5f6a7b8c9d0`) — снимок расчётной скорости КАЖДОГО
      экстрактора (`qty_per_cycle`/`cycle_seconds` из ESI, пересчитанные
      в единиц/час) копится при каждой синхронизации
      (`scripts/sync_colony_status.py::record_extraction_samples()`), не
      перезаписывается. Копится с нуля начиная с этой даты — ESI сама
      историю не хранит, отдаёт только текущий снимок; «Total Output» из
      референса честно не считаем (при интервале синхронизации 30 мин и
      цикле экстрактора ~15 мин сумма по снимкам была бы то занижением,
      то задвоением — не настоящее число). `/api/colonies` отдаёт
      накопленные снимки в `pin.extraction_history`, `extractorRateHTML()`
      на фронтенде — текущая скорость, среднее по истории и график
      (простой SVG-график столбцами, без библиотек).
- [x] **Оповещать о дефиците — с отступлением от собственного плана.**
      Изначально в этом же пункте было записано «порог просадки не
      выдумывать, вывести из истории самой колонии» — по прямому решению
      пользователя (11.09.2026) вместо этого используется фиксированный
      порог `DEFICIT_UNITS_PER_HOUR = 48000` ед./ч, одинаковый для всех
      ресурсов: колония, добывающая меньше, уже дефицитна для всей
      цепочки сама по себе, независимо от плотности своего месторождения.
      Осознанное отступление от исходной формулировки, а не забытое
      правило — обе версии текста больше не расходятся, потому что старая
      удалена, а не оставлена рядом с новой.
      **Уточнено в тот же день:** дефицит — СВОЯ категория, не «требует
      внимания» (той остаются только истекающий таймер и забитый причал —
      красный знак запрета, колония там уже не работает как надо прямо
      сейчас). Дефицитная колония продолжает добывать, просто ниже
      порога, поэтому получает отдельный сигнал: маленькая янтарная точка
      на круге (`.def-mark`, иконка ресурса не гасится), свой счётчик «M
      дефицит добычи» в сводной строке рядом с «N требуют внимания»
      (`isColonyDeficient()`), предупреждение в панели «Детали» экстрактора
      и в подсказке при наведении — обе последние две детали не менялись.
- [x] **Предлагать добавить добывающую колонию для дефицитных ресурсов —
      с уточнением области действия (11.09.2026).** Изначально в этом же
      пункте было записано «связать просадку с производственной цепочкой
      ТЕКУЩЕГО ПЛАНА (`domain/planner.py`/`advice.py`)» — по прямому
      решению пользователя область сужена: сверяется только с уже
      ДЕЙСТВУЮЩИМИ колониями (`coloniesData`, `/api/colonies`), не с
      расчётным планом на экране. Обоснование пользователя: «это касается
      только если персонажи уже что-то производят — либо цепочка была при
      входе, либо построена по плану и стала настоящей колонией, в любом
      случае это относится уже к существующим колониям». Расчётный план
      мог бы описывать вариант, который ещё не построен или никогда не
      будет — предупреждать о его цепочке значило бы предполагать
      действие, которого пользователь не совершал.

      Реализация — целиком на фронтенде, backend не менялся: для каждого
      дефицитного экстрактора (`pinDeficitInfo()`, web/index.html) ищутся
      реальные фабричные пины ЛЮБОЙ колонии пользователя, чей `recipeInputs`
      (уже приходит в `/api/initial-data` из `data/recipes.json`, правило 2)
      включает добываемый ресурс (`realChainProductsUsing()`). Если
      совпадение есть — предупреждение называет продукт цепочки под угрозой
      и предлагает добавить добывающую колонию (`extrDeficitChain`, RU/EN);
      если нет — остаётся прежний общий текст «дефицит — нужна
      дополнительная добыча» (`extrDeficit`), без ложного утверждения о
      конкретной цепочке, которой на самом деле нет. Без точного расчёта
      «сколько именно персонажей/планет не хватает» — по решению
      пользователя: «просто добавьте ещё одну добывающую колонию», без
      непроверенной арифметики (см. правило 1: не было честного источника
      для понятия «скорость добычи здоровой колонии», кроме самого порога
      48 000 ед./ч).
- [x] **Найдена и исправлена системная ошибка расчёта скорости добычи
      (13.09.2026, обнаружил пользователь: скриншот игрового клиента с
      настоящим «Avg. Per hour: 17 030» против наших 21 868–22 061 —
      расхождение ~28%).** `extractorRatePerHour()`/`extractorAvgRatePerHour()`
      считали скорость напрямую по `qty_per_cycle` — а это, как оказалось,
      БАЗОВОЕ значение программы экстрактора на момент установки
      (`install_time`), а не фактический объём текущего цикла. Реальная
      добыча в игре затухает от этого значения по нелинейной кривой с
      шумовой модуляцией на протяжении всей программы (это видно и на
      графике в самом игровом клиенте — объём цикла падает с ~7000 до
      ~2000 за сутки). Наш прежний расчёт брал базовое значение как есть —
      тем более значительное завышение, чем дольше программа уже работает.
      Ошибка напрямую искажала `DEFICIT_UNITS_PER_HOUR`: завышенная
      скорость могла скрыть реальный дефицит.
      Формула затухания не публикуется CCP официально, но воспроизведена
      независимо несколькими сторонними PI-инструментами по их открытым
      исходникам и чейнджлогам («Planets in Space» в том числе) —
      правило 2 (сверка источников) соблюдено, перенесена как
      проверенный факт игровой механики, а не выдумка.
      `extractorDecayedQty(baseValue, installTime, cycleSeconds, atMs)`
      (`web/index.html`) — честный порт формулы: затухание
      `baseValue/(1+t·0.012)` плюс модуляция тремя косинусами, без новых
      данных ESI (`install_time`/`qty_per_cycle`/`cycle_seconds` уже
      сохранялись `sync_colony_status.py::pin_detail()` для другой цели).
      `extractorRatePerHour()` считает по этой формуле на текущий момент;
      `extractorAvgRatePerHour()` и график (`extractorRateChartSVG`)
      пересчитывают каждый снимок `extraction_history` затуханием НА
      МОМЕНТ этого снимка (тот же `install_time`, честная оговорка: если
      экстрактор переустанавливали, старые снимки этого не знают — редкий
      и не проверяемый нами случай). Отдельно от бага `pinCycleInfo()`
      (последняя ревизия того же дня выше): там причина в том, что ESI
      обновляет поле только при визите в клиент, здесь — что поле вообще
      не про «сейчас», а про момент установки; используется реальное
      время (`Date.now()`), не `game_last_update`, потому что затухание
      считается сервером EVE непрерывно и не зависит от визитов игрока.
- [x] **Найдена и исправлена ещё одна ошибка сводки «N простаивает»
      (13.09.2026, пользователь прислал скриншот: добывающая колония с
      рабочей программой экстрактора, но с двумя фабриками переработки
      на той же планете, простаивающими уже больше суток — колония в
      сводку простаивающих не попала).** `isColonyIdle()` для
      `role_key==='mine'` проверяла только `nearest_expiry` (программу
      экстрактора) — фабрики на той же планете (частая связка «добываю
      и тут же перерабатываю в одной колонии») вообще не влияли на счёт,
      хотя для `role_key==='proc'` та же самая проверка (`pinCycleInfo(
      ...).idle` по всем фабричным пинам) уже была. Исправлено: у
      добычи колония теперь простаивает, если ИЛИ нет программы
      экстрактора, ИЛИ (если на планете есть фабрики) все они
      простаивают — то же честное определение факта, что и у чистой
      переработки, применяется дополнительно, а не взамен.
      Использование `isColonyIdle()` ограничено только сводкой
      «N простаивает» (`renderColonies()`) — цвет кольца отдельного
      пина в списке/сетке (`structOrbsForRow()`) уже считался верно по
      тем же `pinCycleInfo(pin,p.game_last_update)` независимо от роли
      колонии, это не затронуто.
      **Отменено в тот же день.** Пользователь показал ВТОРОЙ скриншот
      (из стороннего PI-инструмента, актуальное состояние ТОЙ ЖЕ связки
      «добываю и тут же перерабатываю»): 7 из 9 фабрик «In production», 2 —
      «Waiting for resources, Idle for 36s». Разъяснение пользователя:
      частичный простой фабрик переработки на добывающей колонии почти
      всегда означает, что добыча не поспевает прокормить все фабрики
      сразу — это уже отдельный, ранее реализованный сигнал «дефицит
      добычи» (`isColonyDeficient()`/`DEFICIT_UNITS_PER_HOUR`), а не
      «колония встала»: колонии из деффицита самокорректируются сами,
      циклически простаивая и возобновляясь по мере накопления сырья.
      Правка выше путала два разных факта под одним ярлыком — хуже того,
      рисковала ложно помечать «простаивает» колонию, которая на самом
      деле работает с ожидаемыми перебоями. Отменена до деплоя на прод
      (успели поймать на этапе ревью): у добычи снова только
      `nearest_expiry`, как было до этого пункта. Логика для чистой
      переработки (`role_key==='proc'`) не менялась ни разу — там нет
      понятия «дефицит», единственный сигнал для полностью простаивающих
      фабрик — это и есть «простаивает».

      **Пятая версия — полноценная симуляция материального потока по
      маршрутам (13.09.2026, по прямому запросу пользователя: «делаем
      полноценно, нужно работающее точное решение»).** Пользователь
      сообщил, что ВСЕ фабрики на добывающих персонажах показывались
      «простаивает», хотя часть из них по факту работает — то же самое
      «неизвестно с момента снимка» четвёртой версии, только теперь
      честно опознанное как недостаточное, а не «наверное и так сойдёт».
      Причина названа самим пользователем: «возможно это из-за того что
      мы не читаем данные по маршрутам, линкам и т.п.?» — да, именно так.
      Сторонний PI-инструмент с открытым кодом сравнивает
      `last_cycle_start` со снимком (`lastUpdate`) только как СТАРТОВУЮ
      точку — дальше полноценно симулирует материальный поток по
      маршрутам между пинами (routes) и входным буферам каждой фабрики
      (contents против demands схемы) от снимка до настоящего «сейчас».
      Без маршрутов у нас не было
      возможности узнать, докуда доходит сырьё экстрактора и сколько его
      накопилось у каждой фабрики — поэтому четвёртая версия честно
      останавливалась на «неизвестно», что на практике означало
      «простаивает» для любой колонии, которую давно не открывали.

      Реализовано:
      - `Colony.routes` (миграция `a1b2c3d4e5f6`) — тот же массив
        "routes", что ESI отдаёт рядом с "pins"/"links" в ответе
        `GET /characters/{id}/planets/{id}/` (уже запрашивался ради
        "links", новых обращений к ESI не потребовалось).
        `scripts/sync_colony_status.py::routes_detail()` резолвит
        `content_type_id` в имя тем же справочником, что и остальные
        поля пина. `pin_detail()` теперь кладёт `pin_id` в КАЖДЫЙ пин
        (раньше — только у экстрактора, для истории добычи), иначе
        маршруты не с чем сопоставить.
      - `data/schematics.json` (уже существовал, извлекается из
        проверенных игровых шаблонов — `scripts/extract_schematics.py`,
        правило 2 — но раньше не отдавался фронтенду) — точные
        количества входа/выхода за цикл каждой фабричной схемы, по
        type_id продукта. Отдаётся в `/api/initial-data` как
        `schematics` (`api/blueprints/reference.py::
        _schematics_by_output()`), фронтенд сопоставляет
        `pin.product` → `ids[pin.product]` (уже есть) → эту карту —
        новых серверных полей на пине не потребовалось.
      - `simulateColonyFactories(pins, routes, gameLastUpdateIso)`
        (`web/index.html`) — честный, по образцу стороннего
        PI-инструмента, но сознательно упрощённый порт: событийный
        цикл вместо очереди с приоритетом
        (пинов в колонии мало, линейный поиск минимума дешевле),
        экстрактор выдаёт затухшее количество (`extractorDecayedQty`,
        та же формула, что и у панели экстрактора) на каждый свой
        цикл и рассылает по исходящим маршрутам; фабрика на каждом
        цикле сначала забирает то, что уже накопилось на
        маршрутизированных к ней складах (честно учитывает остаток,
        который был ДО начала проекции, не только новые поставки), потом
        проверяет, хватает ли входов по схеме, и либо производит, либо
        простаивает. Склад/причал/КЦ — проходной буфер без своего цикла
        (тот же приём). Сознательно НЕ считает ёмкость склада в
        кубометрах (на фронтенде нет объёма товара поштучно, это
        серверный расчёт) и не воспроизводит приоритет между несколькими
        потребителями одного дефицитного ресурса (берёт по порядку
        маршрутов ESI) — оба упрощения не выдумывают данные, только не
        разрешают редкие случаи конкуренции с точностью до кванта.
        Возвращает `null`, если считать нечем (нет маршрутов, нет
        данных схемы, нет снимка) — тогда вызывающая сторона просто не
        передаёт результат, и `pinCycleInfo()` честно остаётся на
        «неизвестно» четвёртой версии, поведение не меняется для
        колоний, ещё не пересинхронизированных этой версией.
      - `pinCycleInfo(pin, gameLastUpdate, simResult)` — новый третий
        параметр: если задан, отдаётся как есть, минуя собственный
        расчёт. Все точки вызова (`isColonyIdle`, `structOrbsForRow`,
        `pinUnitsHTML`→`structureUnitFields`) считают
        `simulateColonyFactories()` один раз на колонию и передают
        результат конкретного пина.
      Проверено вручную: синтетическая колония (экстрактор + причал +
      9 фабрик, суммарный спрос фабрик выше добычи) даёт честный
      смешанный результат — часть фабрик «в производстве», часть
      «простаивает» с реальной длительностью, вместо прежнего
      поголовного «простаивает». Колония без маршрутов (старый снимок)
      корректно остаётся на «неизвестно» — регрессии нет.

      **Найдена и исправлена ошибка планирования экстрактора в самой
      симуляции (13.09.2026, сразу после первого деплоя — пользователь
      сообщил: «ничего не поменялось, все добывающие колонии
      простаивают», после ручного прогона `sync_colony_status` для
      немедленной проверки).** У экстрактора, в отличие от фабрики,
      поле `last_cycle_start` вообще не сохраняется отдельно —
      `pin_detail()` кладёт его только фабрикам (см. её докстринг).
      `simulateColonyFactories()` планировала первое событие экстрактора
      именно от `pin.last_cycle_start`, которого у экстрактора никогда
      нет — `entry.lastRun` оставался `null`, экстрактор ни разу не
      попадал в очередь событий, не выдавал ни грамма сырья за всю
      симуляцию, и результат был ровно тем же «все простаивают», который
      вся эта работа должна была устранить. Исправлено: экстрактор
      планируется от `install_time` (оно у него есть всегда и не
      привязано к визитам в колонию, в отличие от `last_cycle_start`).
      Проверено на реальной колонии с прода (character 2122010193,
      planet 40292538, вытащена напрямую из БД по SSH): до фикса
      симуляция физически не могла сработать (экстрактор не
      планировался); после — 7 из 8 фабрик честно «в производстве», 1
      реально простаивает — то самое смешанное состояние, которое и
      было целью всей этой работы.

### Фаза 8 — Визуальный редизайн по макету Claude Design

**Источник:** макет, сгенерированный Claude Design поверх текущего живого
приложения (тот же набор CSS-переменных: `--void/--hull/--seam/--dim/
--read/--bright/--flux/--forge/--alarm/--calm/--t-p1..p4/--ring-*` — макет
не придуман с нуля, а является рестайлингом уже существующей системы
токенов, см. `web/index.html:16-37`). Файл получен от пользователя как
экспорт conversation.json Claude Design (~4.8 МБ, разметка + встроенный
PNG-логотип + скрипт компонента); статическая HTML-разметка со
`{{ }}`-плейсхолдерами (Claude Design/DC-шаблонизатор), не готовый к
вставке HTML — переносится вручную, значение за значением, не копипастой.

**Условия задачи (прямо оговорены пользователем 13.09.2026):**
1. Отдельная ветка — можно откатить одним ревертом мержа, не трогая
   логику по одной правке за раз.
2. Меняются ТОЛЬКО расположение элементов, стили и цвета — никакой
   правки функционала (расчётов, эндпоинтов, структуры данных,
   поведения кнопок сверх смены внешнего вида). Если для визуального
   эффекта пришлось бы менять структуру данных или добавлять новую
   функцию — это отдельный вопрос пользователю, не молчаливое решение.
3. Сначала тестируем локально (dev-сервер, обе темы, оба языка, все три
   вида дашборда, панель колонии, обе вкладки, модалки) — деплой на VPS
   только отдельным одобрением после этого.
4. Каждый шаг — маленький, независимо проверяемый и коммитится
   отдельно; следующий шаг не начинается без одобрения предыдущего.

**Что приносит макет (в порядке от общего к частному):**

- **Шрифт заголовков/лейблов/кнопок** — `Saira Condensed` (жирный,
  прописные буквы, широкий трекинг), поверх уже подключённых `IBM Plex
  Mono` (числа) и `IBM Plex Sans` (текст). Сейчас в приложении
  Saira Condensed не подключён вовсе — новый `<link>` на Google Fonts.
- **Палитра** — тот же набор переменных, другие значения: холодный
  синевато-серый (`--void:#080b11`, акцент `--amber:#d4a04a`) заменяется
  тёплым «промышленно-кузнечным» (`--void:#0a0805`, акцент
  `--gold:#e8a33d` + новый `--gold-hi:#ffca74`, `--forge:#f0871e` как
  вторичный акцент). Есть и вторая тема (`body.light` аналог) — макет
  её тоже прорисовал, переносить обе сразу, не откладывать светлую.
- **Декоративный мотив «уголки» HUD** — золотые L-образные уголки
  (`::before`/`::after`, 1-2px border-left+top / border-right+bottom) на
  карточках шага настроек, модалках, элементах списка закупки, юнитах
  структур в панели колонии. Чисто декоративный CSS, не меняет разметку.
- **Верхняя панель** — логотип-аватар (кружок с иконкой) слева от
  «PI Director», активная вкладка снизу подсвечена золотой чертой
  (сейчас — `--flux`, синяя), кнопка «Войти через EVE SSO» с заливкой-
  градиентом и золотой рамкой вместо текущей тонкой обводки.
- **Чипы статуса (ESI/сборщики/данные/версия)** — структурно те же,
  что уже есть в приложении (мы их сами добавляли в этой сессии) —
  меняется только палитра и, возможно, моноширинный акцент на цифрах
  (уже так и есть).
- **Карточки колонии (дашборд, все три вида)** — структура полей
  (иконка планеты + кольцо, роль, система/планета, тип, км, хвостовая
  метка, кнопка «Детали», полоса структур) совпадает с текущей
  `colonyCardHTML()`/`orb()`/`structOrb()` почти один в один — перенос
  сводится к перекраске и замене шрифтов, без изменения вёрстки/классов.
- **Панель колонии (Детали)** — метрики CPU/Power/разбивка Power/
  «до конца программы» и список юнитов структур — тоже структурно
  совпадают с `structureUnitFields()`/`pinUnitsHTML()`; в макете добавлен
  уголок-декор на карточке юнита и крупнее моно-цифры.
- **Настройки — решено в Шаге 9: сетка из макета.** Три РАВНЫЕ карточки
  в сетке (`grid-template-columns:repeat(auto-fit,minmax(300px,1fr))`)
  с номерами «01/02/03» вместо фиксированной левой рейки 270px
  (`.rail`/`.split`). Подробности реализации — Шаг 9 в плане внедрения
  ниже.
- **Таблицы («Что выгоднее производить», план)** — в макете у таблицы
  плана заголовки сгруппированы в три блока («Колония» / «Производство»
  / «Нагрузка», `colspan`, с подписью помельче под каждым) — сверить,
  есть ли такая группировка в текущей `renderTable()`/`renderProfit()`;
  если нет, это тоже расширение разметки таблицы, не только стилей, и
  тоже отдельный вопрос, а не подразумеваемое решение.
- **Модалки (Справка/О проекте)** — рамка с золотыми уголками увеличенного
  размера (2px), шапка с логотипом и градиентной подложкой — структура
  контента (`isHelp`/`isAbout`) не меняется, только обёртка.

**План внедрения (каждый шаг — отдельный коммит, ждём одобрения перед
следующим; ветка `design/claude-mockup-theme` от `main`):**

- [x] Шаг 0. Завести ветку, ничего не менять — контрольная точка отката.
- [x] Шаг 1. Подключить шрифт `Saira Condensed` (Google Fonts `<link>`),
      пока нигде не использовать — проверить, что подключение само по
      себе ничего не ломает (test_frontend_integrity.py, страница
      открывается).
- [x] Шаг 2. Перекрасить палитру: заменить только HEX-значения
      существующих переменных в `:root`/`body.light` на новые из макета
      (без переименования самих переменных — `--amber` остаётся
      `--amber` по имени, значение становится золотым; так меняется
      сразу вся палитра одним диффом без правки каждого места
      использования). Добавить только новые переменные, которых не было
      (`--gold-hi`, `--glow`), пока неиспользуемые.
- [x] Шаг 3. Применить `Saira Condensed` к заголовкам разделов (`h2`,
      `h3` дашборда/настроек) и кнопкам с прописными буквами — точечно,
      с проверкой в обеих темах.
- [x] Шаг 3.1 (внеплановый). Верхняя панель не должна переноситься на
      вторую строку ни при какой ширине (жёсткое требование) — заменил
      фиксированные `@media`-пороги для чипов-индикаторов на JS-измерение
      факта переноса (`fitTopbar()`/`topbarWrapped()`), с прогрессивным
      скрытием по приоритету (чипы → подпись бренда → Справка/О проекте →
      тема → язык) и мобильным выпадающим списком «Ещё» для всего
      спрятанного. Минимальная поддерживаемая ширина окна — 500px
      (мобильные экраны сознательно не поддерживаются, ниже 500px —
      горизонтальная прокрутка).
- [x] Шаг 4. Верхняя панель: логотип-аватар (`web/logo.png`), активная
      вкладка золотой чертой (`--amber` вместо `--flux`), кнопка входа
      EVE SSO новым стилем (золотая рамка + градиентная подложка).
- [x] Шаг 5. Декоративные уголки — новый CSS-класс `.hud-corner`
      (`::before`/`::after`, золотая скоба сверху-слева и снизу-справа),
      применён на уже существующих карточках со своей рамкой: модалка
      (`.modal-box`), элемент списка закупки (`.shop-item`), карточка
      юнита структуры (`.unit .panel`). «Карточки шагов настроек» из
      исходной формулировки шага не тронуты — такой карточки в текущей
      рейке `.rail` физически нет (там просто сгруппированные `h2`+
      `.field`, без рамки), она появится только если Шаг 9 (ниже)
      решится в пользу сетки 01/02/03 из макета — тогда уголок добавится
      туда же самим классом, без новой правки CSS.
- [x] Шаг 6. Карточки колонии дашборда — перекраска и шрифты по месту,
      без изменения структуры DOM: кнопка «Детали» золотая вместо синей
      (`.details-btn`), подпись хвостовой метки Saira Condensed
      заглавными (`.tail .lbl`, как заголовки Шага 3), фон значка
      системы (`.sysbadge`) переведён с захардкоженного цвета (остался
      от палитры до Фазы 8, не совпадал с текущим `--flux` в тёмной
      теме) на `var(--t-p1-bg)`, как в макете. Виды «Список» и «Сетка»
      этих элементов не используют — тронуты не были, но проверены на
      регрессию визуально. Обе темы проверены на собранном плане.
- [x] Шаг 7. Панель колонии (Детали). Уголки карточек юнитов (`.unit
      .panel`) — уже сделаны в Шаге 5. Перекраска: кнопка «Назад»
      (`.cp-head .back`) — золотая рамка/текст, по ховеру заливка
      сплошным золотом, как «Войти через EVE SSO» из Шага 4; фон значка
      «есть иконка» у входного сырья фабрики (`.inputs span.have`) —
      тот же баг захардкоженного цвета до Фазы 8, что чинили в Шаге 6 у
      `.sysbadge`, то же исправление на `var(--t-p1-bg)`. Метры CPU/Power
      (`.meter i.cpu/.pg`) уже совпадали с макетом цветами `--ring-extract`/
      `--alarm` — трогать было нечего. Разбивку Power по вкладам
      (структуры/головы/линки) макет рисует отдельной цветной шкалой —
      у нас это только текст (`pgFrom`); отдельная гистограмма — это
      новая разметка, а не перекраска, оставил как есть, не в рамках
      этого шага.
- [x] Шаг 8. Модалки (Справка/О проекте) — новая рамка/шапка. Рамка
      золотая (`.modal-box`, вместо `--seam`), уголок увеличен до 2px/14px
      (переопределение `.modal-box.hud-corner`, а не общий модификатор —
      единственное место с таким акцентом). Шапка: лого-аватар 22px перед
      заголовком (`web/logo.png`, тот же файл, что в топбаре) и
      градиентная подложка `linear-gradient(90deg,var(--accent-bg),
      transparent)`. Кнопка закрытия по ховеру золотая вместо read/read.
      Структура контента (`isHelp`/`isAbout`, весь `.modal-body`) не
      тронута — только обёртка, как и требовал этот шаг.
- [x] Шаг 9. **Решение пользователя:** сетка из макета (выбрано). Рейка
      `.rail`/`.split` (270px + main) заменена на вертикальный `.split`:
      заголовок «Задание на план», сетка из трёх карточек-шагов
      (`.steps-grid`, `auto-fit, minmax(300px,1fr)` — переносится в
      столбец сама, без media-запроса), золотая панель действия
      с кнопкой «Построить план», блок «Сохранённые планы», затем
      `<main>` с результатами (advice/decision/messages/profit/таблица)
      — не тронут, как и было. У карточек 01/02/03 счётчики выбранного
      (`prodCount`/`constCount`, реальные числа из `chosen`, не
      выдуманные) — обновляются в существующих `renderProducts()`/
      `renderConstellations()`. Все id/onclick сохранены один в один,
      только перенесены в новую обёртку — функциональность не менялась.
      Проверено: полный цикл (выбор → расчёт → результаты) в обеих
      темах, адаптивность на 500-1300px, дашборд/топбар не задеты.
- [x] Шаг 10. **Решение пользователя:** оставить как есть (структуру не
      трогать). При проверке оказалось, что группировка заголовков
      («Колония»/«Производство»/«Нагрузка», `colspan`, подпись помельче
      под каждым) уже была в разметке до Фазы 8 (`thead .group th`,
      `renderTable()`) — макет её просто повторяет почти один в один,
      острой необходимости в структурной правке не было изначально.
      Перекрасил цвет группового заголовка с `--flux` (синий) на
      `--amber` (золотой), как в макете — единственное, что
      действительно отличалось.
- [x] Шаг 11. Полная ручная проверка на собранном плане: обе темы (тёмная/
      светлая), оба языка (RU/EN, в т.ч. одновременно со светлой темой),
      все три вида дашборда (детали/список/сетка), список закупки,
      вкладка «Настройки» (все три карточки-шага + панель дефицита/
      избытка персонажей + объединённый блок «Построить план»/
      «Сохранённые планы»), открытая панель колонии (план и реальная
      колония), обе модалки (Справка/О проекте), узкая ширина (500px —
      топбар не переносится, карточки-шаги складываются в столбец без
      искусственного растягивания). Регрессий не найдено.
- [x] Шаг 12. `python -m pytest tests/ -q` — 375 passed. Целостность
      вёрстки проверена тестом, а не глазами (правило 8); редизайн не
      уронил ни одного существующего теста.
- [x] Шаг 13. Деплой на VPS выполнен (15.09.2026, после явного
      одобрения) — копия `web/index.html` + `web/logo.png`, без
      миграций и перезапуска сервисов: изменения только во фронтенде.
      **Слияние с main (15.09.2026).** К моменту слияния на `main` уже
      накопилось 22 коммита функциональных изменений поверх той же
      точки ветвления (PR #52–#61: индикатор «Перезайти», честная
      проекция состояния фабрики, затухание добычи, полноценная
      симуляция материального потока по маршрутам, версия 0.10.1,
      сокращённая справка). Дизайн менял только вёрстку/цвета/CSS в
      `web/index.html`, функциональные PR почти не пересекались с ним
      построчно — слияние разрешено вручную (PR #62), сохраняя логику
      `main` и разметку/стили дизайна, с полной ручной проверкой
      каждого затронутого функционалом экрана после слияния (все
      380 тестов зелёные, регрессий не найдено).

### Фаза 9 — Доработки по итогам ревью (в процессе)

- [x] **Подсказка чипа «сборщики» переведена на английский (16.09.2026,
      выполнено).** Имя каждого джоба (`j.name`) приходило с сервера уже
      по-русски — единственное место в приложении, где правило 9 (весь
      текст переводим) не соблюдалось. Джобам заведён стабильный `key`
      (`server_status`/`market_prices`/`refresh_tokens`/`sync_skills`/
      `sync_colonies`/`backup`, `scripts/scheduler.py::Job.key`), пишется
      в `scheduler_status.json` рядом с прежним `name` (не вместо — старый
      снимок без `key`, оставшийся после предыдущего запуска до
      перезапуска планировщика, не должен ломать подсказку), отдаётся
      через `/api/meta` (`api/blueprints/meta.py::_job_status()`).
      Фронтенд (`web/index.html::renderJobsChip()`) переводит по `key`
      через новую карту `JOB_LABEL_KEYS` → 6 новых ключей `T` на RU/EN
      (`jobServerStatus`/`jobMarketPrices`/`jobRefreshTokens`/
      `jobSyncSkills`/`jobSyncColonies`/`jobBackup`), с откатом на
      `j.name`, если `key` в снимке ещё нет (переходный снимок сразу
      после обновления, до первого перезапуска планировщика).
- [x] **Для колоний переработки считаем, когда закончится запас сырья в
      причале (16.09.2026, выполнено).** Колонии переработки
      (`role_key==='proc'`) — это причал/склад + фабрики без собственного
      экстрактора, сырьё туда привозит игрок вручную (в отличие от
      добывающих, где его пополняет экстрактор сам) — запас конечный и
      ничем сам не растёт. `simulateColonyFactories()` (`web/index.html`)
      расширена: после проекции буферов до «сейчас» (уже была) для каждого
      причала/склада (`pin.kind==='launchpad'|| 'storage_facility'`, не
      командный центр) и каждого вида сырья, которое реально потребляет
      хоть одна фабрика по исходящим маршрутам оттуда (`entry.demands` —
      готовая продукция без потребителя в расход не попадает сама по
      себе, ESI не различает «вход»/«выход» у предмета, только type_id),
      делится текущий запас на суммарный расход этих фабрик в час
      (`schematics.json`); по колонии в целом берётся минимум по всем
      парам (причал, вид сырья) — совместное истощение нескольких видов
      сразу, фабрика встаёт по первому кончившемуся. Результат —
      `sim.depletionMs`, честный `null`, если считать нечем (как и у
      факторного цикла). Показывается в панели колонии (`colonyTail()`)
      тем же местом, где для добычи — «до конца программы экстрактора»:
      новая плашка «до конца запаса сырья в причале» (`procDepletionLbl`,
      RU/EN), с откатом на прежнее «нет данных», если проекция
      недоступна. Справка дополнена абзацем на обоих языках. Математика
      проверена вручную в браузере на синтетических данных (простое
      деление и случай, где фабрика уже частично выбрала причал до
      «сейчас» — оба дали ожидаемый результат).

      **Дополнено 16.09.2026 по скриншотам пользователя: тот же таймер
      добавлен в сетку и список по персонажам, не только в панель
      «Детали».** `orb()` (`web/index.html`) раньше вообще не рисовал
      плашку для `role_key==='proc'` в этих двух видах — только
      «требует внимания»/пусто, без времени, хотя у добычи там же
      честно тикает «до конца программы». Теперь `orb()` для переработки
      считает тот же `sim.depletionMs`, переводит в абсолютную метку
      (`Date.now()+depletionMs`) и использует общий с добычей механизм
      живого тика по `data-exp` (`fmtBadgeLeft` — минуты, пока меньше
      часа, иначе часы, иначе дни; та же плашка, что и у экстрактора, по
      прямому требованию пользователя «также как и для добывающих
      колоний»), а не пересчитывает симуляцию на каждый тик таймера.
      `colonyTail()` (панель «Детали») дополнена тем же `exp`, чтобы
      значение там тоже тикало само, а не оставалось статичным текстом
      до следующей полной перерисовки — тот же факт, что и у `orb()`,
      просто раньше не был вынесен в `data-exp`.
      Готовая продукция, приходящая в причал взамен израсходованного
      сырья, по-прежнему не путается с самим сырьём: расчёт учитывает
      только типы, которые реально потребляет фабрика (см. выше) — ESI
      не различает «вход»/«выход» у предмета в причале, только type_id,
      и это единственный способ отличить одно от другого, не выдумывая.
- [x] **Лимиты ESI — разобраны по официальной документации
      (15.09.2026), заготовка на масштаб «десятки-сотни пользователей»,
      не один; проактивное чтение токенового лимита добавлено
      16.09.2026.** По `developers.eveonline.com/docs/services/esi/
      rate-limiting/`: у ESI ДВЕ независимые системы, не одна.
      (1) Старый лимит ошибок — 100 ответов НЕ 2xx/3xx в минуту, ОБЩИЙ
      НА ВСЁ ПРИЛОЖЕНИЕ (не на пользователя): `esi_client.py` уже
      проактивно следит за `X-ESI-Error-Limit-Remain` и сам
      останавливается, не дожидаясь 420 — это единственная защита,
      которая по конструкции одинаково работает что при одном
      пользователе, что при сотне: общий бюджет исчерпывается быстрее
      при массовом сбое (например, если ESI на профилактике и
      возвращает 5xx пачкой на сотни персонажей разом), но клиент
      остановит ВСЮ синхронизацию раньше, чем дойдёт до 420, а не
      только для того пользователя, у кого случилась ошибка.
      (2) Новая система — плавающее окно ТОКЕНОВ на пару (группа
      маршрута, персонаж): 2 токена за 2xx, 1 за 3xx, 5 за 4xx, 0 за
      5xx, заголовки `X-Ratelimit-Remaining/Limit/Used/Group`, `Retry-
      After` при 429. Бюджет считается НА ПЕРСОНАЖА — значит масштабируется
      сама по себе: 100 пользователей с личными персонажами не делят
      один бюджет, у каждого свой. `esi_client.py` уже реагировал на сам
      429+Retry-After — эта защита полная и достаточная сама по себе, но
      не давала видеть, куда упирается бюджет, ДО того как он реально
      исчерпается. Добавлено (16.09.2026, PR по итогам этого пункта):
      `EsiClient._note_token_limits()` читает `X-Ratelimit-Remaining/
      Limit/Group` при каждом ответе и хранит последнее увиденное
      значение по группе (`EsiClient.token_limits`) — только для
      наглядности, никакой самоблокировки по этому лимиту нет и не
      нужно (см. выше почему). `sync_colony_status.py::main()` печатает
      сводку по итогам прогона в свой лог. Тесты —
      `tests/test_esi_client.py::TestTokenLimitVisibility` (в т.ч. явно
      проверяет, что низкий остаток НЕ блокирует следующий запрос —
      отличие от лимита ошибок).
      Посчитано по реальным цифрам с прода на 15.09.2026 (16
      персонажей, 81 колония, ~5 колоний/персонаж): `sync_colonies`
      каждые 30 мин делает ~96 запросов ≈ 3.2/мин — на два порядка
      меньше и старого, и нового лимита. Раздел Planetary Interaction в
      ESI состоит ровно из двух эндпоинтов (список колоний, детали
      одной), оба уже используются полностью (включая `routes` — #58);
      справочные вызовы (`/universe/schematics/`, `/types/`,
      `/planets/`, `/systems/`) кэшируются на диске навсегда по id, в
      сеть уходят по разу за всё время работы, а не на каждой
      синхронизации.
      **Что реально не масштабируется — не лимит ESI, а наш собственный
      джоб.** `sync_colony_status.py::main()` обходит персонажей
      строго последовательно (`for character in characters`), без
      параллелизма. При 16 персонажах/81 колонии это ~96 запросов —
      секунды. При росте на порядки (сотни пользователей, тысячи
      колоний) время одного прогона `sync_colonies` (интервал — 30 мин)
      может начать приближаться к самому интервалу и упереться не в
      лимит ESI, а в то, что job не успевает отработать до следующего
      запуска. Порог, после которого стоит распараллелить обход
      персонажей (с уважением общего лимита ошибок — распараллеливание
      не должно снимать защиту `ERROR_LIMIT_FLOOR`) или разбить на
      батчи — не определён, стоит пересчитать по факту, когда
      пользователей станет заметно больше одного.
- [x] **Переход на Postgres в продакшене (15.09.2026).** С версии
      0.1.0 архитектура задумана СУБД-независимой: SQLite на
      разработке, Postgres/MariaDB/MySQL на проде — переключение
      только сменой `PI_DATABASE_URL` + `alembic upgrade head`, модели
      те же (`infra/db.py::_build_engine()`, `UtcDateTime` decorator
      уже сглаживает разницу timezone-aware/naive между движками).
      Фактически же прод все PR #1–#62 простоял на SQLite несмотря на
      это — разрыв между зафиксированным намерением (CLAUDE.md: «в
      продакшене — Postgres») и реальностью, найден пользователем
      15.09.2026. Решено закрыть сразу, не откладывая: по мере роста
      пользователей (Фаза 9, пункт про лимиты ESI выше) конкурентная
      запись веб-процесса и фоновых джобов в один SQLite-файл станет
      узким местом раньше многого другого.
      Движок — Postgres, не MariaDB/MySQL: нативный `JSONB` для колонок
      `pins`/`routes`/`structures`/`assumptions` (в MariaDB JSON — это
      `LONGTEXT` с CHECK, не настоящий тип), и это уже была
      зафиксированная в CLAUDE.md цель, а не новое решение с нуля.
      Добавлено: `psycopg[binary]>=3.1` в `requirements.txt` (нужен
      только на проде — в dev с SQLite ни один код его не импортирует),
      `scripts/migrate_sqlite_to_postgres.py` — разовый перенос уже
      накопленных строк (персонажи, зашифрованные токены, реальные
      колонии, история добычи, сохранённые планы) через ORM-модели, а
      не выгрузка/загрузка на пустую базу: без него переключение
      означало бы для всех вошедших пользователей чистый лист — заново
      логиниться через EVE SSO, терять сохранённые планы. Единственный
      autoincrement-PK среди перенесённых таблиц (`ExtractionSample.id`)
      требует ручной синхронизации Postgres-последовательности после
      копирования явных id — скрипт делает это сам. `deploy/README.md`
      дополнен реальными командами установки Postgres на Ubuntu (было
      только «переключите URL», без самой установки сервера).

      **Перенос на проде выполнен в тот же день, нашёл ещё одну ошибку
      по пути.** Первый прогон `migrate_sqlite_to_postgres.py` упал на
      первой же строке `extraction_samples`: `pin_id` был `Integer`, а
      реальные ESI id — 13-значные (`1053137617427`), SQLite ширину не
      проверял, Postgres — проверил. Расширено до `BigInteger`
      (миграция `b2c3d4e5f6a7`, PR #64), после нее перенос прошёл
      целиком (29 персонажей, 16 токенов, 81 колония, 12 795 снимков
      добычи), `PI_DATABASE_URL` на проде переключён, сервисы
      перезапущены, старый SQLite-файл оставлен как путь отката.

      **Заодно нашёл и закрыл регрессию бэкапа, которую сам же создал
      переключением.** `scripts/backup.py` до этого честно писал в лог
      «PI_DATABASE_URL не SQLite — используйте pg_dump» и просто
      ничего не делал для БД — то есть с переключением на Postgres
      суточный бэкап тихо перестал бы копировать саму базу, снимая
      только `data/cache/*.json`. Добавлена реальная поддержка
      `pg_dump` (тот же скрипт, та же логика "раз в сутки через
      scheduler"), формат — обычный SQL-дамп (восстановление
      `psql имя_базы < файл`, без `pg_restore`). Сбой `pg_dump`
      теперь честно возвращает код ошибки и не создаёт папку бэкапа с
      частичным содержимым — раньше молчаливый пропуск выглядел бы
      как «бэкап есть», хотя базы в нём не было бы вовсе.
- [x] **Источник игровых шаблонов назван в самом приложении, не только
      в коде (15.09.2026).** Игровые JSON-шаблоны застройки
      (`data/templates/`) — не наши, взяты из стороннего репозитория
      DalShooth/EVE_PI_Templates. В коде и dev-документации это уже
      было явно указано с самого начала (`CLAUDE.md`, `data/README.md`
      — полная ссылка, `roadmap.md`), но раздел «О проекте» → «Откуда
      данные» в самом приложении до сих пор говорил обезличенно
      «игровые JSON-шаблоны застройки», без имени автора и ссылки —
      обычный посетитель не мог узнать, откуда данные, не читая код.
      Добавлена ссылка на репозиторий и на автора (DalShooth) в оба
      языка. `tests/test_docs.py::test_about_credits_dalshooth_
      templates` — закрепляет факт тестом, по той же схеме, что и
      лицензия/автор самого PI Director.
- [x] **Зафиксировать версии зависимостей в репозитории (16.09.2026,
      выполнено).** `requirements.txt` по-прежнему задаёт версии только
      нижней границей (`Flask>=3.0`, `pandas>=2.2` и т.д.) — это
      описание намерения совместимости, не то, что реально ставится.
      Добавлен `requirements.lock.txt` — точные версии, снятые
      `pip freeze --local` из уже работающих venv ОБОИХ окружений (прод
      — Ubuntu, по SSH; разработка — Windows) и сверенные построчно:
      все пересекающиеся пакеты (`Flask`, `SQLAlchemy`, `alembic`,
      `cryptography`, `PyJWT`, `waitress`, `pandas`, `openpyxl`, `pytest`
      и их транзитивные зависимости) совпали версия в версию — значит
      один список без platform-маркеров подходит обоим окружениям.
      Пакеты, специфичные одному окружению (`psycopg`/`psycopg-binary` —
      только прод, Postgres; `colorama`/`tzdata` — транзитивные
      Windows-зависимости `click`/`pandas`), включены в общий файл: это
      чистый Python без платформенных условий установки, лишний пакет на
      другой платформе безвреден. `esprima` (только тесты) на проде не
      установлен — не мешает работе `web`/`scheduler`, эти сервисы его
      не импортируют, прод не запускает `pytest`.
      `pip install -r requirements.lock.txt` вместо `requirements.txt` —
      в `CLAUDE.md` (раздел «Запуск») и `deploy/README.md` (обе точки
      установки: Windows-заготовка и раздел 7.4, systemd на Ubuntu).
      Обновлять вручную по инструкции в шапке файла — не генерировать
      автоматически при каждой сборке, в этом весь смысл фиксации.
- [x] **Разобрано по репозиторию github.com/Alexmidrus/EveGAS_calc
      (GasLens, 15.09.2026) — четыре пункта, решения приняты; (1) и (4)
      выполнены 16.09.2026, (2) и (3) закрыты без действия (см. ниже).**

      **(1) Шрифты — заменены на Inter + JetBrains Mono, самостоятельный
      хостинг (16.09.2026, выполнено).** Раньше — IBM Plex Sans/Mono +
      Saira Condensed, грузились с `fonts.googleapis.com`/
      `fonts.gstatic.com` (внешний CDN-запрос на каждый визит). Скачаны
      те же 4 юникод-диапазона на семейство, что и у GasLens — latin,
      latin-ext, cyrillic, cyrillic-ext, — с самого Google Fonts
      (`fonts.googleapis.com/css2?family=Inter:wght@100..900&family=
      JetBrains+Mono:wght@100..800`, переменный вес одним файлом на
      диапазон, не по файлу на насечку — тот же приём, что и у
      источника), в `web/fonts/` (8 файлов, 242 КБ на все, отдаются тем
      же `web_files()` маршрутом, что и остальной `web/`, проверено
      `fetch()` из браузера — 200, верный `content-type`). `<link>` на
      Google Fonts заменён на `@font-face` с относительным путём и теми
      же `unicode-range`, что в исходном ответе Google (не выдуманы).
      Все `font-family` в CSS переведены: `'IBM Plex Sans'`→`'Inter'`,
      `'IBM Plex Mono'`→`'JetBrains Mono'`, `'Saira Condensed'`→`'Inter'`
      (Saira Condensed нигде не осталась — полная замена, по решению
      пользователя, принятому осознанно ради смены визуального
      характера редизайна Фазы 8, не только ради самохостинга).
      Проверено в браузере: все 8 `@font-face` зарегистрированы,
      реально используемые (latin+cyrillic) подмножества — `status:
      loaded`, неиспользуемые (latin-ext/cyrillic-ext) — честно
      `unloaded` до первой надобности; скриншот дашборда — вёрстка не
      разъехалась.

      **(2) Async Flask — не нужен, обсуждено и закрыто.** Правило 3
      (CLAUDE.md) уже гарантирует, что ни один HTTP-обработчик не ходит
      в сеть — весь блокирующий I/O вынесен в фоновые скрипты по
      расписанию, а async даёт выигрыш именно там, где запрос ждёт
      медленный I/O и есть что параллелить. Нагрузку уже решает правило
      5 (кэш/ETag/лимит размера запроса), не конкурентность. GasLens
      пришёл к тому же выводу независимо («Flask (traditional
      server-side rendering, not async)»). Реальное узкое место при
      росте пользователей — последовательный обход персонажей в
      `sync_colony_status.py` (см. пункт про лимиты ESI выше) — это
      вопрос фонового скрипта, не веб-фреймворка, async Flask его не
      решает.

      **(3) Jinja-шаблоны — не нужны, обсуждено и закрыто.** У GasLens
      — Jinja2+HTMX (сервер рендерит HTML-фрагменты, HTMX подменяет).
      У нас — самодостаточный SPA (`web/index.html`), вся логика
      планирования и симуляции колоний считается в JS на клиенте, без
      перезагрузки страницы на взаимодействие. Наш домен —
      вычислительный (плановик, симуляция материального потока живые
      таймеры), а не «список с фильтром», для которого HTMX силён.
      Переход на Jinja — переписать весь фронтенд с нуля, а не сменить
      пару зависимостей, ради архитектуры, которая хуже подходит именно
      нашей задаче.

      **(4) Документация организована как у GasLens, `README.md`
      переписан без упоминания «Фаз» (16.09.2026, выполнено).** У них:
      `docs/DOMAIN.md` (игровая механика и формулы), `docs/SPEC.md`
      (функциональные требования и интерфейс), `docs/ESI.md` (правила
      работы с API), `docs/ROADMAP.md` (этапы разработки и решения),
      `docs/CLAUDE.md` (инструкции для разработки с Claude). Раньше у
      нас всё было смешано в одном `roadmap.md` (1400+ строк —
      архитектура, история решений, TODO) и одном `CLAUDE.md` (правила
      проекта + устройство репозитория), а верхнеуровневый `README.md`
      держал таблицу «Фаза 3 — по коду готово; нужен client_id от CCP» и
      «Фаза 6 — нужна установка на сервер», хотя `client_id` получен и
      сервер развёрнут ещё в начале сентября 2026.

      Сделано: `roadmap.md` целиком стал `docs/ROADMAP.md` (`git mv`,
      история сохранена) — все ссылки на путь по репозиторию (комментарии
      в `.py`/`web/index.html`, `CLAUDE.md`, `deploy/README.md`) обновлены
      на новый путь. Из `CLAUDE.md` выделены `docs/DOMAIN.md` («Что
      известно точно, а что нет» и «Механики игры» — про игру, не про
      код) и `docs/ESI.md` (бывшее правило 11 целиком, с добавленным
      разделом про исключение из правила 3 и про новый токеновый лимит,
      16.09.2026) — сами разделы в `CLAUDE.md` заменены короткими
      пунктами с ссылкой на новый файл, а не удалены совсем: самое
      важное («единственная точка обращения — `esi_client.py`», «не
      выдумывать плотность сырья») остаётся видно сразу, не заставляя
      открывать второй файл ради одной строки. `docs/SPEC.md` НЕ заведён
      — это структура GasLens для справки, не обязательство копировать
      всё один в один; у нас функциональные требования и так живут в
      справке приложения (проверяется тестом, `domain/features.py`) и
      в `docs/ROADMAP.md`, дублировать их третьим документом было бы
      источником рассинхронизации, а не ясности. Устройство репозитория
      и правила разработки с Claude остаются в `CLAUDE.md` в корне — этот
      файл читает сам инструмент, переносить его нельзя.

      `README.md` переписан заново по структуре из GasLens (что делает /
      требования / установка / первые данные / запуск / конфигурация /
      сбор данных по расписанию / вход через EVE SSO / развёртывание /
      тесты / устройство / ограничения / версия / лицензия) — с нашими
      реальными данными: таблица переменных окружения, таблица сборщиков
      с интервалами, честный раздел «Ограничения» (те же пункты, что и в
      справке приложения — плотность сырья, число планет на персонажа,
      приостановленное прямое P2, неучтённый налог POCO), без единого
      «Фаза N».
- [x] **Пересмотрены условия «простаивает»/«требует внимания» в сводке
      «Мои колонии в игре» (16.09.2026, выполнено).** Разобрано по
      каждой из четырёх категорий сводки, реализовано в
      `renderColonies()`/`isColonyIdle()`/`orb()` (`web/index.html`):

      1. **Всего планет** — без изменений, корректно.
      2. **Простаивает (добыча)** — как и сейчас: `!nearest_expiry`
         (программа экстрактора закончена), причал на это не влияет.
      3. **Требует внимания (добыча)** — меняется: только «причал
         полон» (`isLaunchpadFull()`), программа экстрактора на это
         больше НЕ влияет — старое предупреждение «программа
         заканчивается меньше чем через 2 часа» (`soon` в
         `renderColonies()`) убирается совсем, по прямому решению
         пользователя: «только когда уже встала» — то есть без
         заблаговременного 2-часового предупреждения, только по факту
         полного причала или уже закончившейся программы (последнее —
         через категорию 2, не 3).
      4. **Счётчики независимые, не взаимоисключающие**: колония, где
         программа закончилась И причал полон одновременно, попадает
         в ОБА счётчика сразу («простаивает» и «требует внимания») —
         не одно ИЛИ другое. Подтверждено пользователем явно (таблица
         истинности: программа кончилась+причал не полон → только
         «простаивает»; причал полон+программа активна → только
         «требует внимания»; оба условия → оба счётчика).
      5. **«Причал полон» — только для добычи**, не для переработки:
         сейчас `isLaunchpadFull()` проверяет причал/склад любой
         колонии без учёта роли (`renderColonies()`,
         `web/index.html`), хотя для переработки полный причал (сырья
         на входе) — скорее хорошо, запас на будущее, а не плохой
         сигнал (в отличие от добычи, где полный причал = добывать
         больше некуда). Нужно завести проверку роли в само условие
         счётчика «требует внимания», не менять сам `isLaunchpadFull()`
         (он используется и в других местах — кольцо заполненности на
         круге колонии, подсказка при наведении — там роль не при
         чём, это просто честный процент).
      6. **Простаивает (переработка)** — переопределено: не «все
         фабрики находятся в состоянии idle по `pinCycleInfo()`» (как
         было), а «закончились входящие ресурсы сырья в причале,
         осталась только готовая продукция», то есть буквально
         `sim.depletionMs<=0` от того же расчёта, что и плашка «до
         конца запаса сырья в причале» (пункт выше в этой же Фазе, уже
         реализован — эта часть ждала именно его и теперь на него
         опирается напрямую, не оценивая «на глаз»).

      **Реализация (16.09.2026).** `isColonyIdle()` для `role_key==='proc'`
      заменена на `sim.depletionMs<=0` (пункт 6). `attnN` в
      `renderColonies()` заменён на `rows.filter(p=>p.role_key==='mine'
      &&isLaunchpadFull(p)).length` — старое 2-часовое предупреждение
      (`soon`) убрано совсем (пункт 3), причал учитывается только для
      добычи (пункт 5). Счётчики независимые по конструкции — оба
      фильтра проходят по одному и тому же `rows` без взаимного
      исключения (пункт 4), проверено таблицей истинности в браузере
      (`isColonyIdle`/`isLaunchpadFull` на синтетических строках):
      простаивает-без-полного-причала → только «простаивает»;
      причал-полон-программа-активна → только «требует внимания»; оба
      условия → оба счётчика; ни одно → ни одного. Заодно поймана и
      исправлена вторая точка того же сигнала — `orb()` (иконки в
      сетке/списке по персонажам) считала «требует внимания»
      (мигающее красное кольцо + запрещающий значок) через
      необусловленный `isLaunchpadFull(row)`, без учёта роли: заведена
      `launchpadAlarm=!isProc&&isLaunchpadFull(row)`, у переработки
      кольцо при полном причале остаётся обычным синим «заполненность»,
      не переключается на тревогу.
- [x] **«Прямое P2» приостановлено (15.09.2026, выполнено).** Пользователь
      спросил, работает ли «Делать P2 прямо на добывающих планетах», и
      сам верно заметил: готового P0→P2-шаблона у DalShooth нет.
      Подтверждено — `domain/direct_p2.py` действительно считает
      застройку из стоимостей отдельных структур (те же стоимости точно
      воспроизводят итоги всех пяти реальных шаблонов,
      `tests/test_capacity.py`), а не берёт её из проверенного игрового
      источника; сама КОМБИНАЦИЯ (два экстрактора + P1- и P2-фабрики на
      одной планете) ни разу не была сверена в игре. По решению
      пользователя — убрано из функционала, помечено в репозитории как
      приостановленное (не удалено, включаемо обратно):
      - `api/blueprints/plans.py::calculate()` — `direct_p2` форсируется
        в `False` независимо от тела запроса (второй барьер поверх
        UI, на случай прямого вызова `/api/calculate`).
      - Флажок «Делать P2 прямо на добывающих планетах» убран из
        `web/index.html` (Настройки), с комментарием почему; JS,
        собиравший тело запроса, шлёт `direct_p2:false` напрямую, не
        читая несуществующий чекбокс. Неиспользуемые ключи `T`
        (`directP2`/`directP2Hint`) удалены.
      - Раздел справки переименован в «P2 прямо на добывающей планете
        (пока приостановлено)», текст сокращён до объяснения идеи и
        причины паузы — вместо инструкции «как включить».
      - Запись `direct_p2` убрана из `domain/features.py` (реестр — про
        то, что пользователь реально может включить, не про весь код в
        репозитории) — `tests/test_docs.py` больше не требует упоминания
        в справке по ключевым словам реестра.
      - `domain/direct_p2.py`, `domain/planner.py::_plan_direct_p2/
        _place_direct_p2`, `tests/test_direct_p2.py` — НЕ удалены,
        работают на уровне domain-логики и тестов, просто недостижимы
        через API/интерфейс. Снять обе блокировки (`plans.py` +
        чекбокс), когда компоновку проверят в игре.
- [x] **Добавлен HTTP-тест на изоляцию `/api/colonies` между двумя
      аккаунтами (16.09.2026, выполнено).** Пользователь спросил, не видят ли
      разные пользователи чужих персонажей/колоний/планов — проверено:
      разделение реально есть везде (`account_id` в подписанной cookie
      сессии, `api/session.py`), инцидент был найден и исправлен
      11.09.2026 (CLAUDE.md, Фаза 3). Тестами подтверждено для
      персонажей (`test_db.py::TestLoad::
      test_prod_loads_real_rows_of_matching_account_only`,
      `test_prod_without_account_id_sees_nothing`) и для планов
      (`test_api.py::TestPlansIsolation`,
      `test_plan_storage.py::TestAccountIsolation` — 7 тестов).
      Честный пробел был именно в ПОКРЫТИИ ТЕСТАМИ, не в самой защите:
      `/api/colonies` (`api/blueprints/meta.py::colonies()`) фильтрует
      через тот же `load_characters(current_account_id())`, что и
      персонажи (уже проверенный примитив), поэтому гарантия логически
      была та же — но отдельного HTTP-теста на два разных `account_id`
      именно для этого эндпоинта не было (`TestColonies` в `test_api.py`
      тестирует сортировку/историю добычи, не многопользовательскую
      изоляцию). Добавлен `TestColoniesIsolation` (`tests/test_api.py`)
      по образцу `TestPlansIsolation::
      test_two_accounts_do_not_see_each_others_plans` — два аккаунта, у
      каждого своя колония, `/api/colonies` каждому отдаёт только свою
      (`test_two_accounts_do_not_see_each_others_colonies`), и анонимный
      визит на проде без входа не видит ничего
      (`test_anonymous_prod_visit_sees_no_colonies`).
- [x] **Основной персонаж — чтобы не логиниться заново всеми альтами на
      новом устройстве/браузере (16.09.2026, выполнено).** Причина была
      найдена в коде, не гипотеза: `account_id` — случайная строка в
      ПОДПИСАННОЙ COOKIE СЕССИИ БРАУЗЕРА (`api/session.py::
      ensure_account_id()`), заводится заново при первом визите без
      действующей cookie. `api/blueprints/auth.py::_store()` при каждом
      логине ПЕРЕЗАПИСЫВАЛ `account_id` персонажа на account_id ТЕКУЩЕЙ
      сессии безусловно — значит группировка персонажей в «одного
      пользователя» существовала только внутри ОДНОЙ cookie браузера, не
      персистентно.

      **Уточнено с пользователем перед реализацией (два открытых
      вопроса из наброска ниже):** (1) группа создаётся кнопкой
      «Сохранить группу» ПРЯМО В АКТИВНОЙ СЕССИИ, когда все альты уже
      видны вместе (не код привязки между отдельными входами — сложнее,
      а нужные персонажи и так уже вместе в типичном сценарии) — но
      основного выбирает САМ ПОЛЬЗОВАТЕЛЬ отметкой, не автоматически
      первый; (2) вход персонажем из ДРУГОЙ уже существующей группы, чем
      активна в сессии — честный отказ, не слияние и не молчаливая
      перезапись.

      **Реализация:**
      - `Character.primary_character_id` (nullable Integer,
        миграция `57fc4d4e8f63`) — NULL, если персонаж не в группе;
        внутри группы у ВСЕХ персонажей (включая основного — ссылается
        сам на себя) одно и то же значение — character_id основного.
      - `api/session.py::set_account_id()` — новая функция, заменяет
        account_id визита на конкретное значение (для перехода на
        групповой id), в отличие от `ensure_account_id()` (заводит
        случайный, только если его ещё нет).
      - `POST /api/auth/group` (`api/blueprints/auth.py::
        group_characters()`) — принимает `primary_character_id`,
        проверяет, что он входит в число esi-персонажей ЭТОГО визита,
        отказывает (409), если участники уже состоят в ДВУХ РАЗНЫХ
        существующих группах (сравниваются друг с другом, не с новым
        primary_id — иначе переназначение основного внутри ОДНОЙ и той
        же группы ложно считалось бы конфликтом), иначе привязывает всех
        участников к новому `primary_character_id` и переключает
        account_id визита на групповой (`set_account_id`).
      - `_store()` (вход через SSO) — если у персонажа уже есть
        `primary_character_id`, account_id визита становится
        детерминированным `f"char:{primary_character_id}"` вместо
        случайного; если в сессии уже есть персонажи из ДРУГОЙ группы —
        `GroupConflict`, колбэк отвечает `redirect
        /?auth=error&reason=group_conflict`, ничего не перезаписывая.
        Докстринг `_store()` обновлён — старое «повторный вход
        перевязывает — это ожидаемо» верно только для персонажей БЕЗ
        группы.
      - `web/index.html::renderCrew()` — радио-кнопка «основной» рядом с
        каждым esi-персонажем этого визита (только когда их ≥2), кнопка
        «Сохранить группу» (`saveCharacterGroup()`); текущая группа
        читается из `primary_character_id` в ответе `/api/characters`
        (новое поле, `api/blueprints/meta.py::characters()`) — если он
        один и тот же у всех esi-персонажей визита, соответствующее
        радио предотмечено.
      - Справка и `domain/features.py` (`character_grouping`, с версии
        0.10.2 — `version.py` обновлён) дополнены на обоих языках.
      - Тесты — `tests/test_auth.py::TestGroupCharacters` (создание
        группы, отказ на чужого персонажа, отказ на две разные
        существующие группы, переназначение основного внутри ОДНОЙ и той
        же группы — не конфликт) и `TestLoginGroupConflict` (вход из
        чужой группы отклонён, вход вторым персонажем ТОЙ ЖЕ группы —
        обычный успешный вход).
- [x] **Прибыльность построенного плана с учётом налога POCO
      (16.09.2026, выполнено).** Для уже построенного плана считается
      расчётный месячный выход, выручка, налог POCO (экспорт + импорт)
      и чистая прибыль при полной загруженности цепочки.

      **Что уже есть и переиспользуется:**
      - `domain/profit.py` — уже официально признаёт налог POCO
        неучтённым фактором в собственной документации («налог POCO при
        вывозе с планеты — колонка есть в planet_industry.csv, но
        объёмы перевозок мы не считаем») и уже содержит нужный
        вычислительный движок: `colonies_for()`/`evaluate()` разворачивают
        цепочку через `expand_demand()` (`domain/throughput.py`) и знают
        `schematic.output_per_hour` на каждом тире.
      - `data/planet_industry.csv` уже содержит РЕАЛЬНЫЕ колонки
        `POCO Tax Rate [%]` и `POCO Owner` по каждой конкретной планете
        (обнаружено при разборе этого пункта, нигде не используется) —
        точнее, чем «по типу», но статичный снимок, ставка/владелец
        меняются в игре когда угодно. Пользователь решил вводить ставку
        вручную по ТИПУ планеты — честнее, потому что не протухает
        молча (правило 1), но грубее, чем данные по конкретной планете.
        Стоит держать в уме, что точные данные по факту уже лежат в
        репозитории на будущее.
      - Разница с уже существующей таблицей «Что выгоднее производить»
        (тот же `domain/profit.py::rank()`, вкладка «Настройки»):
        та таблица считает ОБОБЩЁННУЮ экономику «один полный шаблон
        каждого продукта» (`FACTORIES_PER_TEMPLATE`), не привязана к
        конкретному построенному плану. Новая функция — про РЕАЛЬНОЕ
        число колоний/фабрик из УЖЕ ПОСТРОЕННОГО плана
        (`structures_detail` строк плана), не обобщённое предположение.

      **Открытый вопрос закрыт исследованием, не гаданием.** POCO
      облагает налогом И экспорт (вывоз с планеты, NPC-часть — 10%,
      снижается скиллом Customs Code Expertise), И импорт (ввоз на
      планету, NPC-часть — 5%) — подтверждено EVE University wiki и
      support.eveonline.com (docs/ROADMAP.md ссылается на источники в
      комментариях `domain/poco_tax.py`), не выдумано и не одна ставка
      на всякий вывоз. Пользователь подтвердил это как основу расчёта.

      **Граф хопов не потребовался — точный расчёт получен БЕЗ него.**
      Вместо аппроксимации «хоп = переход между тирами» (черновой вариант
      выше) найден точный способ через сами строки уже построенного
      плана: каждая строка знает, что производит (`res_out`,
      `structures_detail` — настоящие фабрики этой строки) и, если это
      переработка, что потребляет (`schematic.inputs`). Экспортный налог
      строки — с её СОБСТВЕННОГО выхода по ставке ЕЁ ТИПА планеты;
      импортный налог строки-переработки — с её СОБСТВЕННОГО потребления
      по той же ставке. Сумма по всем строкам плана даёт ровно то же,
      что дало бы суммирование по хопам (экспорт источника + импорт
      назначения), но без необходимости знать, какая колония кормит
      какую — это уже описано СВОЕЙ строкой с каждой стороны.

      **Реализация:**
      - `domain/poco_tax.py` (новый модуль, не часть `profit.py` — тот
        считает ОБОБЩЁННУЮ экономику шаблона, этот про РЕАЛЬНЫЕ
        колонии/фабрики уже построенного плана) — `evaluate_plan_
        profitability()`, `HOURS_PER_MONTH = 720` (30×24, конвенция
        EVE-калькуляторов, зафиксирована константой). Выручка считается
        только с строк, чей `res_out` — целевой продукт запроса
        (промежуточные тиры не продаются, только платят экспортный
        налог). Честные пробелы: продукт без цены/планета без ставки —
        в `missing_prices`/`missing_rates`, не подставляется 0 (правило
        1) — но `monthly_revenue`/`export_tax`/`import_tax` по
        отдельности остаются частичными суммами, а `monthly_net_profit`
        — `None`, пока картина не полная целиком (частичная чистая
        прибыль выглядела бы прибыльнее, чем есть на самом деле).
      - `POST /api/plan-profitability` (`api/blueprints/plans.py`) —
        принимает уже построенные строки плана (фронтенд их и так уже
        имеет, пересчитывать план заново не нужно), `target_products`,
        `poco_rates`; цены — тот же снимок, что и у `/api/advice`
        (`api/blueprints/market.py::_load_snapshot`), правило 3 не
        нарушено. Ставка выше 100% отклоняется как явная опечатка.
      - `web/index.html` — сетка из 8 полей ставок POCO по типам планет
        (`renderPocoRateInputs()`, вкладка «Настройки», рядом с «Запас
        на истощение месторождений» — тот же характер ввода; пустое
        поле — «не введено», не 0%) и панель «Прогноз прибыльности
        плана» (`renderPocoProfit()`, под таблицей «Что выгоднее
        производить»): выручка/налог/чистая прибыль в месяц, честные
        подсказки о недостающих ценах/ставках, оговорка о допущении
        полной загруженности.
      - Справка (`domain/features.py::plan_poco_profitability`, версия
        0.10.3) и `CLAUDE.md` дополнены на обоих языках.
      - Тесты — `tests/test_poco_tax.py` (9 тестов: экспорт+импорт на
        каждом хопе, промежуточный продукт платит налог, но не продаётся,
        честные пробелы по цене/ставке, чистая прибыль требует полной
        картины) и `tests/test_api.py::TestPlanProfitability` (5 тестов:
        HTTP-контракт, валидация ставок, работа без снимка цен).

      **Реальные колонии — отдельным пунктом ниже**, по решению
      пользователя (16.09.2026): версия для плана сначала, реальные
      колонии — отдельный PR после неё.
- [x] **То же самое, но для настоящих колоний («Мои колонии в игре»,
      16.09.2026, занесено пользователем, реализовано в тот же день).**
      Тот же расчёт (месячный выход × цена − налог
      POCO), только источник данных другой — реальные синхронизированные
      колонии, а не расчётный план. Два решения приняты после
      обсуждения, и они СИЛЬНО упрощают эту версию относительно плановой:

      1. **Скорость — реальная текущая, не гипотетическая «на полную».**
         В отличие от плана (там нет исполнения, только теория), для
         настоящих колоний уже есть честные данные о факте: скорость
         добычи с поправкой на затухание (`extractorRatePerHour`, #55),
         реальное состояние каждой фабрики через симуляцию материального
         потока по маршрутам (`simulateColonyFactories`, #58/#59) —
         честное число «простаивает»/«в производстве» на каждый пин.
         Месячный выход берёт ИМЕННО текущую скорость каждого пина
         (с поправкой на дефицит/простой, если он есть), не
         теоретический максимум — честнее, но может быть ниже, если
         колония работает не на полную.
      2. **Налог — поколонийно, без графа между колониями.** ESI не
         отдаёт связь «какая колония кормит какую» МЕЖДУ разными
         колониями (в отличие от связей МЕЖДУ ПИНАМИ ОДНОЙ колонии,
         которые есть, #58) — куда игрок физически везёт сырьё
         дальше, знает только он сам. Поэтому налог считается за
         каждую колонию САМУ ПО СЕБЕ: что реально покидает ЭТУ планету
         (у майнинг-колонии — либо сырьё с экстрактора, если на планете
         нет своих фабрик, либо продукт её собственных фабрик, если
         добыча перерабатывается прямо на месте; у перерабатывающей —
         выход её фабрик), обложенное ставкой POCO для ТИПА ЭТОЙ
         планеты (тот же пользовательский ввод по типам, что и в
         версии для плана — не дублируется, один общий источник ставок
         на обе функции). Понятие «цепочка» и попытка связать колонии
         между собой сознательно не реализуются — пользователь выбрал
         этот вариант явно, вместо более сложной альтернативы (ручная
         разметка пользователем, какие колонии друг друга кормят).

      **Что переиспользуется:** ставки POCO по типам планет — общий
      ввод с предыдущим пунктом (одна настройка на обе функции, не
      две отдельные). Цена продукта — та же инфраструктура снимка цен
      (`/api/market/best-product`), что уже используется для
      «Что выгоднее производить»/оценки плана.

      **Место показа — решено пользователем:** агрегированная строка
      над списком «Мои колонии в игре» (не на карточке каждой колонии
      по отдельности) — тот же характер, что и у панели прогноза для
      плана под таблицей «Что выгоднее производить».

      **Реализация:**
      - `domain/poco_tax.py::ColonyProfitability`/`evaluate_colonies_
        profitability()` — новый расчёт рядом с версией для плана, не
        вместо неё (разные допущения, см. докстринг класса): вход —
        список записей `{label, product, units_per_hour, planet_type}`
        по одной на настоящую колонию, а не строки плана. Колония с
        `units_per_hour is None` (текущее состояние неизвестно) — в
        `missing_output`, не участвует ни в одной сумме (не 0, честный
        пробел, отдельный от `missing_prices`/`missing_rates`); `0` —
        сама по себе честный факт («простаивает»), не пробел. Налог —
        только экспортный (`monthly_tax`), без `export`/`import` пары
        версии плана: колонии не связаны графом.
      - `POST /api/colonies-profitability` (`api/blueprints/plans.py`) —
        тот же контракт по духу, что и `/api/plan-profitability`;
        валидация ставок и чтение снимка цен вынесены в общие
        `_parse_poco_rates()`/`_load_prices()`, переиспользуемые обоими
        эндпоинтами (было продублировано, теперь один источник).
      - `web/index.html` — текущая скорость выхода каждой колонии
        считает новая `colonyOutputRatePerHour()`: для добычи без своей
        фабрики — уже существующая затухшая скорость экстрактора
        (`extractorRatePerHour`, #55, ничего досчитывать не нужно); для
        добычи со своей фабрикой и для переработки — номинальная
        скорость схемы (`data/schematics.json`), но только если
        `pinCycleInfo()` (с учётом `simulateColonyFactories()`, #58/#59 —
        те же данные, что красят кольцо на кружке пина) подтверждает,
        что цикл идёт СЕЙЧАС; простаивающая фабрика — честный 0, а
        состояние «неизвестно с момента снимка» — честный `null`, не 0
        и не номинал. Какой именно пин колонии физически отдаёт продукт
        дальше — общая с `producedInfo()` логика, вынесена в
        `colonyOutputFlow()` (рефакторинг без изменения поведения круга
        колонии — тот же выбор пина, что и раньше). Новая
        `renderColoniesProfit()` строит агрегированную панель (тот же
        визуальный компонент `.poco-profit`, что и у прогноза для
        плана) над списком «Мои колонии в игре» (`#coloniesProfit`,
        сразу над `#colonies`), перерисовывается при загрузке колоний,
        смене языка и правке ставок POCO.
      - Справка (`domain/features.py::colonies_poco_profitability`,
        версия 0.10.4) дополнена в разделе «Мои колонии в игре» на
        обоих языках — не общий раздел с версией для плана, потому что
        расчёт различается (нет графа между колониями, реальная, а не
        теоретическая скорость).
      - Тесты — `tests/test_poco_tax.py::TestColoniesProfitability`
        (7 тестов: сумма по нескольким колониям, простой — честный 0,
        неизвестная скорость — честный пробел, отдельные пробелы по
        цене/ставке, пустой список, форма `to_dict()`) и
        `tests/test_api.py::TestColoniesProfitability` (5 тестов:
        HTTP-контракт, валидация, работа без снимка цен).
- [x] **Круг добывающей колонии показывал сырьё P0, а не выход P1
      (16.09.2026, выполнено).** Найдено пользователем: на основном
      кружке колонии во всех трёх видах «Моих колоний в игре»
      (детальный/список/сетка) для добычи `producedInfo()`
      (`web/index.html`) брала продукт активного экстрактора — то есть
      сырьё P0, которое эта же колония сразу перерабатывает в P1 своей
      собственной фабрикой (`basic_industry_facility`, шаблон miner_00),
      а не то, что она фактически отдаёт дальше по цепочке. На кружках
      пинов в детальном виде (`structOrbsForRow()`) для этой же фабрики
      уже показывался правильный P1-продукт — несовпадение с главным
      кружком колонии. Исправлено: `producedInfo()` для добычи сначала
      ищет P1-продукт на пине `basic_industry_facility`, и только если
      его нет (пины ещё не полностью пересинхронизированы) — честно
      откатывается на сырьё активного экстрактора. Подсказка при
      наведении также поправлена: «Добывает» показывается только для
      отката на сырьё, «Производит» — когда показан настоящий P1-выход
      фабрики (новое поле `info.extracted`).
- [x] **Кнопка «↓ Мои колонии» ведёт к панели прибыльности по факту, не
      к самому списку (16.09.2026, выполнено, PR #91).** `jumpTo('colonies')`
      → `jumpTo('coloniesProfit')`: панель прогноза стоит прямо над
      списком, прыжок теперь сразу показывает и её. `#coloniesProfit`
      добавлен в общий `scroll-margin-top` (тот же приём, что и у
      `#dashBody`/`#colonies`/`#shopping`).
- [x] **«Прогноз прибыльности плана» перенесён с «Настроек» на
      дашборд (16.09.2026, выполнено, по прямому запросу пользователя —
      обсуждено заранее: панель относится к уже построенному плану, а
      не к вводу параметров, симметрично панели по факту у настоящих
      колоний).** `#pocoProfit` перемещён из `<main>` страницы
      «Настройки» (был между таблицей «Что выгоднее производить» и
      сырой таблицей плана) в начало страницы «Дашборд» — СРАЗУ НАД
      `#dashBody` (карточками плана), той же симметрии ради, что и у
      `#coloniesProfit` над списком колоний. Кнопка «↑ План»
      (`jumpTo`) перенацелена с `#dashBody` на `#pocoProfit` — иначе
      прыжок вверх молча пропускал бы панель прибыльности, которая
      теперь стоит выше карточек, а не ниже таблицы плана, как раньше.
      `#pocoProfit` добавлен в общий `scroll-margin-top`. Таблица «Что
      выгоднее производить» (`#profit`) и ввод ставок POCO по типам
      планет остались на «Настройках» без изменений — решение
      пользователя (перенос только панели прогноза, не всего блока).
      Справка (RU/EN) обновлена: раздел «Прогноз прибыльности плана»
      теперь описывает панель на дашборде, а не «под таблицей выше».
- [x] **Три мелкие находки на дашборде плана, все 16.09.2026 (PR #97).**
      (1) `orb()` для строк РАСЧЁТНОГО ПЛАНА не вызывал `avatarImg()` —
      в виде «Сетка» вместо портрета персонажа оставались только
      инициалы (соседняя ветка для настоящих колоний это уже делала).
      (2) В виде «Список по персонажам» плана аватар был на дефолтных
      30px из CSS, а не на 52px, как в таком же списке «Мои колонии в
      игре» — размеры визуально разъезжались; заодно по просьбе
      пользователя убраны имя и счётчик колоний под аватаром (та же
      информация — в title-подсказке, как у колоний). (3) Подсказка
      колонии (`.orb .tip`) центрирована `left:50%` — у крайних левых/
      правых кругов сетки центр выводил её за экран. Добавлен расчёт
      сдвига (`fixTipOverflow`/`--tip-shift`) сразу после вставки кругов
      в DOM (не по наведению — так поймали гонку с CSS-переходом
      `transform`, сдвиг получался неверным), пересчитывается при
      изменении ширины окна и после `document.fonts.ready`
      (`font-display:swap` у шрифтов на пару кадров сам сдвигает
      вёрстку — тоже поймано вручную при проверке).
- [x] **Причал/склад перерабатывающей колонии — содержимое не убывало,
      готовая продукция не показывалась, а время до конца сырья считалось
      с систематической ошибкой (17.09.2026, найдено пользователем).**
      Три связанных находки в одном заходе:
      1. Панель «Детали» рисовала `pin.contents` как есть — статичный
         снимок ESI с последней синхронизации (раз в 30 мин), не
         досчитанный вперёд. Список сырья в причале визуально не менялся
         между обновлениями страницы, хотя фабрики его расходуют прямо
         сейчас (`simulateColonyFactories()` это уже считает — просто
         результат никуда не попадал, использовался только для
         `depletionMs` и `cycle`/`idle` фабрик).
      2. Тот же снимок никогда не включал готовую продукцию, которая
         едет обратно в причал по маршруту от фабрики — она появляется
         в игре ПОСЛЕ снимка, а мы её не досчитывали.
      3. `depletionMs` («до конца запаса сырья в причале») считался
         формулой «остаток ÷ расход в час» — непрерывным приближением
         поверх ПО СУТИ дискретного потребления: фабрика забирает вход
         целиком в момент старта цикла, а не размазывает расход по всей
         его длительности. Формула систематически занижала время: цикл,
         на старт которого сырья ещё хватило, в игре всё равно
         докручивается до конца. Пользователь описал symptом точно:
         «время почти точное, но показывает окончание на час раньше, как
         будто пропускает тот цикл, когда сырья уже не будет, но фабрики
         уйдут на последний цикл».

      **Реализация (`simulateColonyFactories()`, `web/index.html`).**
      Расписание событий раньше останавливалось ровно на «сейчас»
      (`schedule()` отбрасывало всё позже `nowMs`); теперь горизонт
      продлён на 90 дней вперёд (дальше `MAX_EVENTS=50000` в любом
      случае оборвёт раньше на настоящих колониях), и депletion ищется
      ТЕМ ЖЕ САМЫМ точным событийным движком, что считает «сейчас»-
      состояние — не отдельной формулой:
      - `snapshotResults()` — состояние на «сейчас» снимается ровно в
        момент, когда лента событий впервые переходит через `nowMs» (до
        этого момента оно ещё совпадает с тем, что вернула бы старая
        версия функции). Для фабрик — тот же `{cycle,idle,...}}`, что и
        раньше. Для причала/склада/хранилища КЦ — новое: `buf`
        симуляции переводится обратно в список `{name, amount}` (то же
        имя typeId → название продукта, что и у `pin.contents`) —
        содержит и остаток сырья, и уже произведённую, но не вывезенную
        продукцию, если для неё задан маршрут сюда.
      - `firstFutureIdle` — во время продолжения симуляции ПОСЛЕ
        «сейчас» отмечается первый момент, когда фабрика, которая
        только что работала (`wasActive`), не смогла набрать полный
        цикл сразу по всем своим входам. Это и есть момент, когда
        производство в колонии реально останавливается —
        `depletionMs = firstFutureIdle - nowMs`. При нескольких
        независимых цепочках в одной колонии первая же встающая фабрика
        сама даёт нужный минимум — отдельный перебор «по каждому причалу
        и виду сырья», как было раньше, не нужен: точный движок уже
        учитывает совместное истощение нескольких входов одной фабрики.
      - `storageBlockHTML(pin, projectedContents)` — новый необязательный
        параметр; когда он есть (панель «Детали» реальной колонии,
        `pinUnitsHTML()` передаёт `ctx.sim.contents`), список содержимого
        строится из проекции, а не из `pin.contents`. Ёмкость в м³
        по-прежнему берётся из снимка — объём одной единицы товара знает
        только сервер (`type_volume()`), на фронтенде считать не из чего;
        сам список товаров при этом уже живой.
      - Проверено вручную на синтетической колонии (3 продвинутых
        фабрики, причал с двумя видами сырья 1:1 и обратным маршрутом
        готовой продукции): содержимое причала в панели «Детали»
        уменьшается и показывает накопленный продукт; расчётное время до
        конца сырья (590 мин) на 25 мин больше, чем дала бы старая
        формула (565 мин) — направление ошибки подтверждено, не только
        сам факт наличия.
      - Справка (RU/EN, раздел «Мои колонии в игре») дополнена.
        `version.py` → 0.10.5 (исправление в пределах уже завершённой
        Фазы 9, не новая фаза).
      **Найдена и исправлена ещё одна ошибка в той же правке, в тот же
      день — по свежим данным прода, не по синтетике.** Пользователь
      сравнил панель «Детали» с посторонним PI-инструментом на настоящей
      колонии (Meda Metha, AV-VB6 XIII, 12 продвинутых фабрик на один
      причал): конечный продукт (Miniature Electronics) совпал день в
      день — 3060 у обоих, а вот сырьё разошлось: у нас в причале
      оставалось 1370 каждого вида, у стороннего инструмента (и, значит,
      в игре) — 890. Ключевая деталь была не в математике депletion, а в
      исходных данных: у КАЖДОЙ из 12 фабрик в её собственном
      `pin.contents` уже лежал ровно один цикл сырья (40+40) — то есть
      игра резервирует вход СЛЕДУЮЩЕГО цикла в фабрике сразу, как только
      кончился предыдущий, а не «по требованию» перед стартом. Прежняя
      версия `simulateColonyFactories()` пыталась забрать сырьё со
      склада только ПЕРЕД проверкой «хватает ли на цикл» — то есть на
      каждое событие ровно один раз, и следующий забор случался только
      на СЛЕДУЮЩЕМ событии этой же фабрики (через целый `cycleMs`).
      Итог: и выпуск продукции, и расход склада синхронизировались
      верно НАЧИНАЯ со второго события, но на 12 фабриках, доживших до
      51-го цикла каждая (реальные данные: `last_cycle_start`
      совпадает с `game_last_update`, между снимком и «сейчас» прошло
      ~51.35 часа), склад отставал от продукции ровно на один общий
      «раунд» — 12 × 40 = 480 единиц каждого вида сырья, что на
      реальных числах точно объясняет расхождение 1370 vs 890
      (25370 − 51×40×12 = 890, у нас же выходило 25370 − 50×40×12 =
      1370). Та же самая недостача одного раунда забора и объясняла
      «время до конца сырья на час меньше, чем в реальности» — вопрос,
      с которого пользователь начал разбор.
      Исправлено: логика забора со склада вынесена в `pullFromStorage()`
      и вызывается ДВАЖДЫ на каждое успешное событие фабрики — один раз
      перед проверкой «хватает ли на цикл» (как раньше — покрывает
      случай, когда сырья не хватало и его успели подвезти), и один раз
      сразу ПОСЛЕ успешного производства (новое — фабрика немедленно
      пытается набрать буфер под следующий цикл, как это делает игра).
      Проверено на точной копии настоящих данных этой колонии, вытащенных
      из БД прода по SSH (`colonies.pins`/`colonies.routes`,
      character_id 2122845449, planet_id 40291557): проекция теперь даёт
      ровно 890/890/3060 — побитовое совпадение с посторонним
      инструментом, не приблизительное.

      **Уточнён критерий «до конца запаса» — снова в тот же день, снова
      по разбору той же реальной колонии.** Пользователь дал точную
      контрольную цифру вручную: 890 сырья, 27 минут до конца текущего
      цикла, расход 12×40=480 за раунд — по простому делению должно было
      остаться «два раунда» (87 минут), но реальный игровой инструмент
      показывал на час больше. Разбор на точной событийной модели
      объяснил разницу: раунд, где сырья хватает не на все 12 фабрик
      сразу (890 → 410 после первого раунда, а второй требует 480), НЕ
      останавливает колонию целиком — 10 из 12 фабрик всё равно получают
      своё и отрабатывают ещё цикл, только 2 остаются без сырья. Первая
      версия правки (`firstFutureIdle`) фиксировала момент, когда
      встаёт ПЕРВАЯ работавшая фабрика — это честнее прежней формулы, но
      всё ещё занижает время для колонии в целом: она не «встала», просто
      временно недосчиталась одной-двух фабрик из многих.
      Промежуточный вариант (обнаружение нехватки сразу в момент резерва
      «под следующий цикл», а не на следующей проверке той же фабрики)
      был реализован и отменён в тот же час — численно он давал ровно
      наивные «87 минут», которые пользователь как раз назвал неверными;
      сохранён как несостоявшийся, чтобы не наступать на те же грабли
      снова.
      **Решение пользователя:** плашка показывает момент, когда встаёт
      ПОСЛЕДНЯЯ из работавших фабрик (`lastFutureIdle` — то же
      присвоение на каждый переход «работала → встала» после «сейчас»,
      без ограничения «только первый раз»; события идут строго по
      возрастанию времени, поэтому последнее присвоение за весь проход
      и есть истинный максимум). Честное ограничение: при нескольких
      НЕЗАВИСИМЫХ причалах/цепочках в одной колонии число означает
      «когда встанет самая живучая цепочка», не «когда любая из них» —
      отдельный учёт по каждой независимой цепочке потребовал бы заранее
      знать, какие фабрики вообще связаны общим источником, а какие нет.
      Проверено на той же синтетической схеме (890/27 мин/12 фабрик):
      движок даёт 207 минут — больше и наивных 87, и промежуточных 147,
      что для этого случая ожидаемо (несколько раундов частичной
      конкуренции подряд, а не один).

- [x] **Заполненность причала/склада в м³ — тоже статичный снимок, не
      успевала за составом (17.09.2026, найдено пользователем сразу
      вдогонку).** Состав причала со временем сдвигается от «тяжёлого»
      сырья к более «лёгкой» готовой продукции (или наоборот, смотря по
      объёму на единицу) — список содержимого это уже честно показывал
      (см. пункт выше), а вот заполненность в м³ (и кольцо на самом
      круге колонии) оставалась от последнего снимка ESI, потому что
      объём одной единицы товара знал только сервер
      (`scripts/sync_colony_status.py::type_volume()`,
      `data/cache/type_volumes.json`), фронтенду взять его было неоткуда.
      Пользователь спросил прямо: знаем ли мы, сколько места занимает
      единица сырья каждого тира, или можно прочитать занятый объём из
      игры — ответ: да, уже знаем, просто эта информация не доходила до
      фронтенда.
      Реализовано: `/api/initial-data` отдаёт `type_volumes` —
      `api/blueprints/reference.py::_type_volumes()`, тот же кэш
      `data/cache/type_volumes.json`, ЧТО сервер использует для
      настоящего used_m3 (не живой запрос к ESI — правило 3, просто
      чтение уже накопленного файла). Кэш растёт во время работы
      процесса (новые типы предметов дописывает `sync_colony_status`),
      поэтому, как и у `_type_ids()`/`_schematics_by_output()`, свежие
      типы попадают во фронт только после перезапуска процесса — до
      этого честное «нет данных» для конкретно этого предмета, не
      поломка. `simulateColonyFactories()` (`web/index.html`) считает
      `usedM3` в момент снимка «сейчас» по той же проекции содержимого
      (сумма amount×объём по каждому предмету, честный `null`, если
      объём хоть одного неизвестен). `storageBlockHTML()` использует
      проекцию вместо статичного `pin.used_m3`, когда она есть;
      `launchpadFillPct()`/`launchpadPin()`/`isLaunchpadFull()`
      (кольцо на круге колонии и общий счётчик «требует внимания») —
      туда же, принимают тот же `sim`, что и бейдж истощения, вместо
      того чтобы каждый читать сырой `pin.used_m3` по отдельности.
      Тесты — `tests/test_api.py::test_initial_data_includes_type_
      volumes`/`test_initial_data_type_volumes_empty_without_cache_
      file`. Проверено вручную: кольцо и число в м³ в подсказке круга
      совпадают друг с другом и с панелью «Детали» на синтетических
      данных (объём вручную посчитан построчно и сверен с выводом).

      **На реальной колонии после деплоя выяснилось: объём для
      конечного продукта причала до сих пор не знаем (17.09.2026,
      найдено пользователем).** `type_volume()` запрашивает объём
      только для того, что РЕАЛЬНО лежит в `contents` пина на момент
      КОНКРЕТНОЙ синхронизации — колонию «Meda Metha» синхронизировали
      2026-09-14, когда в причале ещё не было ни грамма Miniature
      Electronics (только сырьё), поэтому объём для него в кэш так и не
      попал. Список содержимого панели «Детали» это не портит (он не
      требует знать объём), а вот `usedM3` честно уходит в `null` при
      первом же неизвестном типе — интерфейс откатывается на статичный
      `pin.used_m3`, ровно то, что пользователь и увидел (96%,
      9641/10000 м³ — то же самое число, что было при самой первой
      находке этой проблемы, несмотря на задеплоенный фикс).
      Не баг проекции — честный пробел в данных, просто обнаруживший
      себя не сразу, а только на КОНКРЕТНОЙ колонии с конкретной
      историей синхронизаций.

      Решение — не ждать, пока каждая колония случайно засинхронизируется
      с нужным содержимым в причале, а закрыть пробел разом: каталог
      продукции PI конечен (68 продуктов P1-P4 + R0-сырьё, из которого
      они делаются) и не меняется без обновления игры, значит можно
      заранее узнать объём для ВСЕХ них через публичный ESI-эндпоинт
      (`/universe/types/{id}/`, без авторизации, не расходует лимит
      ошибок ни одного персонажа) — не дожидаясь, пока конкретный
      предмет случайно попадётся в чьём-то причале.
      `scripts/backfill_type_volumes.py` (новый, разовый, по образцу
      `resolve_pi_structure_type_ids.py`): берёт все имена из
      `domain/recipes.py::load_recipes()` (P1-P4 продукты + их
      R0-источники), резолвит в type_id через `data/type_ids.json` — с
      учётом уже существующего `domain/planets.py::
      RESOURCE_NAME_ALIASES` для единственного известного расхождения
      имён между `recipes.json` и игрой/`type_ids.json`
      («Microorganisms» / «Micro-Organisms»), и запрашивает объём
      каждого через уже существующий `type_volume()`. Запущен на проде
      по SSH одноразово — `data/cache/type_volumes.json` дополнен
      объёмом всех 83 продуктов сразу, а не только тех, что случайно
      засветились в чьём-то причале на момент синхронизации.
- [x] **Ставка POCO берётся автоматически из data/planet_industry.csv,
      ручной ввод по типу становится переопределением (17.09.2026, по
      прямому запросу пользователя после обсуждения мультирегиональности:
      «зачем ручной ввод, если ставка уже есть в файле?»).**
      Действительно есть — `POCO Tax Rate [%]`/`POCO Owner` на каждую
      КОНКРЕТНУЮ планету, просто не использовались нигде в расчёте
      (`domain/poco_tax.py` изначально сознательно выбрал ручной ввод по
      типу — обоснование было: «статичный снимок устаревает молча»).
      Пересмотрено, а не отменено: ставка/владелец POCO в игре
      действительно не постоянны (владелец меняет ставку в любой момент
      без предупреждения, структура меняет владельца после войны за
      суверенитет) — но правильный ответ на этот риск не «не
      использовать данные вовсе», а «использовать точные данные и честно
      показать, откуда они и с какой даты», что и сделано.

      **Дополнительно найдено при подготовке (17.09.2026):** ставка «по
      типу» была бы заведомо ГРУБЕЕ точной — в загруженном регионе у
      одного и того же типа планеты встречаются РАЗНЫЕ ставки сразу
      (Barren: 218 планет на 3%, 68 на 1%; Temperate: 81 на 3%, 30 на
      1%; остальные 6 типов — уже 100% одна ставка). Значит «по типу»
      в принципе не может быть точным источником для двух самых частых
      типов планет — только точная ставка КОНКРЕТНОЙ планеты (которая у
      нас уже есть в файле) даёт настоящее число, а не приближение,
      которое правило 1 не одобряет.

      **Приоритет источников ставки** (`domain/poco_tax.py::_resolve_rate()`):
      1. ручной ввод пользователя по типу планеты — переопределение,
         если он знает, что реальная ставка сменилась после снимка файла;
      2. точная ставка ЭТОЙ КОНКРЕТНОЙ планеты из
         `data/planet_industry.csv` (`domain/planets.py::PlanetBook.
         poco_rate()`, новый метод, по образцу `radius_km()`);
      3. иначе — честный пробел (`missing_rates`), как и раньше.

      **Реализация:**
      - `domain/planets.py` — `POCO_RATE_COLUMN`, `POCO_SNAPSHOT_DATE`
        ("2026-09-07" — дата последнего изменения файла в репозитории,
        честно как единственный доступный ориентир, в самом CSV такой
        метки нет), `PlanetBook.poco_rate(system, planet)` (ставка одной
        планеты, доля 0..1) и `PlanetBook.poco_rate_breakdown()`
        (сводка «сколько планет этого типа на какой ставке» — для
        честной подсказки пользователю, что «по типу» — приближение).
      - `domain/poco_tax.py` — `RowFlow`/`ColonyProfitability` получили
        `system`/`planet`; `_resolve_rate()` — общий резолвер для
        `evaluate_plan_profitability()` и `evaluate_colonies_
        profitability()`, обе получили новый необязательный параметр
        `planets: PlanetBook | None` (по умолчанию `None` — старое
        поведение, только ручной ввод, без регрессии).
      - `api/blueprints/plans.py::_load_planets_book()` — общий
        загрузчик для обоих эндпоинтов (`/api/plan-profitability`,
        `/api/colonies-profitability`), `None` при отсутствии файла
        (свежий репозиторий без данных — прогноз честно остаётся на
        ручном вводе, не падает).
      - `/api/initial-data` — новые поля `poco_rate_breakdown` (сводка
        по всем типам) и `poco_snapshot_date`.
      - `web/index.html` — `renderColoniesProfit()` теперь шлёт
        `system`/`planet` в каждой записи колонии (у строк плана они уже
        были всегда, ничего добавлять не пришлось). `renderPocoRateInputs()`
        — плейсхолдер сменился на «авто» (не «—»), над каждым полем
        всплывающая подсказка с разбивкой по ставкам и датой снимка из
        `pocoRateBreakdown`/`pocoSnapshotDate`. Справка (RU/EN, оба
        места — прогноз для плана и для настоящих колоний) и подпись
        поля обновлены под новый смысл (переопределение, не единственный
        источник).
      - Тесты — `tests/test_planets.py::TestPocoRate`/
        `TestPocoRateBreakdown` (на настоящем CSV, включая проверку
        разброса ставок внутри Barren), `tests/test_poco_tax.py::
        TestAutomaticRateFromPlanetsFile` (5 тестов: авто без ручного
        ввода, ручной ввод переопределяет авто, пробел при отсутствии
        обоих, старое поведение без `planets` не изменилось, то же для
        настоящих колоний), `tests/test_api.py` (сквозные тесты на оба
        эндпоинта + `/api/initial-data` отдаёт `poco_rate_breakdown`/
        `poco_snapshot_date`). Проверено вручную в браузере на реальной
        планете 57-KJB I (Barren, 3% в файле): без единого введённого
        значения расчёт дал ровно `105 638 400 × 0.03 = 3 169 152`
        налога на экспорт; ручной ввод 50% для Barren корректно
        переопределил его на `105 638 400 × 0.5`.

- [x] **Ручной ввод ставки POCO по типу планеты убран (17.09.2026).**
      Решение пользователя при переходе к мультирегиональности: с
      несколькими регионами переопределение «по типу планеты» перестаёт
      быть однозначным (один и тот же тип в разных регионах — разные
      наборы реальных ставок), только усложняет ввод без пользы.
      Единственный источник ставки POCO теперь — точная автоматическая
      ставка КОНКРЕТНОЙ планеты из `data/planet_industry.csv`
      (`domain/planets.py::PlanetBook.poco_rate()`, не изменился);
      планета без известной ставки — честный пробел (`missing_rates`),
      как и раньше, просто без запасного ручного варианта поверх.

      **Убрано:**
      - `domain/poco_tax.py::_resolve_rate()` — параметры `planet_type`/
        `poco_rates` убраны, резолвер теперь только читает
        `planets.poco_rate(system, planet)`. `evaluate_plan_profitability()`
        и `evaluate_colonies_profitability()` лишились параметра
        `poco_rates: dict[str, float]`.
      - `domain/planets.py::PlanetBook.poco_rate_breakdown()` и
        `POCO_TYPE_COLUMN` — были нужны только для честной подсказки
        над полем ручного ввода («в одном типе несколько ставок сразу»),
        без самого поля не нужны.
      - `api/blueprints/plans.py::_parse_poco_rates()`/`MAX_POCO_RATE` —
        валидация тела запроса `poco_rates`, оба эндпоинта
        (`/api/plan-profitability`, `/api/colonies-profitability`)
        больше не принимают и не ждут это поле.
      - `/api/initial-data` — поля `poco_rate_breakdown`/`poco_snapshot_date`
        (нужны были только для подсказки над убранным полем).
      - `web/index.html` — вся панель «Переопределить ставку POCO по
        типу планеты» (карточка «Где добываем»): HTML-поле, CSS
        (`.poco-rates`/`.poco-rate-item`), JS (`POCO_PLANET_TYPES`,
        `POCO_RATES_KEY`, `savePocoRates()`, `renderPocoRateInputs()`,
        `pocoRatesFromInputs()`), ключи словаря `T` (`pocoRatesLabel`,
        `pocoRateAuto`, `pocoRateBreakdownTitle`, `pocoRateUnknownTitle`),
        справка (RU/EN, оба места — прогноз для плана и для настоящих
        колоний) переписана под «только автоматика».
      - Тесты — `tests/test_planets.py::TestPocoRateBreakdown` удалён;
        `tests/test_poco_tax.py` переписан целиком на ставки только
        через `_FakePlanetBook` (без `poco_rates=`); `tests/test_api.py`
        — тест `/api/initial-data` на `poco_rate_breakdown`/
        `poco_snapshot_date` удалён, тесты валидации `poco_rates`
        (нечисловая ставка, ставка выше 100%) удалены как проверяющие
        код, которого больше нет, остальные переписаны на мок
        `_load_planets_book()` вместо `poco_rates` в теле запроса.
        `python -m pytest tests/ -q` — 433 прошли.
- [x] **Исправлена сводка «N простаивает»: колония, уже закончившая
      переработку в прошлом, не считалась простаивающей (17.09.2026,
      найдено пользователем на реальных данных).** Колония с несколькими
      перерабатывающими фабриками: часть отработала и остановилась
      (сырьё в причале кончилось), часть вообще ни разу не запускалась
      (сырьё до них просто не дошло) — но в сводке на дашборде такая
      колония не считалась «простаивает», хотя раньше считалась.

      **Причина** — в `simulateColonyFactories()` (`web/index.html`):
      `results.depletionMs` (время до исчерпания сырья в причале, на
      котором строится `isColonyIdle()` для переработки, см. запись
      16.09.2026 выше) присваивался только из перехода «работала →
      встала», случившегося строго ПОСЛЕ момента снимка
      (`t>nowMs && wasActive`, введено в тот же день чуть раньше,
      см. коммит `33af896`/`e4f7653`). Если причал опустел ещё ДО
      снимка — переход уже в прошлом (`t<=nowMs`) — присвоение не
      происходило вовсе, и `depletionMs` оставался `null` («неизвестно»,
      честный пробел), хотя колония на самом деле давно и однозначно
      простаивает.

      **Исправление:** условие `t>nowMs` убрано — присвоение теперь
      безусловно на каждом переходе «работала → встала», независимо от
      того, в прошлом это событие относительно снимка или в
      спроецированном будущем (переменная переименована в
      `lastIdleTransition` для точности). `depletionMs` в случае
      прошлого перехода получается отрицательным («истощилось NN назад»)
      — везде далее по коду (`fmtLeft()`, `isColonyIdle()`) `<=0` уже и
      так означает «кончилось», отдельной обработки не потребовалось.
      Статус отдельной фабрики («ещё ни разу не запускалась» —
      `pinCycleInfo(...).neverStarted`) не менялся — это разные, ранее
      уже разделённые сигналы: у фабрики — что происходит с ней самой,
      у колонии — исчерпан ли общий запас сырья в причале.

      Автотеста нет (проект не гоняет JS — правило `test_frontend_
      integrity.py`, см. её докстринг), проверено вручную в браузере
      через синтетический сценарий в консоли (1 фабрика отработала один
      цикл на 3000 ед. сырья и встала 40 минут назад, 4 фабрики без
      единого цикла на том же причале): `depletionMs` стал `-2 400 000`
      (−40 мин) вместо `null`, `isColonyIdle()` — `true` вместо `false`.
      `python -m pytest tests/ -q` — 439 прошли (правка не затрагивает
      Python-код).
- [x] **Тот же баг, второй случай (17.09.2026, найден пользователем сразу
      следом за первым).** Пользователь забрал из причала готовый продукт
      и не подвёз сырьё — причал стал пуст ЦЕЛИКОМ (не «опустел в ходе
      проекции», а пришёл пустым уже на снимке). Колония снова пропала из
      «N простаивает», хотя в деталях каждая фабрика честно показывала
      простой.

      **Причина** — тот же `simulateColonyFactories()`, но другая грань:
      `lastIdleTransition` (см. запись выше) присваивается только на
      НАБЛЮДАЕМОМ переходе «`entry.active` было true → стало false»
      внутри симуляции. Если причал уже пуст на старте, ни одна фабрика
      ни разу не может набрать цикл ни на одном шаге — `entry.active`
      остаётся тем же `false`, с которого стартовала переменная, весь
      прогон. Перехода просто НЕТ, потому что переходить не из чего — не
      «неизвестно», а «всё время симуляции была уже не активна», но код
      это не отличал от настоящего «неизвестно».

      **Исправление:** новый флаг `anyFactoryEverActive` — true, если
      ХОТЬ ОДНА фабрика хоть раз стала активной (`entry.active=true`) за
      весь прогон. Если по итогам прогона `lastIdleTransition` так и не
      присвоился, но в колонии есть хотя бы одна учитываемая фабрика
      (`anyFactory`) и `anyFactoryEverActive` осталось `false` —
      честная нижняя граница: простаивает как минимум с момента снимка
      (`gameLastUpdate`, раньше — не знаем, не гадаем), `lastIdleTransition`
      выставляется в `startMs`. Не путать с «фабрика ни разу не
      запускалась вообще» (`pinCycleInfo(...).neverStarted`, у неё нет
      `last_cycle_start`) — здесь фабрика РАНЬШЕ работала, просто на
      момент ЭТОГО снимка ей нечем разгоняться.

      Проверено вручную в браузере тем же способом (синтетический
      сценарий в консоли): причал и буферы фабрик пусты с нуля, у одной
      фабрики last_cycle_start из истории (час назад — час до снимка) —
      `depletionMs` стал `-3 600 000` (снимку было 60 минут) вместо
      `null`, `isColonyIdle()` — `true`. Отдельно проверено, что здоровая
      колония с большим запасом сырья по-прежнему не считается
      простаивающей (`depletionMs` далеко в будущем, `idle:false`) —
      правка не задевает нормальный случай. `python -m pytest tests/ -q`
      — 439 прошли (правка не затрагивает Python-код).
- [x] **Явный блок «Командные центры» в Excel-выгрузке (17.09.2026, по
      прямому запросу пользователя).** Лист «Сводка» раньше показывал
      только «тип планеты + количество» — какой именно командный центр
      нужен, подразумевалось неявно (тип планеты = тип КЦ), плюс
      бесполезная колонка «Роль», всегда с одним и тем же значением.
      Пользователь попросил явный отдельный блок с прямым указанием на
      командные центры, тем же смыслом, что и «Список закупки» в вебе
      (`renderShopping()`, web/index.html).

      **Реализация** (`api/blueprints/export.py`):
      - `_style_header()` получил `start_row` — заголовок таблицы можно
        сдвинуть вниз, освободив строку 1 под заголовок блока.
      - Лист «Сводка»: строка 1 — жирный заголовок
        `tr["cc_shopping_title"]` («Список закупки: командные центры» /
        «Shopping list: Command Centers»), строка 2 — шапка таблицы
        (тип планеты / количество / **новая колонка** «Командный центр»),
        данные — с строки 3. Колонка «Командный центр» — тоже формула
        (`=A{row}&" Command Center"`), не строка из Python, тем же
        принципом, что и COUNTIF рядом (лист должен пересчитываться при
        ручной правке). Итоговая строка переименована в «Всего
        командных центров» (было просто «Всего»).
      - Убраны колонка «Роль» и ключи `sum_role`/`sum_role_value` —
        несли одно и то же статичное значение на каждой строке, не
        нужны, когда есть явная колонка с названием командного центра.
      - Тесты — `tests/test_api.py::TestExport::
        test_summary_has_explicit_command_center_block` (новый),
        `test_summary_uses_formulas_not_precomputed_values` обновлён под
        сдвинутые строки. `python -m pytest tests/ -q` — 440 прошли.
        Проверено вручную содержимое сгенерированного `.xlsx` (openpyxl)
        на плане с двумя типами планет — заголовок блока, шапка,
        формулы обеих колонок и итоговая строка на ожидаемых местах.
- [x] **Выбор планет под переработку — сначала по налогу POCO, потом по
      радиусу; колонка налога в плане и в Excel (18.09.2026, найдено и
      запрошено пользователем на реальном плане).** Пользователь
      заметил, что часть перерабатывающих колоний встала на Storm, хотя
      Barren/Temperate в домашней системе хватало с запасом, и что
      налог POCO вообще не влиял на выбор планеты.

      **Причина первой части (найден настоящий баг).** `_cycle()` в
      `domain/factory_site.py` кругами перебирает ВЕСЬ список кандидатов
      (предпочтительные и остальные вперемешку) — если нужно больше
      колоний, чем РАЗЛИЧНЫХ предпочтительных планет в системе, обход
      в том же круге захватывал и другие типы, хотя правило 4 в
      докстринге модуля прямо говорит: одна планета несёт неограниченное
      число колоний, значит предпочтительные не могут «закончиться» по
      количеству — могут только не подойти по размеру. Тест
      `test_planet_shortage_is_solved_by_extra_colonies` эту регрессию
      не ловил (не проверял `used_fallback_types`), хотя воспроизводил
      её в своих же данных.

      **Исправление:**
      - `domain/factory_site.py::_pool()` (новая функция) — кандидаты
        только предпочтительных типов, пока среди них есть хоть один
        подходящий под шаблон по размеру; общий список (с другими
        типами) — только когда НИ ОДНА предпочтительная планета не
        подходит вовсе. `_cycle()` теперь получает этот пул, а не всегда
        полный список.
      - `PlanetCandidate.poco_rate: float | None` — из
        `PlanetBook.poco_rate()`, читается в `_candidates()`.
      - Порядок сортировки кандидатов (по прямому запросу пользователя,
        подтверждено: «налог → радиус»): `(не предпочтительный тип,
        ставка неизвестна, ставка по возрастанию, радиус по
        возрастанию)`. Радиус остаётся ЖЁСТКОЙ проверкой пригодности
        (`_fits()`, не изменилась) — порядок влияет только на то, КАКАЯ
        из уже подходящих планет используется первой.
      - Новые предупреждения (`domain/plan_messages.py`):
        `site_fallback_size_used` — когда предпочтительные планеты в
        системе есть, но ни одна не подошла по размеру под конкретный
        шаблон (раньше `used_fallback_types` считался, но нигде не
        показывался пользователю — мёртвый код, наконец получил
        применение); `site_higher_tax_used` — когда часть колоний
        вынужденно встала на планету с более высокой ставкой POCO, чем
        у лучшего использованного варианта. Оба не блокируют план (по
        решению пользователя: «предупреждать... или в плане помечать»,
        план остаётся рабочим).
      - `domain/planner.py::PlanRow.poco_rate` — ставка POCO конкретной
        планеты каждой строки (включая добывающие — тот же честный
        источник, что и у прогноза прибыльности), заполняется одним
        проходом в конце `build_plan()`.
      - `web/index.html::renderTable()` — новая колонка «POCO %» в
        таблице плана; строка подсвечивается (`.pct.warm`), если её
        ставка выше минимальной среди перерабатывающих строк плана —
        та же простая клиентская логика сравнения, что уже давно
        используется для подсветки CPU/PG ≥90%, без отдельного
        серверного флага на строку.
      - `api/blueprints/export.py` — колонка `poco_rate` в листе
        «План» (Excel), формат ячейки `0.0%` поверх хранимой доли.
      - Справка (RU/EN) переписана: явно назван порядок «сначала налог,
        потом радиус» и честно описано, когда используются другие типы
        планет. `domain/features.py` — новая запись
        `site_selection_by_poco_rate`.
      - Тесты — `tests/test_factory_site.py`: 4 новых
        (`test_does_not_fall_back_to_other_types_when_preferred_can_host_all_colonies`
        воспроизводит исходный баг и проверяет исправление;
        `test_lower_tax_preferred_planet_used_first_even_if_larger`;
        `test_warns_and_marks_fallback_types_when_preferred_too_big_for_template`;
        `test_warns_when_some_colonies_end_up_on_higher_tax_planet`).
        `tests/test_planner.py::test_poco_rate_filled_from_planets_file`,
        `tests/test_api.py::test_plan_sheet_includes_poco_rate_column`.
        `python -m pytest tests/ -q` — 446 прошли. Проверено вручную в
        браузере на dev-стенде: колонка POCO % отображается в таблице
        плана, план строится без ошибок в консоли.
- [x] **Бэкап Postgres — сжатый custom-формат вместо текстового SQL
      (18.09.2026, по прямому запросу пользователя).** Обсуждение
      пользователя: без сжатия размер дампа растёт линейно с базой (на
      18.09.2026 — 11 МБ, 14 копий вместе — 27 МБ, но это только начало).
      `scripts/backup.py::_backup_postgres()` — `pg_dump -Fc` вместо
      обычного `pg_dump`, файл `pidirector.dump` вместо `pidirector.sql`.
      Восстановление меняется на `pg_restore --no-owner --clean
      --if-exists -d pidirector файл` вместо `psql pidirector < файл` —
      обновлено в `deploy/README.md`. Тесты — `tests/test_backup.py::
      test_postgres_backup_uses_compressed_custom_format` (новый, проверяет
      флаг `-Fc` в команде), `test_backs_up_postgres_via_pg_dump` обновлён
      под новое имя файла. `python -m pytest tests/ -q` — 449 прошли.
- [x] **Добывающие колонии с истёкшей программой экстрактора не считались
      простаивающими (18.09.2026, найдено пользователем на реальных
      данных — тот же класс бага, что чинили для depletionMs у
      переработки 17.09.2026, только у добычи).** Десятки колоний с
      бейджем «программа завершена» на дашборде не попадали в сводку
      «N простаивает».

      **Причина.** `isColonyIdle()` (`web/index.html`) для добычи
      проверяла `!p.nearest_expiry` — «нет запущенной программы» —
      только полное ОТСУТСТВИЕ поля. Но `nearest_expiry` — это
      `expiry_time` последней программы экстрактора с ESI
      (`scripts/sync_colony_status.py::nearest_expiry()`), и игра НЕ
      обнуляет его, когда время истекает: поле остаётся тем же прошедшим
      моментом, пока игрок не перезапустит программу вручную. Значит
      «нет поля вовсе» — редкий случай (пинов экстрактора нет физически),
      а обычное «таймер истёк, но ещё виден» — куда более частый, и он
      не ловился вовсе.
      **Исправление:** `!p.nearest_expiry || Date.parse(p.nearest_expiry)<=Date.now()` —
      та же проверка `<=0`, что уже используют бейджи (`fmtBadgeLeft`/
      `fmtLeft`), а не только отсутствие поля. Проверено вручную в
      консоли браузера (истёкшая метка → простаивает, будущая метка →
      нет, отсутствие поля → простаивает, как и раньше). Правка только
      в JS, Python-тестов не затрагивает — `python -m pytest tests/ -q`
      — 449 прошли (без изменений в счёте).
- [x] **Дублирующаяся строка «План не построен» на дашборде убрана
      (18.09.2026, по прямому запросу пользователя).** В одной строке с
      кнопками вида/сортировки/экспорта (`#counts`) статично лежал текст
      «план не построен» (ключ `counts0`), а прямо под ней `#dashBody`
      показывал практически то же самое («План не построен. Откройте
      вкладку «Настройки»...») — один и тот же факт дважды.
      `#counts` заполняется реальной статистикой («N колоний · запас
      есть/нет») только ПОСЛЕ построения плана (`renderDash()`); пока
      плана нет, JS её вообще не трогал, и в DOM оставался статичный
      плейсхолдер из HTML. Убран ключ `counts0` (RU/EN) и текст из
      разметки — `#counts` теперь пустая строка по умолчанию, и
      `renderDash()` явно очищает её в ветке «плана нет», а не оставляет
      как есть. Проверено вручную в браузере: до построения плана строка
      с кнопками пуста, единственное сообщение — под ней; после
      построения `#counts` корректно показывает «N колоний · ...».
      `python -m pytest tests/ -q` — 449 прошли (правка только в JS).
- [x] **Выгрузка настоящих колоний в Excel, тем же форматом, что и план
      (18.09.2026, по прямому запросу пользователя: «раз можем
      экспортировать план, почему не колонии»).**

      **Поведение** (уточнено вопросами к пользователю в процессе):
      - Есть план, но нет колоний (или наоборот) — прежнее поведение,
        просто соответствующий набор листов.
      - Есть и план, и колонии — оба набора листов в ОДНОЙ книге:
        «План»/«Сводка»/«Проверка» (как раньше) плюс «Мои колонии»
        четвёртым листом.
      - Есть только колонии, плана нет — книга состоит из ОДНОГО листа
        «Мои колонии», без «Сводки» и «Проверки»: командные центры уже
        куплены, список закупки не нужен (решение пользователя —
        выбрано из двух предложенных вариантов).
      - Нет ни плана, ни колоний — кнопка (она и раньше не пряталась)
        по клику честно говорит «Нечего экспортировать: нет ни плана,
        ни колоний», без похода на сервер.
      - Простаивающая фабрика/экстрактор по-прежнему показывает
        назначенный продукт (Вход/Выход/Структуры не пустуют) — ESI не
        стирает schematic_id, когда истекает цикл, это разные вещи
        (уточнено вопросом к пользователю: «мы же можем получить данные
        программы фабрик из игры?» — да, можем).

      **Реализация** (`api/blueprints/export.py`):
      - `/api/export` принимает оба поля необязательными (`plan_data`,
        `colonies_data`), проверяет, что хотя бы одно непусто и оба в
        пределах лимита строк (переименован в `MAX_EXPORT_ROWS`).
      - `_colony_to_row()` — одна запись `/api/colonies` → строка того
        же формата COLUMNS, что и план: роль по составу пинов
        (фабрика/экстрактор), текущий продукт — `pin.product`
        (сохраняется независимо от состояния цикла), вход схемы — из
        `domain.throughput.load_schematics()`, радиус и ставка POCO —
        из того же `PlanetBook`, что и весь остальной расчёт
        (`radius_km()`/`poco_rate()`, честный `None` за пределами
        региона, не 0 и не среднее).
      - `_build_workbook()` разбит на переиспользуемые части:
        `_write_rows_sheet()` (общий для «Плана» и «Моих колоний»)
        и `_write_summary_and_check_sheets()` (только когда есть план).
      - `web/index.html::exportPlan()` — шлёт и `plan`, и `coloniesData`
        всегда (пустой массив, если нет); кнопка недоступна только при
        обоих пустых. Справка (RU/EN) и `domain/features.py`
        (`colonies_excel_export`) обновлены.
      - Тесты — `tests/test_api.py::TestExportColonies` (7 новых:
        только колонии → один лист; сопоставление полей строки; таймер
        истёк → продукт всё равно виден; план+колонии → 4 листа;
        отказ при пустых обоих; лимит строк; добывающая колония —
        роль и продукт экстрактора). `python -m pytest tests/ -q` —
        456 прошли. Проверено вручную в браузере: клик без плана и
        колоний — честный алерт; настоящий запрос с колонией из
        консоли — 200 и валидный `.xlsx`; клик по кнопке с той же
        колонией в `coloniesData` — файл скачивается, ошибок в консоли
        нет.
- [x] **HTTP 413 при экспорте настоящих колоний на реальном аккаунте
      (18.09.2026, найдено пользователем сразу после выкладки предыдущего
      пункта).** 87 колоний — nginx отказал телу запроса кодом 413.

      **Причина.** `client_max_body_size 1m` в nginx плюс раздутый
      payload: `exportPlan()` слал `coloniesData` КАК ЕСТЬ, а каждый пин
      несёт `extraction_history` — снимки скорости экстрактора, копятся
      с 13.09.2026 БЕЗ ограничения (`record_extraction_samples()`,
      `scripts/sync_colony_status.py`). На реальном счёте пользователя
      это уже ~21 000 записей на 87 колоний. `_colony_to_row()` на
      сервере (предыдущий пункт) эти записи вообще не читает — они
      просто ехали в теле запроса без всякой пользы.

      **Исправление:**
      - `web/index.html::coloniesForExport()` — новая функция, отдаёт
        ТОЛЬКО поля, которые реально читает `_colony_to_row()`
        (`character`/`system_name`/`planet_index`/`planet_type`/
        `cpu_percent`/`pg_percent` и per-pin `kind`/`product`), а не
        `coloniesData` целиком. `exportPlan()` шлёт результат этой
        функции, не исходный массив. На синтетическом тесте того же
        масштаба (87 колоний, по 240 записей истории на пин, как на
        проде) размер тела упал с 1832 КБ до 15 КБ — в 122 раза.
      - `client_max_body_size` в nginx поднят с `1m` до `3m`
        (`deploy/nginx.conf.sample`, применено и на проде) — разумный
        запас СВЕРХ основного фикса, не основной фикс сам по себе:
        решает именно эту раздутость, а не отменяет ограничение.
      - Проверено вручную в браузере: синтетический аккаунт масштаба
        прод-инцидента (87 колоний × 240 записей истории) — реальный
        запрос на `/api/export` с урезанным payload успешно вернул
        `200` и валидный `.xlsx`. `python -m pytest tests/ -q` —
        458 прошли (правка только в JS и nginx-конфиге, Python не
        затронут).
- [x] **Три ошибки в экспорте настоящих колоний, найденные пользователем
      на реальном файле сразу после первой выкладки (18.09.2026).**
      Прислан реальный `.xlsx`: все роли — «Переработка», «Вход» у
      добывающих колоний — `type_id:2307` вместо имени, «Констелляция»
      пустая на всех строках.

      **1. Роль всегда «Переработка», даже для добычи.**
      `_colony_to_row()` проверял фабрики РАНЬШЕ экстрактора
      (`"proc" if factory_pins else "mine" if extractor_pins`) — колония
      с экстрактором И фабриками на той же планете (обычная оптимизация:
      перерабатывать сырьё сразу на месте, не вывозя P1 — оказалась
      ТАК построена почти вся коллекция колоний реального пользователя)
      всегда получала «Переработка». Уже существующий `realRows()`
      (`web/index.html`, карточки на дашборде) решает это ПРАВИЛЬНО —
      экстрактор первым; при добавлении экспорта эту приоритетность
      забыли повторить. Исправлено: экстрактор снова решает роль первым.

      **2. «Вход» показывает `type_id:2307` вместо имени сырья.**
      Источником входа факторной строки был `domain.throughput.
      load_schematics()` (`data/schematics.json`) — тот резолвит имена
      входов только по `data/type_ids.json`, где нет сырья R0 (оно не
      структура и не продукт с иконкой), и честно, но нечитаемо
      подставляет `f"type_id:{id}"` (`scripts/extract_schematics.py`).
      В проекте уже есть правильный источник для входа P1-рецепта —
      `domain.recipes.py::Recipe.source` (проверено по источникам,
      правило 2), тот же способ уже используют `recipe_inputs` в
      `/api/initial-data` (`api/blueprints/reference.py`). Переключено
      на него; для P2+ вход по-прежнему `Recipe.inputs` (те имена
      резолвятся верно — это продукты с иконками, не сырьё).

      **3. Констелляция всегда пустая.**
      `/api/colonies` ESI-снимок не несёт констелляцию вовсе (только
      систему), а `_colony_to_row()` просто подставлял `None`, не
      пытаясь её найти — хотя данные для этого есть в том же файле,
      что и весь остальной расчёт. Добавлен `PlanetBook.constellation_of()`
      (`domain/planets.py`, по образцу `radius_km()`/`poco_rate()`) и
      подключён в `_colony_to_row()`.

      **Заодно:** «Структуры» для настоящих колоний теперь тоже
      `factory_summary={"factories": N}` (как у расчётного плана), а не
      голая русская строка — переводится на язык листа сам, вместо
      того чтобы застревать по-русски в английской книге.

      Тесты — `tests/test_planets.py::TestConstellationOf` (новый),
      `tests/test_api.py::TestExportColonies` (+3: смешанная колония →
      добыча, вход по имени не по type_id, проверка колонки
      констелляции в уже существующем тесте). Отдельно перепроверено на
      РЕАЛЬНЫХ данных прода (colony пользователя theancientRoman/
      WMH-SO/1: экстрактор Felsic Magma + 10 фабрик Silicon) —
      `_colony_to_row()` теперь верно даёт `role_key='mine'`,
      `constellation='BASILISK'`, `res_out='Felsic Magma'`, без утечки
      type_id. `python -m pytest tests/ -q` — 462 прошли.
- [x] **Вход/выход комбинированной колонии показывали сырьё экстрактора,
      а не реальный P1-продукт (18.09.2026, найдено пользователем сразу
      после выкладки предыдущего пункта).** Роль стала верной («Добыча»),
      но «Выход» остался сырым продуктом экстрактора (например, Felsic
      Magma), хотя колония реально производит и продаёт P1 (Silicon) —
      фабрики на той же планете перерабатывают его на месте; «Вход» стал
      пустым вовсе, хотя сырьё для фабрики известно (это же продукт
      экстрактора). Цитата пользователя: «вместо получаемого p1 продукта
      на майнерах экспортируется p0... на перерабатывающих колониях мы
      же как-то получили вход и выход правильно, что мешает это сделать
      для добычи».

      **Причина.** Предыдущий фикс (пункт выше) свёл вход/выход к одному
      и тому же переключателю, что и роль (`role_key`) — при `role_key
      == "mine"` вход/выход брались ТОЛЬКО с экстрактора, полностью
      игнорируя фабрики на той же планете, хотя реальный конечный
      продукт колонии — то, что выпускают фабрики.

      **Исправление.** `_colony_to_row()`: роль (`role_key`, ярлык
      «Добыча»/«Переработка» для колонки B) по-прежнему решает
      экстрактор первым — без изменений. Но вход/выход/структуры теперь
      не завязаны на `role_key` — они смотрят на `factory_pins`, если
      такие есть на планете, независимо от роли: выход — продукт
      фабрики, вход — продукт экстрактора той же планеты (сырьё местное,
      `Recipe.source`/`.inputs` не нужны — они для привозного сырья).
      Только если фабрик на планете вообще нет — вход/выход возвращаются
      к чистой добыче (выход — сырой продукт экстрактора, входа нет).
      Заодно возвращён `heads` в урезанный payload `coloniesForExport()`
      (`web/index.html`) — трим для фикса HTTP 413 (пункт выше) случайно
      обрезал это поле, из-за чего «Структуры» чистой добычи показывали
      «1 фабрик» вместо реального числа голов экстрактора (эффекта не
      было заметно раньше, пока «Выход» и так был неверным).

      Тесты — `tests/test_api.py::TestExportColonies` (переписан
      `test_combined_colony_is_classified_as_mining_not_processing`:
      комбинированная колония теперь ожидает выход фабрики/вход
      экстрактора, не наоборот; добавлен
      `test_pure_mining_colony_without_factories_reports_raw_output`
      для колонии без фабрик — поведение как раньше, плюс проверка
      `heads` в колонке «Структуры»). Перепроверено на тех же РЕАЛЬНЫХ
      данных прода (theancientRoman/WMH-SO/1: экстрактор Felsic Magma
      (10 голов) + 10 фабрик Silicon) — `_colony_to_row()` теперь даёт
      `role_key='mine'`, `res_out='Silicon'`, `res_in='Felsic Magma'`,
      `factory_summary={'factories': 10}`, `constellation='BASILISK'`.
      `python -m pytest tests/ -q` — 463 прошли.
- [x] **Номер планеты показывался дробным числом («AV-VB6 16.0»), а не
      римской цифрой, как в игре (18.09.2026, найдено пользователем).**
      Цитата: «Число должно быть целое без точки и нуля... сделать как
      в игре — номера планет везде отображать римскими цифрами (касается
      и моих колоний)».

      **Причина.** `data/planet_industry.csv`, колонка «Planet» — числа
      без дробной части, но pandas читает её как `float64` (нет строки,
      которая заставила бы читать как `int64`), и дробность нигде не
      убиралась на всём пути до фронтенда: `domain/planner.py`/
      `api/blueprints/reference.py` собирают `PlanRow.planet` как
      `str(row.get("Planet", ""))` → «16.0» прямо в JSON-ответе
      `/api/calculate`. У настоящих колоний (ESI, `planet_index` —
      целое число) такой дробности не было, но и в игровые римские
      цифры они не переводились — арабское число «4» тоже не то, что
      показывает сама игра.

      **Исправление.** `domain/planets.py::planet_number_to_roman()` —
      новая функция, целое/дробное/строковое на входе -> римская цифра
      (`4` -> `IV`, `16.0` -> `XVI`). Подключена везде, где номер
      планеты ПОКАЗЫВАЕТСЯ пользователю:
      - `web/index.html` — новая `romanNumeral()`, обёрнуты все места
        отрисовки (подсказки орбов, панель колонии, синтетические коды
        юнитов вроде «A0-VIII», строки таблицы плана, карточка «худшей»
        планеты по запасу). Поле `.planet`/`.planet_index` САМО осталось
        числом везде, где по нему матчат/сортируют/шлют на сервер
        (`matchColony()`, `matchPlanRow()`, сортировка по системе, тело
        запроса `/api/colonies-profitability`) — иначе
        `parseInt("XVI",10)` сломал бы сопоставление плана с настоящими
        колониями.
      - `api/blueprints/export.py::_write_rows_sheet()` — общий writer
        листов «План» и «Мои колонии» (18.09.2026, более ранний пункт
        этой же фазы), колонка «Планета» теперь тоже римская цифра;
        отдельно то же самое для листа «Проверка» (свой writer, не
        разделяет код с `_write_rows_sheet()`).

      Тесты — `tests/test_planets.py::TestPlanetNumberToRoman` (новый:
      целое, float «16.0» как реально приходит из PlanetBook, JSON-строка
      «16.0», строка ESI «1», ноль — честно без римской формы, нечисловое
      значение — как есть, `None` — пустая строка), `tests/test_api.py`
      (обновлён `test_colony_row_maps_real_data_to_plan_columns`: `E2`
      теперь `"IV"`; новый `test_plan_sheet_shows_planet_number_as_roman_numeral`
      — и «План», и «Проверка»). Проверено вручную в браузере:
      построен план (Dev Miner Partial → LBGI-2 VIII, Dev Factory Chief
      → 4HS-CR II/VIII), панель «Детали» колонии показывает и системный
      бейдж, и коды юнитов («A0-VIII») римской цифрой. `python -m
      pytest tests/ -q` — 471 прошли.

### Фаза 10 — предложено пользователем (16.09.2026, в очереди, не начато)

Занесено по прямому запросу — только зафиксировать и оценить
реализуемость, не приступать без отдельного явного запроса на каждый
пункт (правило проекта: обсуждение и лог — не то же самое, что
разрешение начать).

- [ ] **Инкрементальные бэкапы Postgres (WAL-архивирование).** Занесено
      18.09.2026 по итогам обсуждения с пользователем сжатия дампа (см.
      выше, тот же день, выполнено). Сейчас `scripts/backup.py` снимает
      ПОЛНЫЙ независимый `pg_dump` каждый день — с ростом базы это
      неэффективно: каждая копия хранит всё состояние целиком, даже
      если между снимками изменилась малая часть. Настоящий инкремент в
      Postgres — не хэши файлов дампа (это почти ничего не даст: база
      синкает колонии каждые 30 минут, дамп почти всегда отличается хоть
      чем-то), а WAL-архивирование: один полный `pg_basebackup` раз в
      какое-то время плюс непрерывная архивация журнала между снимками,
      с возможностью восстановиться на любой момент, а не только на
      время последнего полного дампа.
      **Условие применения — прямое решение пользователя:** внедрять,
      только когда база реально займёт заметное место на диске, не
      раньше. На 18.09.2026 это 11 МБ на 15 ГБ диска (7.6 ГБ свободно) —
      сжатого полного дампа хватает с огромным запасом; заводить
      отдельный процесс архивации WAL сейчас означало бы сложность ради
      сложности (правило проекта — не нагружать систему сверх нужного).
- [ ] **Прямое производство P0→P2 на одной планете.** Код уже есть
      (`domain/direct_p2.py`, Фаза 4) и полностью реализован, но
      выключен в интерфейсе решением пользователя от 15.09.2026
      (`direct_p2 = False` в `api/blueprints/plans.py`) — застройка
      посчитана из стоимостей отдельных структур, а не выписана из
      проверенного игрового шаблона (такого шаблона на два экстрактора
      + P1 + P2-фабрики в наборе DalShooth нет), и ни разу не сверена
      в игре. Это не разработка с нуля, а **проверка в игре**: снять
      флаг, построить план с `direct_p2=true`, реально застроить хотя
      бы одну такую планету и сверить настоящие CPU/PG с расчётом.
      Риск — только в цифрах застройки, не в архитектуре.
- [ ] **Мультирегиональность.** Спланировано 17.09.2026 (план см. ниже),
      план утверждён пользователем, **реализация не начата — ждёт
      отдельной явной команды на каждую фазу**, как и остальные пункты
      этого раздела.

      **Проверено 16.09.2026 (правило 11 — источник истины, не память):**
      ни ESI, ни SDE плотность/абундантность сырья по планетам не
      отдают вообще — это не наш пробел, а то, как устроена игра.
      ESI даёт только уже построенные колонии конкретного персонажа
      (`/characters/{id}/planets/*`) и справочник схем
      (`/universe/schematics/{id}/`), для ещё не колонизированных
      планет данных о ресурсах там нет (руководство CCP по PI,
      developers.eveonline.com/docs/guides/pi/, ни словом об этом не
      упоминает). SDE абундантность тоже не публикует — сервер считает
      её процедурно. По вики EVE University, единственный способ узнать
      состав планеты — сканировать её в игре (скилл Remote Sensing)
      либо взять готовую стороннюю базу (eve-webtools.com,
      ellatha.com Planetary Production Database) — обе сами собраны тем
      же ручным сканированием, не через API CCP. Скелет планеты
      (Constellation/System/Planet/Type/Radius) — наоборот, механически
      выводится из официального SDE-экспорта (`mapPlanets.jsonl`,
      `mapSolarSystems.jsonl`, `invTypes.jsonl`) для ЛЮБОГО региона —
      только плотность остаётся ручным пробелом.

      **Утверждённый план (17.09.2026), файлы/детали — см. историю
      планирования; краткое изложение:**
      1. Справочник планет **переезжает из CSV в БД** (новые таблицы
         `regions`/`planets` через SQLAlchemy+Alembic, `data/planet_industry.csv`
         удаляется после разовой миграции скриптом
         `scripts/migrate_planets_csv_to_db.py`). `domain/planets.py::load_planets()`
         строит тот же `PlanetBook` (тот же публичный API — `radius_km()`,
         `poco_rate()`, `regions()`, `systems_in()` и т.д. не меняются),
         но из БД, а не из файла.
      2. Новый разовый/по расписанию сборщик `scripts/refresh_sde.py`
         (в `scripts/scheduler.py`, по образцу `refresh_market_prices.py`)
         наполняет `regions`/`planets` скелетом **по всем регионам New
         Eden сразу** из официального SDE-бандла — плотность и POCO при
         этом не трогает, если строка уже существует. Регион без
         достаточных данных плотности помечен статусом `no_data` и не
         показывается пользователям в планировщике (правило 1 — план не
         должен считаться на догадках).
      3. Новый admin-раздел (`api/blueprints/admin.py`, `web/admin.html`),
         доступ — allowlist по `character_id` через переменную окружения
         `PI_ADMIN_CHARACTER_IDS` (приложение многопользовательское,
         правка справочника планет влияет на расчёты всех, поэтому не
         открыта всем подряд; неавторизованным — 404, не 403). В разделе:
         список всех регионов игры со статусом, скачивание CSV-шаблона
         по региону (снимок текущего состояния БД — что уже есть,
         дозаполнить плотность), загрузка заполненного файла обратно, и
         ручное редактирование планет по одной без файла.
      4. Кросс-региональная валидация плана: домашняя система
         (переработка) обязана быть в том же регионе, что и выбранные
         для добычи constellations, если для добычи выбран один регион;
         если выбрано несколько — домашняя система обязана быть в одном
         из НИХ (не в произвольном третьем). Проверяется на сервере
         (`domain/planner.py::PlanRequest`), а не только сужением
         выпадающего списка на фронтенде.
      5. Кнопка «Весь регион» переименовывается в «Все регионы» только
         когда пользователю доступно больше одного региона; при одном —
         без изменений в тексте и поведении.
      6. 28 колонок плотности для приостановленной фичи "прямое P0→P2"
         (`domain/direct_p2.py`) в новый workflow не входят — только у
         уже существующих данных по Fountain (переносятся как есть при
         миграции), для новых регионов эти колонки не заполняются, пока
         `direct_p2` не разморозят отдельным решением.

      Реализация делится на 5 фаз/PR: (1) схема БД + миграция, (2)
      allowlist + пустой admin-blueprint, (3) admin CRUD + `refresh_sde.py`
      + шаблон/загрузка файла, (4) доступность региона в планировщике +
      валидация + кнопка, (5) документация вместе с (1) и повторно с
      (3)-(4). Наполнение БД данными по регионам, помимо переноса
      Fountain, — отдельная задача уже после того, как сам инструмент
      (админ-интерфейс) существует, не часть этого плана.
- [x] **План и выгода только по переработке из закупаемого P1
      (18.09.2026, по прямому запросу пользователя).** Не строить
      добывающие колонии вовсе: план и прибыльность считаются в
      предположении, что весь нужный P1 покупается на бирже, а строятся
      только перерабатывающие колонии P2→P4.

      **Цена закупки — по прямому уточнению пользователя.** Первый
      набросок этого пункта предлагал `sell_min` (цена продавца — то,
      что реально стоит купить мгновенно). Пользователю прямо показали
      разницу между `sell_min` и `buy_max` (верхний buy-ордер, тот же,
      которым уже оценивается выручка от продажи готового продукта) —
      пользователь сознательно выбрал **`buy_max`**: «цены закупки будем
      брать jita buy», подтверждено повторно после явного предупреждения
      о разнице. Решение зафиксировано, второй словарь цен не заводился
      — `evaluate_plan_profitability()` использует ТОТ ЖЕ `prices`
      (`buy_max`), что и выручка/налог на импорт.

      **Реализация:**
      - `domain/throughput.py::Demand` — новое поле `purchased_p1:
        dict[str, float]` (P1, единиц в час). `expand_demand()` — новый
        параметр `purchase_p1: bool = False`: при `recipe.tier=='P1'`
        продукт становится листом дерева (не разворачивается в P0, не
        попадает в `demand.factories`/`missing`), записывается в
        `purchased_p1`. Дерево выше P1 (P2→P3→P4) разворачивается той
        же функцией, без дублирования.
      - `domain/planner.py::PlanRequest` — новое поле `purchase_p1: bool`.
        `build_plan()` передаёт его в `expand_demand()` и добавляет
        `result.assume("p1_purchased_on_market")`. Блоки размещения
        переработки и добычи (оба итерируют `demand.factories.items()`)
        **не потребовали явного `if`** — раз P1 не попадает в этот
        словарь в режиме закупки, добывающий блок естественно не находит
        ничего для размещения. `surplus_mining` явно отключён вместе с
        `purchase_p1` (излишек добычи бессмысленен без добычи вовсе).
        `PlanResult.to_dict()["purchased_p1"]` — новое поле ответа.
      - `domain/plan_messages.py` — новый код `p1_purchased_on_market`
        (RU/EN) — план честно показывает в «Допущениях», что это не
        обычный расчёт (правило 1).
      - `domain/poco_tax.py::evaluate_plan_profitability()` — новый
        параметр `purchased_p1`; стоимость закупки (`rate * HOURS_PER_MONTH
        * price`) вычитается из чистой прибыли ОТДЕЛЬНО от налога POCO на
        ввоз того же P1 на планету переработки (этот налог уже считался
        как обычно — ему всё равно, откуда физически взялся материал).
        Новое поле `PlanProfitability.monthly_purchase_cost` (`None`,
        не 0, когда закупки нет — правило 1).
      - `api/blueprints/plans.py` — `purchase_p1` в `/api/calculate`
        (включён в `cache_key()`), `purchased_p1` — необязательное поле
        `/api/plan-profitability` (клиент пересылает то, что вернул
        `/api/calculate`).
      - `web/index.html` — чекбокс «Покупать весь P1 на бирже» в
        карточке «02 — Где добываем» (`togglePurchaseP1()`): визуально
        гасит список созвездий и запас на истощение (`.disabled-section`),
        сам подставляет ВСЕ констелляции региона той же
        `pickAllConstellations()`, что и кнопка «Весь регион» — иначе
        `factorySys` (домашняя система) остался бы пустым, так как
        заполняется именно из выбранных констелляций; предыдущий выбор
        пользователя запоминается и возвращается при выключении. Новая
        плитка «Закупка P1 / мес» в прогнозе прибыльности плана —
        только когда `monthly_purchase_cost != null`.

      Тесты — `tests/test_planner.py::TestPurchaseP1` (5 тестов: нет
      строк добычи, есть строки переработки, P1 учтён как закупленный
      не построенный, допущение записано, `to_dict()` отдаёт
      `purchased_p1`) и `TestSharedInputs::test_purchase_p1_stops_expansion_at_p1`;
      `tests/test_poco_tax.py::TestPurchaseP1` (3 теста: стоимость
      закупки уменьшает чистую прибыль, отсутствующая цена — честный
      пробел, а не 0, обычный план не показывает «закупка: 0»);
      `tests/test_api.py` — HTTP-контракт `/api/calculate` и
      `/api/plan-profitability`. `python -m pytest tests/ -q` — 487
      прошли. Проверено вручную в браузере (`preview_start`): чекбокс
      включает подстановку всех констелляций и гасит список, план на
      P2-продукте строит только переработку.
- [x] **Подсказка «персонажей больше, чем нужно» не знала про
      purchase_p1 и переставала предлагать продукты раньше, чем пул
      реально заполнялся (18.09.2026, найдено пользователем сразу после
      выкладки предыдущего пункта).** Реальный случай: аккаунт на 84
      колонии, построенный план на закупаемом P1 занял только 24 —
      пользователь честно следовал подсказке («Взять» до тех пор, пока
      панель не пропала), но подсказка молчала о настоящем остатке.

      **Причина.** `domain/advice.py::advise()`/`_chain_size()`/
      `_candidates()` считали через `domain/profit.py::colonies_for()`,
      который безусловно вызывает `expand_demand()` БЕЗ `purchase_p1` —
      то есть всегда закладывает добывающие колонии в `needed_colonies`/
      `spare_slots`, даже когда `build_plan()` их в этом режиме вообще
      не строит. Подсказка «пул заполнен» срабатывала на цифре, которая
      была ЗАВЫШЕНА относительно того, что реально уйдёт в план —
      реальный остаток оставался неиспользованным и не сообщённым.

      **Исправление.** `colonies_for()` — новый параметр `purchase_p1`,
      пробрасывается в `expand_demand()` (та же граница на P1, что и в
      `build_plan()`). `_chain_size()`/`_candidates()`/`advise()`
      (`domain/advice.py`) получили тот же параметр и передают его
      насквозь. `api/blueprints/plans.py::advice()` — читает
      `purchase_p1` из тела запроса `/api/advice`.
      `web/index.html::loadAdvice()` — шлёт `purchaseP1`, и
      `togglePurchaseP1()` сразу пересчитывает подсказку при переключении
      чекбокса, не дожидаясь, пока пользователь тронет список продуктов.
      `domain/profit.py::rank()`/`evaluate()` (карточка «Что выгоднее
      производить») сознательно не тронуты — это независимый от текущих
      настроек плана справочник, не привязанный к purchase_p1.

      Тесты — `tests/test_profit.py::test_purchase_p1_reports_zero_mining_colonies`,
      `tests/test_advice.py::TestPurchaseP1` (4 теста: добыча не входит в
      needed_colonies, запас больше при том же пуле, низкий CCU пула не
      мешает при закупке P1, предложения не требуют добывающих слотов),
      `tests/test_api.py::test_advice_purchase_p1_excludes_mining_from_needed_colonies`.
      `python -m pytest tests/ -q` — 495 прошли. Проверено вручную в
      браузере: с тем же выбором продукта («Biocells») подсказка «занято
      из 65» показала 5 колоний в обычном режиме и 1 колонию сразу после
      включения чекбокса — без повторного клика по продуктам.
- [x] **Двойной шаблон переработки планировался на КАЖДУЮ колонию тира
      по одному биту «есть ли хоть один CCU5 во всём пуле», а не по
      числу реально свободных CCU5-персонажей — план массово проваливал
      колонии с «не хватило персонажей» (18.09.2026, найдено
      пользователем сразу после предыдущего пункта).** Реальный случай:
      план на закупаемом P1 из 8 разных продуктов использовал только 24
      колонии из ожидаемых ~84, с 37 предупреждениями — пользователь
      честно следовал подсказке «Взять», подсказка была уже исправлена
      предыдущим пунктом, но сам расчёт всё равно проваливался.

      **Причина, в два слоя.** (1) `domain/factory_site.py::select_factory_sites()`
      решал, ставить ли двойной шаблон (`p2p3_2factory`/`p4_2factory`,
      экономит планеты), одним условием `ccu_level >= min_ccu_level`,
      где `ccu_level` — МАКСИМУМ CCU по ВСЕМУ пулу персонажей
      (`domain/planner.py::build_plan()`, `max(c.command_center_upgrades_level
      for c in characters)`). Если хоть один персонаж имел CCU V, план
      готовил ВСЕ колонии тира как двойные — а `pool.take(require_ccu5=True)`
      начинал отказывать, как только настоящие CCU5-персонажи
      заканчивались, и все оставшиеся колонии тира проваливались с «не
      хватило персонажей», хотя обычных IC-слотов оставалось много.
      (2) Второй, менее очевидный слой: одно двойное НАЗНАЧЕНИЕ (одна
      планета) — это ДВА РАЗНЫХ персонажа, не один (`build_plan()` зовёт
      `pool.take(planet=...)` дважды с одной и той же планетой; второй
      раз тот же персонаж уже исключён — второй колонии там нужен кто-то
      ещё). Первый черновик фикса этого не учёл и ограничивал число
      двойных НАЗНАЧЕНИЙ числом свободных CCU5-СЛОТОВ напрямую (не делил
      пополам) — половина «спланированных» двойных назначений всё равно
      проваливалась на своём втором персонаже.

      **Исправление:**
      - `domain/planner.py::_CharacterPool.free_slots_matching(require_ccu5=False, min_ccu=0)` —
        новый метод: сколько слотов свободно у персонажей, отвечающих
        условию, не привязано к конкретной планете (в отличие от
        `take()`). Не путать с уже существующим свойством `free_slots`
        (общий остаток без фильтра) — имя нарочно другое, чтобы не
        столкнуться с ним.
      - `domain/factory_site.py::select_factory_sites()` — новый
        параметр `max_double_templates`: ограничивает число ДВОЙНЫХ
        назначений, а не подменяет их одиночными молча — предел делится
        на 2 при переводе «слоты» → «назначения» (см. причину выше).
        Сверх предела ставятся одиночные шаблоны так же, как и нечётный
        хвост — без `allow_single_template_fallback` и без блокировки:
        это не отказ «двойной не помещается» (та проверка не менялась),
        а честный учёт того, кем его укомплектовать. Новое сообщение
        `site_ccu5_slots_exhausted` (RU/EN, `domain/plan_messages.py`) —
        информационное, не критическое.
      - `build_plan()` — передаёт `pool.free_slots_matching(require_ccu5=True)`
        в `select_factory_sites()` ПЕРЕД каждым вызовом (P2_P3, затем
        P4) — снимок на тот момент уже учитывает персонажей, разошедшихся
        по предыдущим тирам/прямому P2. Заодно: одиночные назначения
        (`assignment.template_count == 1`) теперь берут персонажа через
        `prefer_least_skilled=True` (как уже делает добыча) — не тратят
        CCU5 на то, что в нём не нуждается, оставляя его тиру/назначению,
        которому он действительно нужен.
      - Тесты — `tests/test_factory_site.py::TestMaxDoubleTemplates`
        (4 теста: без предела — прежнее поведение, откат на одиночный
        при нехватке слотов, ноль слотов — всё одиночное, предела с
        запасом — без предупреждения), `tests/test_planner.py::TestCcu5Scarcity`
        (2 теста на уровне build_plan(): вся потребность закрывается при
        дефиците CCU5, откат — не блокирующее предупреждение). Проверено
        на точном воспроизведении реального сценария (8 разных
        P2/P3/P4-продуктов, закупка P1, смешанный пул 4×CCU5+8×CCU4+4×CCU3,
        96 слотов) — до фикса: 24 построенные колонии, 30
        предупреждений; после первого (неполного) фикса: 60 колоний;
        после деления предела на 2: все 84 колонии построены, ноль
        предупреждений о нехватке персонажей. `python -m pytest
        tests/ -q` — 501 прошли.

### Фаза 11 — предложено пользователем (17.09.2026, в очереди, не начато)

Занесено по прямому запросу — только зафиксировать и оценить
реализуемость, не приступать без отдельного явного запроса на каждый
пункт (то же правило, что и у Фазы 10).

- [x] **Фильтр по тиру (P2/P3/P4) в карточке «Что производим» (17.09.2026).**
      Кнопки P2/P3/P4 над `#productList` (карточка «01 — Что производим»,
      вкладка «Настройки») — сужают список ВМЕСТЕ с текстовым поиском
      (`#prodSearch`), не вместо него; ни одна не нажата — показаны все
      тиры, как и раньше. Можно нажать несколько сразу (P2+P4, например).

      **Реализация:**
      - `api/blueprints/reference.py::_initial_payload()` — новое поле
        `product_tiers: {имя продукта: тир}` в ответе `/api/initial-data`,
        рядом с уже существующим `recipe_inputs` (тот же принцип: карта
        по имени продукта, только для тех же P2-P4 из `PROCESSING_TIERS`).
      - `web/index.html` — `productTiers`/`activeTiers` (глобальные),
        `PRODUCT_TIER_ORDER=['P2','P3','P4']`, новый блок `#tierFilter`
        в разметке карточки, `renderTierFilter()`/`toggleTierFilter()`,
        `renderProducts()` дополнительно фильтрует по
        `activeTiers.has(productTiers[n])`. Кнопки — не переведены (P2/
        P3/P4 — термины EVE, как и названия типов планет), новый CSS-
        класс `.mini.active` для нажатого состояния.
      - Тесты — `tests/test_api.py::test_initial_data_includes_product_tiers`.
        `domain/features.py` — новая запись `product_tier_filter`, справка
        (RU/EN) дополнена упоминанием кнопок-фильтров. `python -m pytest
        tests/ -q` — 436 прошли. Проверено вручную в браузере: нажатие P4
        сужает список до 8 продуктов этого тира.
- [x] **Переработка кнопки входа SSO и карточки персонажей (17.09.2026).**
      3 живых подпункта реализованы, 4-й снят пользователем ранее.

      1. **Кнопка «Войти через EVE SSO» пропадает из топбара, когда
         залогинен хотя бы один персонаж; вместо неё в карточке
         персонажей — «Добавить персонажа» с той же функцией.**
         `renderCrew()` (`web/index.html`) переключает
         `#ssoBtn.style.display` по условию `crew.some(c=>c.esi_linked)`
         (не просто `crew.length` — на dev-стенде с одними заглушками
         `source=dev` кнопка входа не должна пропадать, входить неоткуда
         было бы иначе).
      2. **Подсказка «Персонажи хранятся на сервере…» (`crewHint`)
         заменена кнопкой «Добавить персонажа»** — рендерится в тот же
         `#crewHint`, только когда есть хоть один esi-персонаж (иначе
         блок пуст, вход — через топбар).
      3. **Основной персонаж — по умолчанию первый залогиненный в этом
         браузере, группа собирается автоматически.** Открытый вопрос
         решён пользователем: ручная перегруппировка остаётся — кнопка
         «Сделать основным» у каждого альта (не общая «Сохранить
         группу» и не радиокнопки выбора, они упразднены).

         **Реализация:**
         - `api/blueprints/auth.py::_store()` — персонаж БЕЗ постоянной
           группы (`primary_id is None`) больше не остаётся негруппированным
           до отдельного `/api/auth/group`: если в текущем визите уже
           есть esi-персонажи с установленным основным — новый
           подтягивается к нему; если есть, но основной ещё не
           установлен (переходный случай для входов ДО этой правки) —
           основным становится наименьший `character_id` среди них;
           если персонаж первый в визите — становится основным сам
           себе. Ветка для уже сгруппированных персонажей (`primary_id
           is not None`, проверка `GroupConflict`) не изменилась.
         - `group_characters()` (`/api/auth/group`) — тот же код, новый
           смысл: не «завести группу», а «переназначить основного»,
           докстринг обновлён.
         - `infra/models.py::Character.primary_character_id` — комментарий
           обновлён, схема не менялась (то же nullable-поле).
         - `web/index.html::renderCrew()` — радиокнопки `primaryPick`
           убраны; у основного — статичная метка «основной», у альтов
           (только когда группа реально из 2+ человек) — кнопка
           «Сделать основным» (`makePrimary()`, тот же `/api/auth/group`,
           без промежуточного состояния «выбрано, но не сохранено»).
           Блок «Сохранить группу» и функция `saveCharacterGroup()`
           убраны целиком.
         - Справка (RU/EN) переписана под автогруппировку; ключ `T.crewHint`
           заменён на `T.addCharacter`, `groupSave`/`groupHint`/
           `groupSaveFail` получили новый смысл («сделать основным»,
           не «сохранить группу»), `groupPickPrimary` убран как
           более не нужный (нет состояния «выбрано, не сохранено»).
           `domain/features.py::character_grouping` — ключевые слова
           и заголовок обновлены под новый текст справки.
         - Тесты — `tests/test_auth.py::TestAutoGrouping` (3 теста: первый
           вход → основной себе, второй вход в том же визите → авто-
           подтягивание, переходный случай с уже существующим
           негруппированным персонажем визита). `python -m pytest
           tests/ -q` — 439 прошли. Проверено вручную в браузере (через
           подменённый `crew` в консоли, т.к. на dev-стенде нет
           настоящего SSO): SSO-кнопка пропадает, «Добавить персонажа»
           появляется, метка «основной» и кнопка «Сделать основным»
           рендерятся верно, клик шлёт `POST /api/auth/group`.
      4. ~~Ручное назначение персоналу роли (добыча/P1→P2/P2→P3/
         P3→P4) с редактированием после построения плана, персонаж без
         обоих скиллов ≥4 не допускается ни к одной роли~~ — **пункт
         снят пользователем целиком (17.09.2026)**, до реализации.
         Пороги по скиллам остаются в точности как сейчас, без
         изменений: `MINER_MIN_CCU=4` (`domain/planner.py:103`)
         по-прежнему гейтит только допуск к майнинг-шаблону по CCU, а
         `interplanetary_consolidation_level` по-прежнему используется
         только как формула количества слотов планет (`IC+1`), не как
         ценз допуска к роли.

После сверки с четырьмя источниками оба спорных числа закрыты — ничего
не возвращается в ответе `/api/calculate` в поле `assumptions` (кроме
`extraction_margin`, но это не спорное число, а сознательная
пользовательская поправка):

1. ~~Отношение цикла advanced к basic~~ — **подтверждено**: вики EVE
   University прямо указывает цикл advanced-фабрики в 1 час против
   30 минут у basic. Там же подтверждено следствие: выход двух
   basic-фабрик направляется в одну advanced без потерь.
2. ~~Число планет на персонажа~~ — **подтверждено**: `Interplanetary
   Consolidation + 1` в материалах источника прямо не было указано,
   подтверждено пользователем по игре 16.09.2026 — из ответа
   `/api/calculate` больше не возвращается как допущение
   (`data/pi_reference.json` → `assumptions.planet_slots_per_
   character`, статус «подтверждено»).

## 7. Открытые вопросы (требуют уточнения, не додумываю)

- ~~Содержимое `templates/` и `web/`~~ — **вопрос закрыт**: файлы получены.
  `templates/` оказалась не Jinja2-шаблонами, а JSON-шаблонами застройки
  планет; `web/index.html` — готовый самодостаточный SPA.
- ~~Лимиты CPU/PG командных центров и формула штрафа по радиусу~~ —
  **вопрос закрыт**: данные взяты из DalShooth/EVE_PI_Templates и заложены
  в `data/pi_reference.json`.
- **Расхождение внутри источника**: README предупреждает, что miner-шаблон
  с CCU5 и 16 головами перегружает PG на планетах больше 15 000 км, но
  пересчёт по его же таблицам даёт предел ~26 000 км. Второе предупреждение
  (2 фабрики P2/P3 выше 12 500 км) с таблицами совпадает точно. До выяснения
  планировщику безопаснее держать консервативный порог из README.
- ~~Какому варианту соответствуют суммы `5700/9800`~~ — **закрыто**: это
  вариант **LS** (2 экстрактора, 4 basic-фабрики, 7 линков). Используется
  только **00** (1 экстрактор с 10 головами, 8 фабрик, 10 линков).
- ~~Нужно ли разбирать сами JSON-файлы шаблонов~~ — **закрыто**: да,
  реализовано в `domain/templates.py`.
- **Расчётное ограничение, не описанное в источнике**: 00-шаблон майнера
  требует Command Center Upgrades **IV** и выше — при CCU III он
  перегружает PG (107.7%) на планете любого размера. Стоит перепроверить
  в игре, прежде чем закладывать в планировщик как жёсткое правило.
- ~~Стоимость второго причала~~ (5200 или 7200 CPU) — решает, применим ли
  двойной P4-шаблон на планетах крупнее 9 600 км. **Закрыто без проверки
  (16.09.2026):** в регионе Fountain нет Barren/Temperate планеты нужного
  радиуса (~20 000 км), проверить в игре не на чем — оставлено
  консервативное значение 7200 CPU (см. раздел 6, Фаза 1).
- ~~Не известно, какой конкретно регион описывает `planet_industry.csv`~~ —
  **закрыто (16.09.2026): регион — Fountain**, подтверждено пользователем
  напрямую. Согласуется и с самим файлом: `POCO Owner = INIT` у видимых
  констелляций — The Initiative (INIT) реально держит суверенитет в
  Fountain (dotlan.net/region/Fountain, проверено). При
  мультирегиональности для каждого следующего региона понадобится такая
  же ручная выгрузка (сканирование в игре или сторонняя проверенная
  база): ни ESI, ни SDE плотность сырья по планетам не отдают (проверено
  16.09.2026, см. Фаза 10).
