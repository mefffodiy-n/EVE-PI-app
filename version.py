"""
Версия приложения.

Держится отдельным модулем, чтобы её можно было прочитать и из API,
и из скриптов, не поднимая Flask.

Правила нумерации: MAJOR.MINOR.PATCH.
  0.x  — идёт Фаза 1, публичного API ещё нет и он может меняться;
  MINOR растёт с каждой законченной фазой из roadmap.md;
  PATCH — исправления в пределах фазы.
"""

VERSION = "0.9.0"

# Что уже работает — показывается в окне «О проекте», чтобы отличать
# реализованное от запланированного и не обещать лишнего. Двуязычно:
# окно «О проекте» есть на обоих языках.
PHASE = {
    "ru": ("Фазы 3–6 по коду закрыты: EVE SSO, синхронизация скиллов и колоний, "
           "служба и бэкапы. Осталось получить client_id от CCP и развернуть."),
    "en": ("Phases 3–6 are code-complete: EVE SSO, skill and colony sync, "
           "service and backups. Left: get a client_id from CCP and deploy."),
}


# Контакт для CCP. Заменить на свой перед публичным развёртыванием:
# по нему с вами свяжутся при проблемах с нагрузкой вместо блокировки.
CONTACT = "https://github.com/mefffodiy-n/EVE-PI-app"


def user_agent() -> str:
    """
    Строка User-Agent для всех внешних запросов.

    Собирается здесь, а не пишется в каждом сборщике: раньше версия
    была вписана в трёх местах и разошлась — 0.5.0, 0.6.0 и 0.1.0
    одновременно. CCP просит представляться, и представляться
    по-разному из одного приложения незачем.
    """
    return f"PI-Director/{VERSION} (planetary industry planner; +{CONTACT})"
