# Зависимости для Фазы 1 (без ESI/workers/Postgres — они появятся в Фазе 2-3,
# см. roadmap.md). Сравните с requirements.txt v1: там версии были зафиксированы
# на сентябрь 2023, что уже устарело.
#
# Верхние границы (>=) указаны сознательно вместо точных пинов: на момент
# написания я подтвердил через PyPI только актуальную версию fastapi
# (0.141.1, июль 2026) — по остальным пакетам точные текущие версии не
# проверялись, поэтому не фиксирую цифры, которые могу дофантазировать.
# Перед первым `pip install` зафиксируйте точные версии через
# `pip-compile` / `poetry lock`, чтобы получить актуальный и воспроизводимый lock-файл.

fastapi>=0.141
uvicorn[standard]>=0.30
pydantic>=2.7
pandas>=2.2
openpyxl>=3.1
httpx>=0.27

# Тесты (в v1 отсутствовали вовсе — см. roadmap.md, раздел "Итоги аудита")
pytest>=8.0
