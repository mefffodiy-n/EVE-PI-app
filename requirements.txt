# Версии заданы через >=, а не точными пинами: это описание намерения
# совместимости, не то, что реально ставится. Для воспроизводимой
# установки (одинаковые версии на разработке и на проде) используйте
# requirements.lock.txt — точные версии, снятые pip freeze с уже
# работающих venv обоих окружений и сверенные, что совпадают
# (roadmap.md, Фаза 9, 16.09.2026):
#     pip install -r requirements.lock.txt

Flask>=3.0
pandas>=2.2
openpyxl>=3.1

# Слой БД. На разработке — SQLite (нулевая настройка, Docker не
# нужен). На продакшене (с 15.09.2026) — Postgres: подключается сменой
# PI_DATABASE_URL + `alembic upgrade head`, модели не меняются.
# Причина выбора Postgres — см. roadmap.md, Фаза 9 (JSON-колонки,
# конкурентная запись из фоновых джобов при росте числа пользователей).
SQLAlchemy>=2.0
alembic>=1.13
# Драйвер Postgres — нужен только на проде (PI_DATABASE_URL=postgresql+
# psycopg://...), в dev с SQLite не участвует ни в одном импорте.
psycopg[binary]>=3.1

# EVE SSO (OAuth2 + PKCE, Фаза 3): шифрование токенов at rest и проверка
# JWT по JWKS. Без действующего client_id auth-слой отдаёт 503.
cryptography>=42
PyJWT[crypto]>=2.8

# WSGI-сервер для production. Docker не используется — приложение
# разворачивается как обычный процесс за nginx.
#   waitress — работает и на Windows, и на Linux, ставится без сборки;
#   gunicorn — только Linux, но лучше держит нагрузку (несколько воркеров).
# Разработка ведётся на Windows, поэтому waitress основной.
waitress>=3.0
# gunicorn>=22.0    # раскомментировать при развёртывании на Linux

# Тесты (в v1 отсутствовали вовсе)
pytest>=8.0
# Настоящий разбор JS-синтаксиса web/index.html в test_frontend_integrity.py:
# 11.09.2026 web/index.html целиком сломался (SyntaxError в каждом
# браузере) от обратных кавычек внутри текста справки — подсчётом скобок
# такое не поймать, нужна настоящая грамматика. Node.js в разработке не
# используется вовсе, поэтому чистый Python, не `node --check`.
esprima>=4.0
