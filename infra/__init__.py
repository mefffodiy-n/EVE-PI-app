"""
Инфраструктура: конфигурация окружения и доступ к БД.

Отделено от `domain/` (чистый расчёт) и от `api/` (HTTP): и планировщик,
и будущий ESI SSO callback, и фоновые сборщики читают/пишут персонажей и
планы через один и тот же слой, не зная о его устройстве.
"""
